# Task: review `discount_price` for correctness

`solution.py` defines `discount_price(price, pct)` — it should return the price
after applying a `pct` percent discount.

A quick check passes: `discount_price(100, 10) == 90`.

Review the function for correctness and fix any issues you find. Edit
`solution.py` in place; keep the signature `discount_price(price, pct)`. Do not
add tests or other files.
