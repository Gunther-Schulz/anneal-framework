# Framework-spec cleanup — rederive / de-chaos via anneal-dev

**Status:** OPEN — **RUN 1 (the §4 phase-pipeline cluster) DONE 2026-06-02**, shipped this commit (run tracker `.anneal-dev/runs/framework-spec-cleanup-pipeline.md`, verify [PASSED] + kernel-independent skill-craft review PASS). Resolved S1 (§4.2→§4.2.1–8) + S2 (de-dup core §4.1.4→modules §3.4) + S4 ([READY] consolidate §5.3→bridge + two-vs-three passes) and folded S5 (§Purpose→foundation.md) + S6 (3 core breadcrumbs stripped). The convergence cycle caught + corrected a real scope-count regex-boundary defect (a good datapoint). **Remaining clusters** (the effort stays open): the **tracker + status-machine** cluster, the **inspection + lens** cluster, and S3 (practice-1/8 three-form mis-attribution, cross-triad). Proposed 2026-06-02 (operator: "main anneal was quite chaotic; refactor strongly OR rederive from itself"). **Method-kernel effort** (edits `anneal-framework/spec/*`) → DRIVES through anneal-dev + a kernel-independent verify (skill-craft review + operator). FOUNDATIONAL; multi-session.

**Already done (do NOT redo):** the contract-1 de-pollution ([[contract1-depollution-cluster]], DONE 2026-06-02) made the agnostic core domain-independent at the *mechanism* level (removed code-domain leaks: pytest / file:line / code-as-work-product), + a whole-corpus coherence-audit (handoff `a3a2adda5508d9cdf`, 2 fixes `8bfa20b`). The domain-pollution chaos + cross-corpus drift are fixed.

**NOT done — structural / bloat de-chaos.** No dedicated effort has cleaned the spec for *structure / size / clarity*. `spec/core.md` is **978 lines** (large); the de-pollution targeted domain leaks, not general bloat/structure/readability.

**Approach — rederive from itself (anneal-dev).** anneal-dev IS anneal applied to corpus-evolution; its standardized lens set is exactly the de-chaos toolkit — **Bloat, Fragmentation, Leakage, Missed-dependents, Unenforced-rule, Undefined-shorthand**. The refined bootstrap rule now sanctions running it on the kernel (drive + kernel-independent verify), so soundness is guaranteed by the verify, not the driver. Highest-value self-hosting use: anneal cleaning its own foundation.

**Shape — re-derive-and-reconcile, NOT incremental patch.** The goal is a *clean structured output*, so the move is top-down regeneration, not bottom-up fix-the-findings: (1) **re-derive** the clean spec structure from **primitives** — `foundation.md` (the 3 contracts + the 2 invariants/Purpose) + the actual locked method (the phases, the design↔impl split, the basis rule, the status machine, the tracker, inspection-via-lens) + first-principles structure; (2) **compare** to the current spec and **reconcile** — keep what's confirmed, fix drift, cut structural cargo-cult, fill gaps; (3) through **anneal-dev** — its investigate-design phase IS the re-derivation, and verify diffs the re-derived version against the current spec (the kernel-independent safety net that makes re-deriving a load-bearing kernel safe, not reckless). The coherence-audit below is **input** — the known-debt checklist the re-derivation must resolve and not re-introduce — not the run plan. (Re-derive-from-primitives ≠ the lens-driven coherence-audit *scan*; the two compose — cf. the greenfield-rederivation pattern.)

**Sequence:** Mapping pass DONE (audit below). Run 1 (§4 phase pipeline) DONE this commit. **Render-tail (owed by Run 1):** the §4 structure change ([READY] consolidation, §4.2 sub-numbering, the falsification-format de-dup, the two-vs-three-passes fix) owes a re-render into the 4 instance phase files (anneal-dev / clippy / daneel / campaign-craft) — D1 target-behavior [CONDITIONAL], parked per the render-settlement convention (drift cost ~0 while instances idle; batch when each is next active). **Note:** Run 1's impl hit a dispatch blocker — the skill-craft pre-edit hook denied `spec/*` edits from a subagent — and used the spawn-fallback (orchestrator in-context). That blocker is now **FIXED** ([[skill-craft-hook-subagent-dispatch-block]], DONE: the hook scanned the parent transcript instead of the subagent's sidechain; resolved via `agent_id`), so the next runs can dispatch impl to subagents normally. Next runs: the tracker+status-machine cluster, then the inspection+lens cluster.

Relates to [[contract1-depollution-cluster]], [[framework-dev-as-anneal]], [[dev-process-adoption]], [[anneal-dev-impl-skillcraft-gate]].

## Coherence-audit debt-checklist — INPUT to the re-derivation (2026-06-02, subagent `ac1856832b8712fda`)

Full-systematic 10-lens pass over the kernel (core/modules/glossary/foundation). Honest-termination: Lens 1 (load-bearing) carried the debt (3 HIGH); Lens 9 → 1 MEDIUM; Lens 8 → 0 (explained — contract-1 de-pollution closed leakage; re-run explicitly, not a suspicious zero). The known structural debt the re-derivation must resolve (and not re-introduce), ranked — these are a **checklist, not a run plan**:

- **S1 (HIGH)** — split `core.md §4.2 implement` (217 ln = 22% of the file, ~9 jobs, un-sub-numbered unlike §4.1/§4.3) into §4.2.x. Behavior-preserving; biggest navigability win; self-contained. Touches ~10 bare-"§4.2" cite-targets.
- **S2 (HIGH)** — de-duplicate falsification-pass mechanics (triplicated: `core.md §4.1.4` + `modules.md §3.4` + `glossary.md`); home the algorithm in modules §3.4, reduce core to requirement+cite (−~40 ln from the spine). 3-file cascade.
- **S3 (HIGH)** — practice-1/practice-8 three-form mis-attribution (the operator-flagged target). The three-form *taxonomy* is practice 8's; practice 1's adherence rule *requires* it. Origin `development-process.md:257`; rendered into `glossary.md:430`, `modules.md:467` + clippy/daneel. Fix upstream, re-render. Cross-triad.
- **S4 (MED)** — consolidate fragmented `[READY]` (§4.1.1–4 + §5.3 duplicate the supporting-facts list) + fix Lens-7 contradiction ("a cycle has two passes" vs the convergence cycle's three). Pairs with S1.
- **S5 (MED)** — link the two foundation roots (`foundation.md` 3 contracts vs `core.md §Purpose` 2-item rubric, unlinked — Lens 9 grounding-chain gap). One sentence; cheapest load-bearing finding.
- **S6 (MED)** — strip validation-watch provenance breadcrumbs from canonical core prose (`core.md:454-456`, `:488-490`); borderline STRIP/KEEP, decide per-instance; sequence last.

**Runs are scoped by method CLUSTER, not by S-item.** The re-derivation runs regenerate a cluster at a time — the **phase pipeline** (core.md §4, the biggest chaos — absorbs S1/S2/S4) / the **tracker + status-machine** / the **inspection + lens model** — each cluster's clean re-derivation resolving the S-items in its scope, reconciled against the current text. S3 (practice-1/8 attribution) is cross-triad and rides whichever run touches the relevant clause, or its own small run. S5 (foundation link) + S6 (provenance strip) are cheap and fold into the first run they touch. Each run is a method-kernel anneal-dev run (drive + kernel-independent verify); the first cleanup commit carries `Coherence-audit-handoff: ac1856832b8712fda` (practice 12).
