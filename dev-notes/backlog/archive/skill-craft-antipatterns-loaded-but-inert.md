# Skill-craft anti-patterns describe this session's failures but didn't FIRE — loaded-but-inert

**Status:** [DESIGN] — surfaced 2026-06-06 (operator probe: "would skill-craft have caught this?").
A **skill-craft** finding (captured in the anneal-framework backlog as the triad's working hub;
when worked it is a skill-craft-canonical edit → runs through anneal-dev + skill-craft's own
Layer-4 self-review mandate). **Deliberate, not now** — a marathon-session capture; classify
before any edit (per skill-craft's own Iterative-narrowing). Frames the "should we open
skill-craft work?" question.

## The pattern
Across this session, skill-craft's anti-patterns **named** several failures *after the fact* but
did **not fire to prevent** them — even when skill-craft was invoked. They are **loaded-but-inert**
(skill-craft's own Iterative-narrowing classification: exists + loaded, but didn't fire → fix the
trigger / add structural enforcement, NOT add new content):

1. **Sibling-clause form-propagation (the triggering instance).** Campaign ② sharpened the
   **dependents** clause of `core.md §3.2.2` (named the search-blind dependent forms) — *with
   skill-craft in the loop* (edit-time invocation + the verify form-review). The **behaviors**
   clause sits at `§3.2.2:247`, immediately below, with the *same* form-weakness (a "must
   enumerate" with no completeness floor / un-fakeable artifact). Skill-craft's form-review **ran
   on §3.2.2 and did not audit the sibling clause for the parallel weakness.** Result: the
   behaviors-clause weakness shipped un-sharpened → the clippy Unit-5 side-effect drop
   (`replacement-side-effect-behavior-parity`). Naked-judgment + un-fakeable-artifact *describe*
   the weakness; they didn't fire on the sibling.
2. **The v-entry gold-plating.** Skill-craft was invoked before the edits, yet the **Additive-reflex**
   anti-pattern didn't prevent the cycle-7 discriminator over-addition — because the gold-plating
   happened at a *design-decision* moment (actioning a surfaced finding), where skill-craft wasn't
   the active lens (anneal-dev's convergence was). Loaded, but not consulted at the moment it
   mattered → `convergence-surfaced-finding-action-brake`.
3. **(general)** Naked-judgment *describes* `§3.2.2`'s behaviors-clause weakness but never fired
   on it — see (1).

## Why this is the real skill-craft question (not "add the sibling rule")
The naive fix — "add an anti-pattern that says audit siblings" — is itself the **Additive-reflex**
(ironic). The diagnosis is **loaded-but-inert**, so the skill-craft-discipline-correct fix is a
**trigger / firing** sharpening, not new content:
- **Sibling-form-propagation:** extend the Layer-4 **Amendment discipline** (which currently says
  "scan every home of the *same concept* + audit each home on amendment" — same-concept-multi-file)
  to also cover **structural-sibling clauses with the same FORM-weakness** ("when you strengthen a
  clause's form — add a completeness floor, name search-blind forms, replace a weak artifact —
  audit its parallel-structured siblings for the same weakness"). Distinct from the existing
  same-concept-multi-file case.
- **Wrong-moment firing (gold-plating):** the Additive-reflex / Edit-as-Pareto checks fire at
  *edit-time*; the gold-plating was a *design-decision*-time move. Either skill-craft's checks need
  to reach design-decision moments, or (better, per level) anneal-dev's convergence carries the
  requirements-anchored brake (`convergence-surfaced-finding-action-brake`) — so this leg may be an
  anneal-dev fix, not skill-craft. Classify at cycle-open.

## Recommendation on "open skill-craft work?"
**Yes, it's legitimate — but deliberately, not at the end of this session.** A skill-craft-canonical
edit triggers skill-craft's Layer-4 self-review mandate (a real cycle), and per skill-craft's own
Iterative-narrowing the first step is **classify each leg** (gap / unloaded / loaded-but-inert) —
which decides whether the fix is a skill-craft trigger-sharpening or an anneal-dev convergence
change. Open it as its own cycle with the empirical base above; do NOT reflexively add anti-patterns.

## Relates to
- `replacement-side-effect-behavior-parity` — the shipped-consequence of leg (1) (the behaviors
  clause the sibling-audit would have sharpened).
- `design-decision-implication-depth-gaps` — the general "sharpened A, didn't connect B" shape;
  leg (1) is the rule-edit-layer instance of it (skill-craft is the would-be catcher that didn't).
- `convergence-surfaced-finding-action-brake` — leg (2)'s home (the gold-plating; likely an
  anneal-dev fix, not skill-craft — to classify).
- `framework-blindspot-class-analysis` / `coherence-audit` (the skill) — the cross-corpus
  sibling-audit is closer to these (periodic/lens-driven) than to skill-craft's per-edit review;
  the per-edit propagation discipline is the skill-craft-specific piece.
- Origin: operator probe "would skill-craft have caught this?" (2026-06-06), grounded in the
  campaign-② §3.2.2 near-miss.
