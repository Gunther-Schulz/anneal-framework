# Backlog — ARCHIVED (2026-08-05, operator backlog-clear GO)

The one-file-per-item backlog corpus (67 items + the ordered index,
now `archive/README-index-2026-06.md`) was archived wholesale: most
items tuned or presupposed the anneal-dev dispatch machinery that
the direct-path release loop (development-process.md, "The release
loop") retired as the default the same day; the rest were June-era
design questions with no firing evidence since. The LIVE queue is
root `BACKLOG.md`. Re-mint only on fresh incident evidence through
the normal flow — never by resurrecting an archived file.
