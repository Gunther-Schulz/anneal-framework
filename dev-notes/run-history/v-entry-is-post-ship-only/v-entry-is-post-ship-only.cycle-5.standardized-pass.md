# Cycle 5 — standardized inspection pass

Run: `v-entry-is-post-ship-only` · cycle 5 (D2 corrected per F8/F10). Scope: edits to
`dev-notes/validation-watch/README.md` (dev-notes companion doc). The corrected delta is
LEANER than cycle 3's (the FIX-SHIPPED axis-sharpen is dropped; Vocab → pointer added).

- **Bloat** — clean. Each added passage is load-bearing: the WATCHING anchor
  ("already-shipped decision") carries the post-ship fix (kills the pre-ship misreading);
  the canonical exclusion carries R1; no restatement. The corrected version REMOVES the
  cycle-3 FIX-SHIPPED axis-sharpen (F8 showed it non-load-bearing + mis-fitting) — a net
  reduction. basis: D2-corrected delta (b) + F8.
- **Fragmentation** — clean, improved. The exclusion is stated once (preamble). The
  Vocab state lines → pointer to the Entry-lifecycle canonical def ELIMINATES the
  pre-existing two-canonical-homes duplication that had already drifted (`:42` vs `:128`,
  F10). The V-vs-backlog principle stays cross-confirming across README/practice-8/CLAUDE
  (one canonical home + references), not duplicated. basis: F10 + D2-corrected delta (b).
- **Unenforced-rule** — clean (appropriately). Filing-shape ("un-implemented gap vs
  shipped-thing-watch") is a judgment discipline, backstopped by post-run self-review +
  Q7's per-state walk + operator; resists mechanization → prose-discipline is the right
  form. basis: D2-corrected (c)/(d).
- **Undefined-shorthand** — clean. "already-shipped decision" is descriptive, used
  consistently; the cycle-3 "remedial-fix axis" coinage (which risked a new
  multiply-read term) is gone. basis: D2-corrected delta (b).
- **Missed-dependents** — clean. D0 enumeration holds under corrected β: in-file
  references to WATCHING/FIX-SHIPPED (transitions `:78-82`, RESOLVED `:60-71`,
  INVALIDATED `:73-76`) reference the states BY NAME, not by the pre/post-fix or
  choice-vs-fix axis → unbroken. Vocab → pointer (no second def to maintain). Cross-doc:
  Q7 definition-agnostic (F5/F9, no-edit), CLAUDE.md (D3) + practice 8 (D4) say "shipped
  fix/choice" (β-consistent). 28 V-entry labels preserved (no relabel). basis: D0 + F5 +
  located reads `:60-82`.
- **Leakage** — N/A (project dev-notes companion, not domain-general spec).
- **Lossy-render** — N/A (no rendered-plugin-clause target; no source-rule paraphrase).
- **Over-claimed-verification** — N/A (no new completeness/verification claim beyond
  D2-corrected's audited located-read bases).
