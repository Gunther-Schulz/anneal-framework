# Cycle 8 — convergence cycle: intent-falsification pass

Run: `v-entry-is-post-ship-only` · cycle 8 (convergence, attacking the cycle-7 refined D2).
Dispatched fresh-context (subagent `a6e584c1056052508`, opus), criteria-first vs R1/R2/R3.
New surfaces: practice 8 read in full (`development-process.md:288-315`); V-16/V-17 re-read.

## Per-R# attack lines
- **{R1, SERVED}** — the de-collided exclusion (cycle-7 drop of "decided-against") does NOT
  over-tighten: V-16/V-17 self-classify "not classifiable upfront" so they don't match "a
  *classifiable* not-yet-built fix"; they stay watches. R1 over-tightening refuted.
- **{R2, finding}** — F-1/F-2 below (the inlined discriminator + umbrella asymmetry). No HARD
  contradiction; the issues are with prose I ADDED in cycle 7.
- **{R3, SERVED}** — re-confirmed: all WATCHING edge shapes (V-16/17 accepted-residual, V-30
  class-recurrence, V-31 rule-residual) fit "an already-shipped decision"; none mis-routed.

## Per-finding lines (orchestrator disposition — note: resolved by REMOVAL)
- **F14 (F-1) [VERIFIED — surfaced → resolved by removal]** the cycle-7 inline "The line
  (practice 8): … a classifiable not-yet-built fix is backlog" MISATTRIBUTES to practice 8 a
  rule it doesn't state — practice-8's actual discriminator (`:305-308`) routes a *form-in-hand
  classifiable fix* to **commit-now (n=1)**, NOT backlog. The inline was a cycle-7 over-addition.
  basis: `development-process.md:305-308` vs cycle-7 inline text.
- **F15 (F-2) [VERIFIED — surfaced → resolved by removal]** umbrella granularity asymmetry —
  README 3-way ("choice, rule, accepted residual") vs CLAUDE/practice-8 2-way ("fix/choice").
  Consistent, not contradictory; resolved by dropping the 3-way enumeration (the cycle-7
  over-elaboration). basis: cross-doc read.
- **F16 (F-3) [VERIFIED — addressed]** D2's cycle-7 basis stale-cites practice 8 `:293-299` for
  the discriminator (which lives `:305-308`). Addressed by removal — the minimal D2 has no
  discriminator clause, so no such citation. basis: `:293-299` vs `:305-308`.
- **D2 [INVALIDATED]→[PENDING]** → reverted to MINIMAL (cycle 9).

## Orchestrator judgment (operator-steered)
Operator flagged overthinking mid-cycle. Assessment: cycles 4/6 caught REAL defects; cycle 7
over-added the discriminator; cycle 8 nitpicked it. Reading practice 8 confirms its "Entry
hygiene" (`:310-315`) ALREADY routes a noticed gap out of validation-watch → the core fix is
small. Reverting to the minimal design (cycle 9). The minimal design is a SUBSET of the
cycle-5/7 designs that convergence cycles 6/8 already attacked with R1/R3 SERVED; every delta
found was in the removed elaborations. Halting the convergence loop is surfaced to the operator
at [READY] (operator owns the gate).
