# model-axis-judgment-conversion — cycle 8 standardized pass

Lens set: core ∪ supplement (empty). Cycle-8 work: opus control experiment (operator-approved;
artifact `.cycle-7.opus-control-experiment.md`) + F25 + re-forms D2(c8)/D3(c8)/D5(c8) +
D6 corrected + D8 basis-strengthened.

- **Missed-dependents** — finding-resolved: D2's target set now includes modules.md §3.3 (the
  cycle-7 falsification's gap); D8's dependent set independently confirmed by the opus sweep
  (daneel located; campaign-craft/bauleitplan matrix-gated). This-cycle basis: D2(c8), D8
  sub-annotation.
- **Undefined-shorthand** — finding-resolved: F21's un-homed token eliminated — bare enum member
  + sibling `ordering-basis: declared/assumed` closed two-member field (D2(c8)(vii)); no
  qualifier-on-enum shape remains. This-cycle basis: D2(c8).
- **Unenforced-rule** — finding-resolved: D5 now names tasked checkers (§4.1.1 supporting fact +
  fresh-context presence check in the §4.1.4 attack); D6 backstop cited. This-cycle basis:
  D5(c8), D6(c8).
- **Over-claimed-verification** — finding-resolved: D3's "self-adjusts regardless of the pinned
  floor" over-claim DROPPED; the boundary cell is now comparison-free by construction, and
  inert-by-default is stated openly rather than implied away. This-cycle basis: D3(c8), F23, F25.
- **Bloat** — clean: the sibling header field replaces (not adds to) the malformed qualifier
  idea; the §3.3 clause is one brief-parameter line; the §4.1.1 fact is one enumerated item.
  This-cycle basis: D2(c8), D5(c8).
- **Fragmentation / Leakage / Lossy-render** — out of scope this cycle: no principle restated
  outside its home, kernel wording stays harness-agnostic (model names remain in the instance
  binding/config), no render committed.
