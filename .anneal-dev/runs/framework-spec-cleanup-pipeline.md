# Run: framework-spec-cleanup-pipeline

- **Status:** PASSED
- **Phase:** verify
- **Mode:** interactive
- **Task:** re-derive-and-reconcile `core.md §4` (the phase pipeline) from primitives — the first re-derivation run of the framework-spec-cleanup effort. Resolve coherence-audit debt S1/S2/S4 (in §4) + cheap-fold S5/S6. Method-kernel edit (`anneal-framework/spec/*`) → driven through anneal-dev + kernel-independent verify (skill-craft self-review + operator).

> Work product: the `anneal-framework` repo (one container; run invoked from here).
> Coherence-audit debt-checklist `ac1856832b8712fda` is INPUT, not run-plan
> (`dev-notes/backlog/framework-spec-cleanup.md`). Rendered §4 dependents
> (anneal-dev / clippy / daneel / campaign-craft phase files) are target-behavior
> [CONDITIONAL] — discharged at the release-loop re-render + verify render-fidelity,
> not edited in this run.

---

## F-track

F1 [VERIFIED — addressed by D2] `core.md §4.2 implement` is 217 ln (462–678), un-sub-numbered, carrying ~9 bold-prefixed topics — unlike §4.1 (§4.1.1–4 sub-numbered). — basis: heading map `grep -nE '^#{1,4} ' spec/core.md` → §4.2 at 462, §4.3 at 679 (217 ln between); §4.1 has §4.1.1–4, §4.2 has zero sub-headings. [S1]
F2 [VERIFIED — addressed by D3] Falsification-pass artifact format is duplicated: `core.md §4.1.4` (391–436) and `modules.md §3.4` (326–380) both fully spell the candidate/shape closed-set/predicate closed-set/coverage-check; `glossary.md` (141–186) carries the definitional layer. — basis: located reads of all three spans; core 398–409 and modules 339–359 both enumerate `any-match`/`any-outside-scope`/`expected-match`. [S2]
F3 [VERIFIED — addressed by D4] `[READY]` spec is fragmented across §4.1.1 / §4.1.2 / §4.1.3 / §4.1.4 + §5.3; §4.1.1 (294–305) and §5.3 (911–919) duplicate the supporting-facts list (no finding [INVALIDATED]; no load-bearing finding < [VERIFIED]; every decision [VERIFIED]/[AUTO-ACCEPTED]). — basis: located reads of §4.1.1 (294–305) and §5.3 (910–924). [S4]
F4 [VERIFIED — addressed by D4] Contradiction: `core.md §4.1` (253) + `glossary.md` Pass entry (232–233) assert every cycle has "exactly two" passes; `core.md §4.1.4` (363–365) gives the convergence cycle three (investigation + standardized + falsification). — basis: read core:253 ("A cycle has two passes, in order:"), glossary:232–233 ("Every cycle has exactly two, in order:"), core:363–365 (the three-pass enumeration). [S4 / Lens-7] [`core.md:473` "exactly two **paths**" is a different referent — not a passes site.]
F5 [VERIFIED — addressed by D5] `core.md §Purpose` (10–24, the 2-item rubric) and `foundation.md` (the 3 contracts) are the two foundation roots and are unlinked. — basis: `grep -n foundation spec/core.md` returns no §Purpose-level citation; foundation.md:3 names "three contracts", core.md:12 names "two things". [S5]
F6 [VERIFIED — addressed by D6] Validation-watch provenance breadcrumbs sit in canonical core prose at 3 sites: §4.1.2 (core:328 "(V-5)"), §4.1.4 (core:455–456 "recorded in V-5"), §4.2 (core:489 "see …validation-watch.md V-24"). — basis: `grep -rnE 'V-[0-9]+|validation-watch' --include='*.md' spec/ foundation.md` → core sites 328/455–456/489; README:88–94 fixed-decision rule states "that provisional nature is understood, never written into the spec." [S6]

## D-track

D1 [VERIFIED] Scope: this run edits `core.md §4` (phase pipeline) + the reconciliation-reach — `core.md §5.3` (S4), `core.md §Purpose` (S5), `modules.md §3.4` (S2 de-dup home), `glossary.md:232–233` + `core.md:253` (S4 passes), `core.md:328/455–456/489` (S6); intra-spec §-cite dependents updated in-run; rendered dependents are target-behavior [CONDITIONAL]. — basis: `grep -rnE '§4\.2([^.0-9]|$)' --include='*.md' spec/ foundation.md instantiation-guide.md development-process.md post-run-review.md` → 20 bare-§4.2 sites; `§4.1.4` → 20 (anchor unchanged by S2), `§5.3` → 6, `§4.3` → 26; render-side `§4.2`: anneal-dev 2 / clippy 1 / bauleitplan 1 / daneel 0 / campaign-craft 0; phase-file homes confirmed (anneal-dev/clippy/daneel/campaign-craft have phases/{investigate-design,implement,verify}.md; bauleitplan has none).
D2 [OUTLINED] Re-derived §4 structure: top-down regenerate §4 from §1's model (3 phases) + each phase's actual jobs; sub-number §4.2 implement into §4.2.x parallel to §4.1.x (behavior-preserving), the concrete sub-section enumeration + the 20-site cite-retarget map derived next cycle. Resolves S1; frames S2/S4. — basis: F1; README "Placement" filter (135–138) homes phase/orchestrator behaviour in core.
D3 [OUTLINED] S2 de-dup: `core.md §4.1.4` carries the convergence/falsification *requirement* (dispatch to fresh-context subagent; aggregate-falsified reopens the entry) + cites `modules.md §3.4`; the artifact format (candidate set / shape closed-set / predicate closed-set / coverage-check) homes in `modules.md §3.4`. — basis: F2; README "Placement" filter (135–138) "an artifact format → `modules.md`".
D4 [OUTLINED] S4 consolidate: single home for the `[READY]` supporting-facts list (§4.1.1 canonical; §5.3 reduced to a cite); reconcile two-vs-three passes (base cycle = two passes; convergence cycle adds the falsification pass — exact wording derived next cycle). — basis: F3, F4.
D5 [OUTLINED] S5 link: one cross-reference tying `core.md §Purpose` (2-item rubric) to `foundation.md` (3 contracts) — the two foundation roots. — basis: F5.
D6 [OUTLINED] S6 strip: remove the 3 validation-watch provenance breadcrumbs from canonical core prose (§4.1.2:328, §4.1.4:455–456, §4.2:489); glossary V-N pointers (379/388/399) are outside §4 — out of this cluster's scope. — basis: F6; README:88–94 fixed-decision rule.

<!-- cycle 2: top-down re-derivation locks the concrete structure -->

D2 [VERIFIED] §4.2 re-derived into 8 sub-sections parallel to §4.1.x: §4.2.1 The impl plan / §4.2.2 Dispatch / §4.2.3 Isolation mechanism / §4.2.4 Integrity check / §4.2.5 Self-check at dispatch boundary / §4.2.6 Tracker writes / §4.2.7 Loopback across the subagent boundary / §4.2.8 Checkpoint. The phase preamble stays un-numbered (locked-design-authority + makes-no-design-decisions + the two finding-paths + no-inline-fix), and the completion statement closes the phase un-numbered — mirroring §4.1's un-numbered cycle/design/scope preamble + numbered cluster. Behavior-preserving (pure re-org). The 20 bare-§4.2 spec cites retarget to their referent sub-section; the two that point at the whole dispatch protocol (`core.md:949`, `glossary.md:122` isolation+integrity) stay bare §4.2. — basis: read §4.2 462–678 = exactly 8 bold-prefixed sub-topics + the preamble-contract (464–490) + completion (673–677); D1's 20-site enumeration.
  considered: number the preamble-contract as §4.2.1 (rejected: §4.1 keeps its cycle/design/scope preamble un-numbered — parallel structure homes the phase-contract as preamble, the machinery as numbered cluster).
D3 [VERIFIED] S2 de-dup: `core.md §4.1.4` keeps the convergence/falsification REQUIREMENT (convergence cycle = full cycle, zero D-deltas; new-surfaces rule; falsification pass iterates [VERIFIED] D-entries, dispatched fresh-context per §3.1; aggregate-falsified reopens [INVALIDATED]→[PENDING]; coverage-check-on-return as the orchestrator gate; spawn-fallback; outputs feed the [READY] artifact; both modes) and CITES `modules.md §3.4` for the artifact format; the candidate-set line-shape + shape closed-set + predicate closed-set (`any-match`/`any-outside-scope`/`expected-match`) + shape-coherence rule are removed from core (they live in modules §3.4). — basis: F2; README "Placement" filter (135–138) "an artifact format → `modules.md`"; the duplicated span is core 391–414 ≈ modules 326–380. Acceptance: core §4.1.4 re-enumerates no predicate/shape closed-set; the §4.1.4 anchor is unchanged so its 20 cites stay valid.
D4 [VERIFIED] S4 consolidate: §4.1.1 is the sole home of the [READY] supporting-facts list; §5.3 reduces to a thin status-machine→[READY] bridge pointing at §4.1.1 (keeps "unresolved status → phase holds", drops the list restatement). The 6 §5.3 cites retarget by referent — core:287/300/304 (in §4.1, citing §5.3 for the status terms) → §5.2 (the status definitions); core:814/890 ("the phase holds") + core:942 keep §5.3 (the bridge). Passes reconciled: a cycle has two passes by default (investigation + standardized inspection); the convergence cycle (§4.1.4) adds a third, the falsification pass — amend `core.md:253` and the glossary "Pass" entry (232–233 "exactly two") to carry the by-default + convergence-exception wording. — basis: F3, F4; §5.3 cite split (`grep §5\.3` → 287/300/304 in §4.1, 814/890 in §5.1/§5.2, 942 in §6); glossary Pass 232–241.
D5 [VERIFIED] S5 link: `core.md §Purpose` gains one sentence tying its 2-item work-quality rubric to `foundation.md`'s 3 architectural contracts (the layer above) — core→foundation, unidirectional, closing the Lens-9 grounding-chain gap. — basis: F5; `grep -n foundation spec/core.md` = no §Purpose cite; foundation.md:10-40 (3 contracts govern the spec — contract 1 names "the anneal-framework spec is domain-agnostic").
D6 [VERIFIED] S6 strip: delete the 3 validation-watch parentheticals from canonical core prose — §4.1.2 "(V-5)" (328), §4.1.4 "recorded in V-5 (…)" (455–456), §4.2 "see …validation-watch.md V-24" (489) — preserving the rule prose they hang on. Glossary V-N pointers (379/388/399) stay out-of-cluster (glossary, not §4). — basis: F6; README:88–94 fixed-decision rule ("that provisional nature … never written into the spec"); README:63-65 (validation-watch "Not part of the spec").

<!-- cycle 3: convergence cycle — falsification pass falsified D1/D2/D3 -->

F7 [VERIFIED — addressed by D1/D2 re-form] The scope §4.2-cite enumeration is incomplete: the basis regex `§4\.2([^.0-9]|$)` excludes sentence-final `§4.2.`, undercounting by 3 — the true bare-§4.2 count is 23 (raw token), not 20; sub-numbered `§4.2.x` = 0. The 3 missed cites: `modules.md:60`, `core.md:953`, `glossary.md:338`. — basis: `grep -rhoE '§4\.2' …` = 23, `grep -rhoE '§4\.2([^.0-9]|$)' …` = 20, `grep -rhoE '§4\.2\.[0-9]' …` = 0; the 3 sites read (each `§4.2.` sentence-final). [convergence falsification, cycle 3]
F8 [VERIFIED — addressed by D3 re-form] The falsification-predicate closed-set has a 3rd canonical home — `glossary.md` "Falsification predicate" (175–185) re-spells `any-match`/`any-outside-scope`/`expected-match` — beyond core §4.1.4 + modules §3.4. It is the README-sanctioned definitions-only layer (retained, not collapsed), but D3's basis did not enumerate it. — basis: read glossary 175–185 (closed set spelled, cites `modules.md §3.4`); README:77–81 ("Glossary is definitions-only / Closed sets are enumerated"). [convergence falsification, cycle 3]

D1 [INVALIDATED] convergence cycle 3 falsified target-dependents: 3 §4.2 cites outside the claimed 20-scope (`any-outside-scope` → falsified). — basis: F7.
D1 [PENDING] reopened — re-form scope with raw-token count (23) + the 3 missed cites enumerated/dispositioned. — basis: F7.
D2 [INVALIDATED] convergence cycle 3 falsified target-dependents: the same 3 sentence-final §4.2 cites lack a retarget disposition. — basis: F7.
D2 [PENDING] reopened — re-form the cite-retarget enumeration to cover all 23 (assign modules:60 / core:953 / glossary:338). — basis: F7.
D3 [INVALIDATED] convergence cycle 3 falsified target-dependents: glossary (175–185) is a 3rd home of the predicate closed-set, unaccounted in D3's basis. — basis: F8.
D3 [PENDING] reopened — re-form basis to name the glossary as the retained definitions-only layer; de-dup scope stays core↔modules. — basis: F8.

<!-- cycle 4: re-form D1/D2/D3 with corrected bases -->

D1 [VERIFIED] Scope (re-formed): the §4.2-cite dependent set is 23 (raw-token count; sub-numbered §4.2.x = 0), not 20; the rest of the reconciliation-reach is unchanged (§5.3=6, §4.1.4=20 both raw=boundary; §4.3=26 not renumbered). Rendered dependents remain target-behavior [CONDITIONAL] → release re-render + verify. — basis: `grep -rhoE '§4\.2' --include='*.md' spec/ foundation.md instantiation-guide.md development-process.md post-run-review.md` = 23; `…'§4\.2\.[0-9]'…` = 0; `…'§5\.3'…` = 6 = `…'§5\.3([^.0-9]|$)'…`; `…'§4\.1\.4'…` = 20 = boundary.
D2 [VERIFIED] §4.2 → 8 sub-sections (§4.2.1–8, structure unchanged from cycle 2); the 23 §4.2 cites retarget by referent — **3 stay bare §4.2** (whole dispatch protocol: `core.md:949`, `core.md:953`; multi-sub-section span: `glossary.md:122` isolation+integrity), **20 retarget** to their sub-section. The 3 previously-missed cites disposition: `modules.md:60` → §4.2.1 (impl-plan preview), `core.md:953` → bare §4.2 (paired with :949), `glossary.md:338` → §4.2.1 (dispatch unit defined in the impl plan). — basis: F7; reads of modules:56–62 (impl-plan preview), core:947–953 (§6 whole-protocol cite), glossary:335–344 (Dispatch-unit/Impl-plan entries).
D3 [VERIFIED] S2 de-dup (decision unchanged); basis re-formed to enumerate all 3 closed-set homes: core §4.1.4 (the duplication collapsed → requirement + cite) and modules §3.4 (the retained format home) are the de-dup pair; `glossary.md` "Falsification predicate" (175–185) is the retained **definitions-only layer** (README:77–81 "Glossary is definitions-only / Closed sets are enumerated") — a legitimate term-definition home, not triplication to collapse. — basis: F2, F8; README:77–81 + README:135–138 (Placement filter "artifact format → modules.md"); modules §3.4 owns the full format (340–380) with core already delegating via 5 in-file §3.4 cites.

<!-- cycle 5: fresh convergence cycle — CLEAN (zero D-deltas) → [READY] -->

F9 [VERIFIED — non-issue] `validation-watch.md` carries 4 `core.md §4.2` refs (303/435/480/1310, incl. :435 `§4.2 "Self-check at dispatch boundary"`) — surfaced by the cycle-5 falsification subagent as the nearest-thing-to-a-missed-dependent. Non-issue: validation-watch is non-canonical (out of D1 scope) and the refs remain accurate post-renumber (implement stays §4.2; the named sub-topic = §4.2.5). — basis: README:63–65 ("`validation-watch.md` … Not part of the spec"); the implement phase keeps its §4.2 number under D2 (only its sub-topics gain §4.2.x).

CONVERGENCE CYCLE 5 CLEAN — all 6 [VERIFIED] D-entries held under fresh-context falsification (`…cycle-5.falsification.md`, subagent `a843aee7853f4a12b`); zero D-track deltas; standardized set accounted for whole; fresh-session implementability PASSED. Design [READY]. Impl plan: `…framework-spec-cleanup-pipeline.impl-plan.md`.

<!-- impl Unit 1 dispatch: loopback-required (mechanical dispatch-path blocker, NOT a spec/design problem) -->

F10 [VERIFIED — deferred] Resolved for this run via spawn-fallback (orchestrator implemented Units 1–2 in-context; the main-session hook path passes — Skill results carry `isMeta=True`). Operator-pull defer of the framework fix (operator: "file hook fix as next-up and continue"); trigger = the hook fix lands per `skill-craft-hook-subagent-dispatch-block.md` (re-enabling subagent dispatch). — basis: backlog item filed + README/sibling cross-linked; spawn-fallback impl landed clean (self-check 4-lens + scope PASS).
F10 [PENDING] The skill-craft pre-edit hook denies every Edit to `spec/*` from a dispatched impl subagent — anneal-dev's subagent-dispatch impl path is blocked for method-kernel edits. **Mechanical/dispatch blocker, NOT a spec problem: D2–D6 remain [VERIFIED] and correct; affected_decisions = none.** — basis: Unit-1 subagent `af60f2dc239967b6b` 5/5 Edit denials on `spec/core.md` (clean `git status --porcelain spec/core.md` throughout, every skill-craft-invocation pattern); orchestrator-confirmed by reading `hooks/skill-craft-pre-edit.py:155–208` — the boundary scan skips `user` events with `isMeta=True` (162) / `origin` set (164), so a subagent-path Skill-result `user` message lacking `isMeta=True` is mis-read as a fresh operator-prompt boundary, moving `last_prompt_idx` past the Skill `tool_use`. Contradicts the archived `impl-skillcraft-gate` conclusion ("gate IS satisfiable from a subagent") → needs reconciliation. Impl plan invalidated (loopback); first-judge recommendation: spawn-fallback (orchestrator implements in-context, "without isolation"; the main-session hook path works — its Skill results carry `isMeta=True`), backstopped by the isolated verify + the method-kernel kernel-independent review.

<!-- IMPLEMENT (spawn-fallback, in-context; skill-craft invoked this turn) — COMPLETE -->

IMPL COMPLETE (spawn-fallback). Unit 1 (`spec/core.md`): D2 §4.2→§4.2.1–8 + intra-/cross-§4.2 cite retargets (4 residual bare cites all legit whole-phase/multi-subsection); D3 core §4.1.4 falsification format → requirement+cite modules §3.4; D4 §5.3→thin bridge + §4.1 passes "two by default"+convergence-third + §4.1.1 status cites →§5.2; D5 §Purpose→foundation.md (1 sentence); D6 0 V-N breadcrumbs left. Unit 2: glossary Pass-entry passes-fix + 6 §4.2 retargets (122/331 stay bare); modules 7 §4.2 retargets; instantiation-guide §4.2→§4.2.3. core.md 978→976 ln (de-dup/consolidate −~24, +8 nav headings). Self-check 4-lens (Lossy-render/Missed-dependents/Undefined-shorthand/Over-claimed) + change-set-vs-scope = PASS. Persistence ref: working tree (no commit — release-loop step 5 operator-approved). Render dependents (instance phase files) = D1 [CONDITIONAL] → release re-render + verify render-fidelity.

<!-- VERIFY — [PASSED] (isolated) + kernel-independent skill-craft review (method-kernel) -->

VERIFY [PASSED] (isolated). anneal-dev battery subagent `a9e186baafa026582`: 3 checks ran — planned-vs-actual (D2–D6 all correctly implemented) + standardized lenses (clean: no Lossy-render/Bloat/Missed-dependents/etc.) + battery (a) render-fidelity N/A→release re-render per D1 [CONDITIONAL]; (b) coherence RAN clean (every §4.2.x/§4.1.x/§5.x/cross-file cross-ref resolves; no drift; one falsification spec-home + glossary definitional layer); (c) skill-quality → the separate kernel-independent review. Kernel-independent skill-craft review subagent `ab65542ebfe636c15`: PASS (Layer 2/4/anti-patterns; clause-level diff of removed §4.1.4 vs modules §3.4 confirms every removed clause survives; Edit-as-Pareto confirmed net-reducing; bonus: §5.3→§5.2 retargets fixed a pre-existing mis-citation).

Finding ledger (verify):
  F-V1 [VERIFIED — non-issue] D2 §4.2.1–8 structure correct (8 headings, no seam-split)
  F-V2 [VERIFIED — non-issue] §4.2 cite retargets all resolve; 4 bare cites legit
  F-V3 [VERIFIED — non-issue] D3 de-dup lossless (modules §3.4 owns format; reopval preserved)
  F-V4 [VERIFIED — non-issue] D4 [READY] sole in §4.1.1; §5.3 bridge; passes reconciled 3 homes
  F-V5 [VERIFIED — non-issue] D5 §Purpose→foundation.md (3 contracts named exactly)
  F-V6 [VERIFIED — non-issue] D6 0 V-N breadcrumbs in core
  F-V7 [VERIFIED — addressed] design-completeness: every hunk maps to D2–D6, no orphan
  F-V8 [VERIFIED — deferred] tracker-count staleness (D2 "3 stay bare" → actual 4: +glossary:331; rule-text correct, run-state count drifted) — trigger: any future §4.2-cite edit
Run terminal: PASSED. Release loop (commit + re-render) is operator-gated downstream.
