# campaign3-enforcement-fidelity — cycle 2 (convergence attempt) intent-falsification artifact

Dispatched: fresh-context subagent `ab66c55aea61feb0f` (opus, top tier per `anneal-dev.config/model-tier.md`). Criteria-first: criteria derived from R1/R2/R3 before reading the design. Grounded in the LIVE spec (self-hosting), not plugin renders.

## Per-R# attack lines
- `{R1, attempted-refutation: verified bindings.md:41 binds framework search (core.md §3.2) wrap-tolerant + modules.md:410-411 the safety-net target-dependents candidate "re-runs §3.2.2's reference enumeration as its search" → inherits wrap-tolerance; modules.md:377-382 expected-match wrapped-miss = false-falsify (conservative); practice 4 (dev-process:163-167) covers audits+dispatched subagents; practice-1 three-form enumeration (dev-process:115-131) satisfied by D1, outcome: SERVED}`
- `{R2, attempted-refutation: verified core.md:513-546 coverage-check clauses (i)-(v) never re-run the entry's recorded query — verdict computed from the subagent's OWN candidate+predicate+result (modules.md:391-403, no subagent judgment); false-as-written query cannot corrupt the verdict; n=1; the V-entry catcher = the same separate-checker, outcome: SERVED}`
- `{R3, attempted-refutation: verified core.md:1018-1029 (a)/(b) + 1043-1047 malformed-on-absence → §4.3 [ISSUES FOUND]; path-existence is a re-checkable filesystem fact (§3.1 bind criterion); D3's own basis (target/dependents) holds so NOT a mechanical-candidate; BUT D3's body "checked present" does not name the producer-independent checker, outcome: FINDING}`

## Per-finding line
- `{finding: D3's "checked present" (tracker D3 (c)) leaves the existence-checker unnamed — if the producing (disposition-recording) context self-asserts existence, the disposition is a §3.1 weak artifact / strong surfacer, NOT a bind, and "faking requires authoring the artifact" fails; if it relies on operator-noticing it is forbidden operator-detection (§1). The bind is LATENTLY available (verify's isolated §4.3 subagent + finding-status ledger can re-run the path-existence check producer-independently and force [ISSUES FOUND]) but D3 must route it there explicitly. The originating incident (F8/F13: a to-be-authored V-entry never authored) is exactly the self-assertion hole. criterion-attacked: R3. refutation: core.md:1043-1047 (malformed-on-absence → §4.3 separate checker) vs D3 body (checker unnamed); §3.1 core.md:139-154 (self-recorded verdict = surfacer not bind); §1 core.md:69-78 (operator-detection malformed). route: [VERIFIED — surfaced]}`
- Secondary (non-blocking, Bloat lens): confirm sub-shape (c) is genuinely distinct from (b) and not "(b) + existence precondition" — its watch-trigger lives in the to-be-authored artifact (a (b)-style observable trigger). Surfaced judgment point, not a refutation.

## Bottom line (subagent)
Design sound for intent on all three; R1/R2 cleanly served with evidence. The single load-bearing focus for the operator's step-4: D3 must, at impl, route the `<path>`-existence check to a producer-independent checker (verify's isolated §4.3 / the [READY]-gate separate-checker re-running the basis-citation re-check), not let the producing context self-assert "checked present."

## Working-context disposition
The R3 finding is ACTIONED (not left surfaced): it is a load-bearing soundness improvement — naming the producer-independent checker is what makes D3 *bind* (and therefore qualify as practice-8 structural enforcement at all; an unnamed-checker D3 is a prose suggestion, malformed per practice 8). → D3 [INVALIDATED] → D3.1. This is a **D-track delta** → cycle 2 NOT converged; mechanical falsification **SKIPPED this cycle** (intent-delta, per `core.md` §4.1.4 / `modules.md` §3.4 skip carve-out). The secondary (b)-vs-(c) point is resolved in D3.1's `considered` field.
