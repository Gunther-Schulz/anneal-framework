# Intent-falsification-pass artifact — provenance-basis-naming, convergence cycle 3

Fresh-context subagent (opus, `a8c6ac7322dd4225e`), criteria-first against R1–R5.
Result: design CONVERGES — no new D-delta; both findings [VERIFIED — surfaced]. The #2-vs-#1
call attacked hard and HELD.

## Per-R# attack lines
- **{R1, refutation: D2 contract (tracker) + §3.2(b) core.md:185-187 — does naming the form force the mismatch-catch? outcome: served}** (bounded by Finding A recognition-gating).
- **{R2, refutation: grep provenance|distrust|use-site|producer → empty; edit lands core.md §3.2.1, clippy = render-debt. outcome: served}**
- **{R3, refutation: read §3.2.1:216-225 vs the general header; cross-check §3.2.2 co-producer precedent core.md:254-264. outcome: served — #2 holds}** — see adjudication below.
- **{R4, refutation: §3.2(b) core.md:185-187 vs D2's located-read-at-producer — well-defined / evidence-bearing / distinct? outcome: served}** (+ Finding B realization watch-point).
- **{R5, refutation: §4.1.1 core.md:409-411 (curated subset) + glossary Basis :49-59 (artifact-types) + lens-set headers. outcome: served}** — dependents closed.

## R3 #2-vs-#1 adjudication (the load-bearing attack — HELD as #2)
The #1 steelman (the open-ended "claims embedded within larger statements" already reaches a
semantic label → Bloat) FAILS on three converging located facts:
1. **Empirical inertness of the general rule:** `provenance-at-handoff-lens.md:43-44` — vanilla-anneal
   (general discipline) caught market_id 2/5, same as act-first; only explicit trace-to-producer → 5/5.
   Direct evidence the general basis rule does NOT fire on the provenance class = the definition of #2.
2. **§3.2.2 precedent:** the co-producer addition (core.md:254-264, commit 49cb453) was the same move —
   a general completeness clause that STILL required naming a specific search-blind form — accepted as a
   sharpening, not Bloat.
3. **Distinct epistemic posture:** the 3 existing forms (target-naming/cited-rules/counts) ground at the
   cited thing itself (trust + read the citation target); the new form DISTRUSTS the surface label and
   redirects to a DIFFERENT site (the producer, not the use-site). Content the general clause does not
   direct a reader to → not redundant with the cited-rules form.

## Per-finding lines
- **{INT-A (recognition-gating ceiling — correctly bounded, NOT overclaim), criterion: R1, refutation:
  D2 trigger "a name/label asserting MEANING/PROVENANCE" fires only once the AI recognizes the
  meaning-dependency; an unrecognized one never triggers — the §3.2.2 INT-4 ceiling
  (`forcing-function-dose-discriminator.md:28-29`). route: [VERIFIED — surfaced]}** — pure judgment, no
  runnable check; the discriminator's own disposition is honest-surface, not a D-delta; D2 does not claim
  to catch unrecognized meaning-dependencies. For the operator's soundness pass.
- **{INT-B (producer-not-use-site redirection must survive impl realization), criterion: R4/R3, refutation:
  §3.2(b) "located read of the source" is unqualified re: producer-vs-use-site (core.md:185-187, F2); the
  distinctness that justifies #2 holds ONLY if the realized text carries the producer-not-use-site
  redirection — which D2/D3 already specify (tracker:127-128, 137-138). route: [VERIFIED — surfaced]}** —
  judgment about impl realization, not a D-flaw; backstopped by verify (§4.3) + the convergence. IMPL
  WATCH-POINT: the producer-not-use-site phrase must survive into the rule-text. No D-track change.

## Bottom line
Converges, zero new D-delta. #2-sharpen call correct + load-bearing. Two surfaced residuals (INT-A
recognition-gating bounded-honest; INT-B impl-realization watch-point), neither holding the phase.
