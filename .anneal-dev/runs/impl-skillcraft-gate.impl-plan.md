# impl-skillcraft-gate — impl plan

One dispatch unit, single container (the anneal-dev repo). Sequential — a one-unit plan runs in-place under the Integrity check (no parallelism, no disjointness search needed; nothing in flight).

## Unit U1 — the skill-craft-invocation dispatch binding (implements D2, scoped by D1)
- **Implements:** D2 (within D1's scope).
- **Container:** the anneal-dev repo (`~/dev/Gunther-Schulz/anneal-dev`) — 1 touched container.
- **Element scope:** `spec/bindings.md:210-223` (add the dispatch-brief binding alongside the existing "dispatched impl subagent applies the standardized lenses" obligation — the skill-craft pre-edit invocation is a parallel dispatched-impl-subagent obligation; cycle-4 falsification) + 3 renders, each carrying the "(or the working context, on the spawn-fallback path)" parenthetical where it states the obligation:
  - `plugin/skills/anneal-dev/phases/implement.md:170-174` (§Dispatch-brief template (a) — the canonical load-instructions)
  - `plugin/skills/anneal-dev/phases/implement.md:76-78` (§Dispatch prose)
  - `plugin/skills/anneal-dev/SKILL.md:71` (pipeline dispatch sentence)
- **Contract scope:** the impl dispatch-brief load-instructions; cross-references `development-process.md` practice 5 + skill-craft (not restated — Fragmentation).
- **Dependencies:** first (no prior unit). **Parallelism:** sequential (single unit).
- **Self-bootstrapping note:** U1's dispatched subagent will itself hit the pre-edit gate (the gap being fixed) → expected forced **spawn-fallback** (the orchestrator implements U1 in its own context, having invoked skill-craft first). The run produces the fix that makes future impl dispatches gate-satisfiable.
