# Run: validation-watch-lifecycle-fix

- **Status:** PASSED
- **Phase:** verify
- **Mode:** interactive (operator cadence: auto-cycle to [READY]; surface at phase transitions + method-kernel touchpoints)
- **Task:** Fix the validation-watch lifecycle's preventive-fix validation gap (operator-surfaced). RESOLVED's "positive load-bearing instance" is unsatisfiable for purely-preventive fixes (a working fix → the failure never recurs → only absence observable, which the lifecycle forbids); and the corpus is internally inconsistent (V-13 accepts absence-closing; the preamble forbids absence). **Operator-locked design (split + 2 refinements).**

## F-track (findings)

- F1 [VERIFIED] **the lifecycle closing logic lives in exactly 2 homes** (the dependent set): `dev-notes/validation-watch/README.md` (canonical — n=1 convention :12-21, Entry lifecycle :23-72, Vocabulary :76-98) + `post-run-review.md:173-191` Q7 (restates the FIX-SHIPPED walk: mitigation-load-bearing/not-exercised/evaded). `spec/glossary.md:377` is only the vocabulary *pointer* (no closing logic) → unchanged — basis: `grep -rniE 'load-bearing instance|positive evidence|mitigation load-bearing|absence-of-recurrence|FIX-SHIPPED' spec/ development-process.md post-run-review.md foundation.md README` (re-runnable; recorded).
- F2 [VERIFIED] **Q7 RESTATES the README closing logic** (not defer) — `post-run-review.md:173-178` re-lists the FIX-SHIPPED classification the README :58-62 also states → a fragmentation: changing the closing rule must touch both, OR Q7 should defer to the README — basis: `post-run-review.md:173-191` read + `README:58-62` read.
- F3 [VERIFIED] **the gap is real + corpus-inconsistent** — README:40-42 "positive evidence... not mere absence-of-recurrence" (strict); but V-13's body accepts disjunct-a "grounded cleanly without pollution" (absence-style) → contradiction — basis: `README:40-42` read + `V-13-minimal-verbatim-scope.md` read (cycle: disjunct-a "grounded cleanly without code-quote pollution").

## D-track (design decisions)

- D1 [VERIFIED] **Scope** = `dev-notes/validation-watch/README.md` (Entry lifecycle + n=1 + Vocabulary) + `post-run-review.md` Q7 + `dev-notes/validation-watch/V-13-minimal-verbatim-scope.md`. NO `spec/*` (glossary pointer unchanged). Retrofitting the other 26 entries to name a catcher = FOLLOW-ON (rule applies going forward + a later sweep), not this run — basis: F1 dependent search (re-runnable). Spec-only: instance renders (clippy/daneel post-run-review Q7 render) DEFERRED to the batch re-render.
- D2 [VERIFIED] **Opportunity-exercised closing rule (the unifying principle; operator refinement).** RESOLVE only on a run where the failure's **opportunity genuinely arose** AND the fix handled it. **Pure non-occurrence never closes** ("no such opportunity arose" = no evidence yet, NOT RESOLVED). Two evidence shapes: **caught** (a catcher fired on a would-have-shipped failure — counterfactual shown) for correctness fixes; **produced-clean** (a form-change held where the old form would have produced the bad artifact — counterfactual shown) for quality fixes. Basis: operator-locked + `README:40-42` (the existing "active catch counter-factually shown" already gestures at it; this generalizes "catch" → "opportunity handled" so it covers prevention).
- D3 [VERIFIED] **n=1 confirmed + justified (operator).** One opportunity-exercised instance is sufficient to RESOLVE; the opportunity-requirement (D2) is what licenses n=1 (the rigor is the real opportunity, not repetition). Multi-run thresholds stay rejected (README:14-21). No change to the n=1 text; D2 supplies the missing *why*. Basis: `README:12-21` (existing n=1 stance) + operator-locked.
- D4 [VERIFIED] **Name-your-catcher / opportunity-test.** Each FIX-SHIPPED entry names how its failure is exhibited: a **catcher** (the check/lens/phase that catches it — correctness) OR an **opportunity-test** (what a run looks like where the old form would have produced the bad artifact — quality). A fix that is **correct-by-construction** (the failure can't be expressed under the new spec) is **NOT a watch entry** — no residual uncertainty to watch. Basis: D2 (closing needs the catcher/opportunity-test named to be checkable) + operator-locked.
- D5 [VERIFIED] **The split (operator's term).** Each entry is a **correctness-watch** (fix = a catcher; closes on *caught*) or a **quality-watch** (fix = a form-change with no catcher; closes on *produced-clean-under-opportunity*). The two are unified by D2's one closing rule (opportunity + handled); the split names the evidence shape. New terms → README Vocabulary (practice-10). Basis: operator-locked ("split") + D2.
- D6 [VERIFIED] **V-13 → quality-watch.** Recategorize V-13 (tracker-pollution concern; the fix is a form-change — citation+fact replaces verbatim — with no catcher): add a `Kind: quality-watch` marker; reconcile its disjunct-a to D2's produced-clean-under-opportunity (an opportunity arose where the old verbatim form would have polluted, the new form grounded clean). Basis: `V-13` read + D5.
- D7 [VERIFIED] **Q7 update + de-fragment.** `post-run-review.md` Q7 FIX-SHIPPED walk → restate per D2 (caught / produced-clean / not-exercised / evaded, per kind) AND **defer to the README** for the lifecycle definition rather than re-stating it (closes the F2 fragmentation) — Q7 carries the *walk procedure*, the README carries the *closing rule*. Basis: F2 + D2.

<!-- D8 added (operator, mid-cycle 1): archive-check closes the archival recurrence-blindness -->
- D8 [VERIFIED] **Archive-check before filing a new entry (operator).** Archiving RESOLVED entries (the folder design shipped earlier) moves them out of the active `ls` index → a recurrence of a resolved issue looks "new" and would be filed as a fresh `V-`, losing the "the fix didn't hold" signal. **Fix:** before creating a new `V-` for a seemingly-new issue, **scan `archive/`** for a matching OR **adjacent** (same root, different manifestation) RESOLVED entry; if found, the issue is a **recurrence** → reopen that entry (`git mv` back to active + set INVALIDATED, per the existing RESOLVED → INVALIDATED transition) rather than create a new entry. Only a genuinely-novel issue (no archive match) gets a new `V-`. This is the **detection mechanism** the existing "RESOLVED → INVALIDATED on later recurrence" transition lacked once entries archive out of sight — the archive becomes the read-half of the loop (closing writes to it; new-issue-detection reads it). The "adjacent" clause catches a partially-holding fix (works for the original manifestation, leaks through a neighbor). Lands in `dev-notes/validation-watch/README.md` (the archival convention + a "before a new entry" step) — within D1 scope. Basis: operator-locked + `README:68-72` (the archival convention this extends) + `README:47-56` (the RESOLVED → INVALIDATED transition this supplies the trigger for).

<!-- cycle 1 convergence (falsification ac... a3128285e111451cb); all HOLD; zero D-delta -->
  - D1 basis strengthened (cycle 1, falsification `a3128285e111451cb`): the corpus-wide grep also surfaced `spec/glossary.md:377` — the vocabulary POINTER list (no closing logic); confirms closing logic lives only in README + Q7, glossary unchanged. Resolution intact.
  - D5 basis strengthened (cycle 1, falsification `a3128285e111451cb`): "catcher" appears generically at `post-run-review.md:159-160` ("the framework's primary catcher" = post-run-review/verify, descriptive, not a defined term). The per-entry "catcher" has its single DEFINITION in the README Vocabulary; no competing definition. **Impl note:** the Vocabulary definition disambiguates (the per-entry catcher is a specific instance, distinct from "the framework's primary catcher").
  - D8 clarified (operator, cycle 1): the archive-check also catches **regressions from unrelated changes** — the archive is a registry of known-and-fixed failure modes; a re-introduction from ANY source (the original fix weakening OR a later unrelated change) trips the archive scan → reopen INVALIDATED. The n=1 fast-close + archive-check pair = aggressive closure with bounded error (a wrong close reopens on recurrence). A methodology-level regression net.
- **CONVERGENCE CLEAN (cycle 1).** Falsification `a3128285e111451cb`: D1-D8 all HOLD (orchestrator-computed); zero D-track deltas (2 basis-only sub-annotations). → [READY].

<!-- [READY] → implement (operator `next phase`, 2026-06-04) -->
- **[READY] fresh-session implementability: PASSED** — per-step (this session): U1 → README Entry lifecycle (:23-72) + Vocabulary (:76-98) + n=1 (:12-21) + V-13 read; U2 → post-run-review.md:173-191 Q7 walk read. Convergence cycle 1 clean (falsification `a3128285e111451cb`, D1-D8 HOLD, zero D-delta). No open [CONDITIONAL].

<!-- implement phase -->
- **U1 [COMPLETED]** README lifecycle + V-13 — checkpoint `4d5cd50` (persistence ref). Change-set: `dev-notes/validation-watch/README.md` (+94/−29: watch-kind split, opportunity-exercised closing, name-your-catcher, correct-by-construction, archive-check, 4 Vocabulary terms) + `V-13` (Kind: quality-watch + disjunct-a reconciled). Self-check 4 lenses + scope clean (subagent `a380d13310efa31c9`); integrity check passed (only README + V-13). No loopback.
- **U2 [COMPLETED]** post-run-review.md Q7 — checkpoint `ac942c4` (persistence ref). Change-set: `post-run-review.md` (+19/−12; D7 Q7 defers to README + restated per opportunity-exercised rule, "mitigation" vocab removed → de-fragmented; D8 archive-check in outcomes-land). skill-craft invoked pre-edit (subagent `abab5d1bb73b02971`). Self-check 4 lenses + scope clean; post-edit grep confirms zero old closing-rule vocab. Integrity check passed (only post-run-review.md). No loopback.
- **IMPLEMENT COMPLETE** — U1 + U2 both done. → verify.

<!-- verify phase — isolated (subagent a071c9d45a9e8b9db), spec-only -->
- **Verify result: [PASSED] (isolated)** — subagent `a071c9d45a9e8b9db`, isolation held. (1) planned-vs-actual + completeness audit clean (D2-D8 all CLEAN; scope = only README + V-13 + post-run-review.md; no spec/core); (2) standardized lenses clean — **Fragmentation RESOLVED** (old "mitigation" closing-rule vocab gone from post-run-review.md; one canonical home = README), **Lossy-render clean** (new rule ≥ as strict: pure-non-occurrence-never-closes preserved + the fix-evaded/INVALIDATED trigger survives; produced-clean generalizes-not-weakens), Bloat economical (in-place rewrite, Q7 net reducer), Undefined-shorthand clean (4 terms defined once, catcher disambiguated), Missed-dependents clean (instance Q7 render = deferred debt); (3) battery — (a) render-fidelity N/A (deferred, cited), (b) coherence RUN clean (README↔Q7 vocab subset-match, archive-check↔transition, V-13 Kind, catcher disambiguation, no dangling refs, 4 states intact), (c) plugin-skill N/A + **method-kernel skill-craft FORM review RUN clean** (Edit-as-Pareto, enforcement-strength, no naked-judgment — counterfactual-anchored). Empty finding ledger; no finding short of [VERIFIED].
- **METHOD-KERNEL GATE — open:** isolated verify [PASSED] + skill-craft form review (clean); **operator soundness review** + **commit approval** remain. Status held IN-PROGRESS.

<!-- run complete -->
- **RUN PASSED 2026-06-04.** verify [PASSED] (isolated, subagent `a071c9d45a9e8b9db`) + method-kernel kernel-independent review (skill-craft form-review clean + operator soundness "yes") + operator commit approval. Release: spec-only, `main` — U1 lifecycle `78be6e8`, U2 Q7 `0aa04e3` (Step-4 discharge, hook-validated). Instance Q7 render → `instance-reinstantiation` Render-debt queue. Follow-on: `validation-watch-entry-retrofit` (the 26 active entries). The fix is also documented durably in `dev-notes/validation-watch/README.md` itself.
