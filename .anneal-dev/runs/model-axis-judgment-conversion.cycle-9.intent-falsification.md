# Intent-falsification-pass artifact — cycle 9

- run: model-axis-judgment-conversion
- cycle: 9 (convergence attempt 4)
- pass: intent-falsification (fresh-context, criteria-first)
- dispatch model: fable-5
- checker-capability relation: equal-ceiling
- ordering-basis: assumed
  - stamped from carried relation inputs (actor = fable-5; declared tier-ordering ABSENT) and independently re-derived by this subagent: located read `anneal-dev.config/model-tier.md` — one fact: the file carries the value line `opus` and no `order:` line.
- artifact shape: per the LIVE spec `spec/modules.md` §3.4.1 (self-hosting rule — live spec governs), whose per-R# line is `{R#, serving-decisions, attempted-refutation, outcome}`; the loaded plugin's `references/tracker.md` omits the serving-decisions field. Live shape followed.
- criteria-first order observed: criteria derived from the requirements record (tracker :28–76) before any D-track read. Faithfulness leg (live core.md:604–608): the R1..R6 enumeration checked against the verbatim request + accepted recommendation ("fork (a) narrow; one gated-kernel campaign over both items; grooming first") — both backlog items covered (R1–R3 carve-out, R4 conversions), narrowness covered (R2, D10), gating covered (R3/R5 operator-soundness handling); no dropped verbatim requirement found.

## Per-R# attack lines

- {R1, serving-decisions: {D2, D3}, attempted-refutation: attacked the carve-out's baseline and its weakest branch — (i) located read live `anneal-dev/spec/bindings.md:265-276` — one fact: the blanket rule reads "Every anneal-dev subagent dispatch — across **all** dispatch kinds … runs at the configured model tier" with no actor-relative clause (external-truth pair: D3-basis claimed-fact "single configured tier for all dispatch kinds, no relation to the actor's model" / re-derived-fact: matches); (ii) probed whether the absent-ordering branch silently re-admits same-tier as judgment-class coverage — located read tracker :160 (D3 cycle-8) — one fact: the branch records equal-ceiling + ordering-basis: assumed; located read tracker :139 (D2 cycle-6 clause (v)) — one fact: residual visibility keys on relation ≠ above, so same-tier is never counted as covering the judgment class, outcome: served}
- {R2, serving-decisions: {D3, D9, D10}, attempted-refutation: searched the reduced D-track for elevation leaking beyond the intent-falsification kind — located read tracker :160 — one fact: "non-elevated dispatch kinds run at the configured tier exactly as today"; located read tracker :101 (D10) — one fact: verify stays under the blanket floor with a named reopen trigger (a verify-located judgment-class false-accept datapoint); D2 (vi)'s brief-schema delta is keyed to the intent-falsification dispatch's brief clause (c) only (tracker :157), outcome: served}
- {R3, serving-decisions: {D2, D3}, attempted-refutation: attacked the ceiling boundary for silent coverage claims and pseudo-diversity — located read tracker :157 (D2 cycle-8 (vii)) — one fact: the header records the bare enum member plus sibling field ordering-basis: declared/assumed (F21's within-field qualifier removed); tracker :139 (D2 cycle-6 (iv)) — one fact: the spawn-fallback names the doubled residual (isolation + capability both waived, never silently); tracker :160 — one fact: inert-by-default is named openly (without an order line the carve-out can never elevate) with populating the ordering as the named arming step; no construction anywhere in the D-track counts a same-model fresh context as diversity; this run's own equal-ceiling residual is recorded F11 (:108), outcome: served}
- {R4, serving-decisions: {D4, D5, D6}, attempted-refutation: re-derived each conversion's target-absence from the live spec — (i) located read live `spec/core.md:247-316` — one fact: §3.2.2's completeness-claim family covers replacement/removal/amendment, structural dependents, and behavior-change, with no closed-value-set paragraph (D4's delta is real); (ii) located read live `spec/core.md:1176-1178` — one fact: Body-shape (c) requires "observable conditions" with no worked-example obligation (D5's delta is real; note the D5-basis phrasing "enumeration currently ends at embedded target-naming/count premises" is imprecise — live §4.1.1 continues through "no finding is left open" and the intent-findings-disposition clause (core.md:419-424) — but the load-bearing fact, absence of any body-shape-(c) clause, re-derives true); (iii) located read live `spec/modules.md:483-485` — one fact: the refutation field requires "the located read or query that surfaced the concern" with no primary-source-pair requirement (D6's delta is real); checker-tasking present in all three (D4 §3.4 always-include mirroring modules.md:408-411 — pattern confirmed verbatim at that location; D5 §4.1.1 + §4.1.4 tasking; D6 malformed-without-pair), outcome: finding (per-finding line 1 — the design's own boundary-bearing decisions do not exhibit the D5 obligation post-re-form)}
- {R5, serving-decisions: {D2, D3, D4, D6}, attempted-refutation: (i) no-judgment-in-mechanical — D4's closed-set candidate is expected-match with orchestrator-computed verdict (tracker :136); D2 (ii)/(vi) keep relation verdicts orchestrator-computed from observable inputs (model parameter + declared ordering, tracker :157); (ii) no operator-detection-dependence — enforcement is malformed-dispatch + coverage-check re-derivation; the order-population unit lands via the operator's step-5 commit approval, a permitted menu-bounded action, not detection; (iii) lens-crowding — searched the full reduced D-track for standardized-lens additions: zero; all three conversions land inside existing passes (§3.2.2/§3.4, §4.1.1/§4.1.4, §3.4.1), outcome: served}
- {R6, serving-decisions: {D8, D3}, attempted-refutation: independent dependent sweep re-run — `rg -ln 'intent-falsification|Dispatch model tier|model-tier|checker-capability' --glob '!.anneal-dev' --glob '!.git'` (this repo) → every hit falls into D8's three dependent kinds, F9's citational set (re-derived: persistence.md hits are path/description, development-process.md:580 is a receipt citation), edit targets, or task-input briefs; instance re-derivation (external-truth pairs for the D8 cycle-8 sub-annotation): claimed "render instances = clippy + daneel + self" / re-derived: coding-clippy EXISTS with 4 render files hitting `intent-falsification` and spec/bindings.md §Dispatch models at :95/:136/:151 (claimed :136-160 — match); daneel's render lives at plugin cache `daneel/0.2.50` (3 files hit `intent-falsification|convergence cycle`; `beat-the-books/.daneel` itself is run-state only) and the matrix tracks daneel rows (instance-reinstantiation.md:65,:207); claimed "campaign-craft/bauleitplan without local source" / re-derived: both ABSENT under ~/dev/Gunther-Schulz; claimed "copywriting-quill not a render instance" / re-derived: zero hits; modellwahl rule 3 still defers the canonical carve-out wording (located read dev-notes/modellwahl-und-anneal.md:48-49, unchanged through latest commit 2d2ed82); no D-entry targets a PBS path — the pbs Vorgriff handling is report-only flag (D8 (c)), outcome: served}

## Per-finding lines

- {finding: post-re-form dogfooding gap on the worked-boundary-example obligation — D2's cycle-8 re-formed resolution is boundary-bearing (closed relation ordering above > diverse > equal-ceiling; malformed-dispatch predicate) yet its body carries no §5.2(c)-shape worked boundary example, and the **diverse arm specifically has no concrete input→outcome trace anywhere in the D-track** (D3 works only the absent-ordering cell); once D5's §4.1.4 presence check lands, the next fresh-context intent pass would flag this very design — F22 surfaced the pre-re-form version, but the cycle-8 response tasked checkers without adding the examples to the re-formed decisions, criterion-attacked: R4, refutation: located read tracker :157 (D2 cycle-8 — full line; no worked example present) + located read tracker :160 (D3 cycle-8 — one fact: the only worked cell is absent-ordering → actor-model/equal-ceiling/assumed), route: [VERIFIED — surfaced] (no coupling-shape reduction: the concern attacks decision-body shape under the not-yet-landed obligation, not any cited basis fact)}
- {finding: relation-computation totality gap — the closed enum {above/diverse/equal-ceiling} has no member for a below-actor dispatch, and D2 names only the malformed-dispatch *verdict* for forbidden downgrades, not what the orchestrator's relation *computation* emits on that input class; under D4's own closed-set completeness rule (each member/input-class disposition named: handled / loudly not-covered) the below-actor class's disposition is implicit, inviting an F21-shape qualifier recurrence at impl (an implementer stamping e.g. "below-actor" into the enum field), criterion-attacked: R1 (the relation computation is the carve-out's enforcement core; also touches R5's no-judgment constraint — an uncomputable input invites improvisation), refutation: located read tracker :157 (D2 cycle-8 (vi) — computation from model parameter + declared ordering, no below-actor outcome named) + located read tracker :139 (D2 cycle-6 — one fact: "Enum stays {above/diverse/equal-ceiling}"), route: [VERIFIED — surfaced] (no coupling-shape reduction: it falsifies no cited basis fact of D2 — the gap is in the resolution's coverage, a design-adequacy judgment)}
- {finding: F25's "one positive diverse-arm datapoint" is mis-tiered under the design's own ordering — the opus-4.8 control against a fable-5 actor ran **below-actor**, which is none of {above/diverse/equal-ceiling}; the datapoint evidences cross-model catching generally, not the diverse member (same-tier, different model) specifically; carried into the release backlog item it would overstate the diverse arm's evidential base (which D2 (v)/F19 deliberately keep tier-evidence-open), criterion-attacked: R3 (honest naming of what is and is not covered), refutation: external-truth pair — claimed-fact: tracker :156 (F25) "one positive diverse-arm datapoint" / independently-re-derived-fact: tracker :160 (D3 cycle-8) proposes `order: fable-5 > opus-4.8 > sonnet-5`, placing opus-4.8 strictly below the fable-5 actor's tier, so the control's relation is not `diverse`, route: [VERIFIED — surfaced] (attacks an F-entry's label; no [VERIFIED] D-entry basis rests on the diverse-arm characterization — D2 cites F25 for the F21-resolution shape, D3 for inert-by-default, both unaffected)}

## Aggregate

- Per-R# outcomes: R1 served · R2 served · R3 served · R4 finding · R5 served · R6 served.
- Coverage matrix: no R# with an empty serving-decisions set.
- Findings: 3, all routed [VERIFIED — surfaced]; zero `mechanical-falsification-candidate` findings → no D-track delta from this pass (intent-clean for the purpose of constructing the mechanical pass, per modules.md §3.3 (c) / §3.4 header field).
