# framework-gap-receipt — cycle 1 standardized inspection pass

Scope this cycle: design of a new practice 13 ("Periodic framework-gap receipt")
in `development-process.md` — an additive, operator-invoked, cross-run periodic
pass. Each line cites this-cycle basis (F#/D# from this cycle, or a this-cycle
located read/search).

- **Bloat** — CAUTION→clean-with-commitment. Risk: practice 13 restating content that
  existing disciplines already carry (the 4 safeguards overlap practice 11, the
  additive-reflex anti-pattern, lens-crowding). D5/D6 commit to *cross-referencing*
  those rather than restating them; the practice body must carry only its load-bearing
  delta (the cross-run tally + pass-type router + caught-side scope). basis: D5, D6
  (this cycle) + lens Bloat.
- **Fragmentation** — clean. No existing receipt / cross-run / class-recurrence machinery
  in the corpus; the pass-type tags the receipt consumes (modules §3.4/§3.4.1) are reused,
  not duplicated. basis: F8 (search empty across development-process.md/post-run-review.md/spec/anneal-dev/spec).
- **Leakage** — clean-with-commitment. development-process.md is dev-process machinery (not a
  rendered spec) and already names instances as examples; still, practice 13 must reference the
  run-records via an *instance-defined locator* (clippy `.passes.md` / anneal-dev `.falsification.md`
  as examples only), not hard-code one instance's paths. basis: D3 (this cycle) + F6 + lens Leakage.
- **Missed-dependents** — FINDING (F9). The receipt is additive (no rule changed/removed), but the
  practice-13 *addition* has one dependent: the intro machinery-enumeration at
  `development-process.md:66-68` lists the cross-cutting machinery and names "the coherence-audit
  cadence" — the receipt's structural sibling — so the receipt warrants a parallel mention there.
  No practice-count or external practice-list dependent (append-numbered; search empty). basis: F9
  (this cycle) + located read `development-process.md:66-68`.
- **Unenforced-rule** — clean-with-commitment. The receipt is operator-invoked (not-running is not a
  violation), so enforcement binds on the artifact-when-run: the evidence-bearing by-class tally + the
  practice-11 routing table + the SG3 forced-justification. Practice 13 must carry its explicit
  practice-8 structural-form classification (as practices 10/11/12 each do). basis: D3, D6 (this cycle)
  + located read practices 10-12 + lens Unenforced-rule.
- **Undefined-shorthand** — clean-with-commitment. "Framework-gap receipt" is the one coined term;
  D10 commits to a single canonical defining sentence in-place (no glossary.md entry — practice-12
  precedent). Practice 13 must carry that sentence. basis: D10, F8 (this cycle) + lens Undefined-shorthand.
- **Lossy-render** — out of scope this cycle. The edit target (development-process.md) is not a render
  source — "anneal-dev renders from none of these" (dev-process machinery); no plugin clause is committed.
  basis: located read `development-process.md:24-25` (anneal-dev rendered from none of these) + lens Lossy-render scope.
- **Over-claimed-verification** — clean, one modeling note. F1-F8 are grounding-*observations* (confirmed
  corpus state feeding D-entries), not defect-concerns; each carries a located read or search, so no
  "verified" claim exceeds its basis. Note: the [VERIFIED — non-issue] disposition is concern-shaped; for
  pure grounding-observations it reads as "no defect — confirmed context." Carried as-is for cycle 1.
  basis: F1-F8 bases (this cycle) + lens Over-claimed-verification.
