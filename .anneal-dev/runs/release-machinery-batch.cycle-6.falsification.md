# release-machinery-batch — cycle 6 (convergence) mechanical falsification pass

**Per-artifact header — intent-clean verdict:** cycle-6 intent carried-forward CLEAN (cycle-4 found only
the docstring finding, closed by a behavior-preserving D3 comment edit), no `mechanical-falsification-candidate`.
Fresh-context subagent (opus) declared candidates+predicates; **orchestrator computed holds-or-falsified**.
Subagent agent-id: a5f9a118f21901312. Result: **every D-entry HOLDS — no falsification.**

- **D1** {target-existence `expected-match:"packaging — this file, the READMEs"` → instantiation-guide.md:491-492 present → **holds**} {target-existence (pre-edit doesn't gate the de-gated paths) `expected-match:"no match"` → `grep claude-plugin|instance-template hooks/skill-craft-pre-edit.py` empty → **holds**} {target-dependents `any-match` (rule-bearing content under de-gated paths) → only JSON manifests under .claude-plugin + only CLAUDE.md/README.md under instance-template non-spec → no match → **holds**}. aggregate: **holds**.
- **D2** {target-existence (step 5 lacks a formation clause) — subagent predicate `expected-match:"final discharge commit"`; ORCHESTRATOR NOTE: **inverted relative to D2's basis**, which CLAIMS step 5 lacks the clause (the gap D2 fills). The result (step 5 :798-819 has no formation-from-checkpoints clause) **confirms** the premise → **holds**} {target-behavior `expected-match:"if not is_corpus:"` → hooks/commit-msg:370-394 corpus-gate present + RULE_CORPUS_PATTERN includes development-process.md → a final commit re-touching it is corpus → discharge validated → **holds**}. aggregate: **holds**.
- **D3** {target-existence `expected-match:"instance-template/"` → docstring hooks/commit-msg:6-8 present → **holds**} {target-existence `expected-match:".claude-plugin"` → merge-relocation comment :34-38 present → **holds**} {target-existence `expected-match:"RULE_CORPUS_PATTERN = re.compile"` → :39 present → **holds**}. aggregate: **holds**.
- **D4** {target-existence `expected-match:"development-process\.md|post-run-review\.md|instantiation-guide\.md"` → KERNEL_SOURCE_PATTERN :61-64 includes all three → **holds**} {target-existence `expected-match:"anything else is corpus-evolution"` → development-process.md:24-44 defines method-kernel as spec/*+foundation.md+anneal-dev/spec/* only (excludes the 3 docs) → the over-match is real → **holds**}. aggregate: **holds**.

Coverage check (orchestrator): 4 lines for 4 [VERIFIED] entries; every claimed coupling shape covered; all predicates from the closed set, shape-coherent. **No falsified aggregate. Zero cycle-6 D-track deltas → convergence CLEAN → [READY].** New-surface requirement met (cycle 6 ran new reads/searches: the carve-out, the pre-edit grep, the rule-bearing-content find, step-5, the hook main logic, the docstring/comment/KERNEL_SOURCE reads, the method-kernel definition).
