# Run: clippy-reinstantiation

- **Status:** PASSED
- **Phase:** verify
- **Mode:** interactive
- **Task summary:** Re-render the clippy instance from the live anneal-framework spec — carry the framework spec deltas clippy lags into its rendered plugin files (campaign ⑥ / tier-⑥; backlog `instance-reinstantiation.md`).

## Requirements record

**Operator verbatim request:** "A" — selecting "(A) the clippy re-render" from the two ripe candidates in the backlog README READ-FIRST banner (2026-06-07 session 12): *"the clippy re-render (`instance-reinstantiation` / tier ⑥ / campaign ⑥) … now unblocked … Heavy … Start FRESH; re-ground in anneal-dev per CLAUDE.md. Render-debt to fold in: the §3.2.2 + §3.2.1 kernel deltas (anneal-dev/plugin self-render + clippy's bindings/lens-set re-point) + F-E (binding-table cite re-point) + the worktree isolation-slot binding."*

**Requirements (goal, separated from any proposed solution):**
- **R1** — Clippy's rendered plugin files (`SKILL.md`, `phases/`, `references/`) carry the live framework spec's current semantics: every framework spec delta postdating clippy's last render is rendered in.
- **R2** — The render is **faithful**: enforcement strength, completeness, and closed-enums preserved against the source clause (no Lossy-render softening of a "must"→"should", no dropped clause/enum).
- **R3** — Clippy's instance-specific sources (`coding-clippy/spec/bindings.md`, `spec/lens-set.md`) and the rendered files stay **coherent** with the framework deltas (glossary-term citations / binding-table interface per the citation-firewall convention).
- **R4** — The render is **verified by a separate-context render-fidelity check** (clause-by-clause vs the source spec).
- **R5** — Clippy stays within structural budget (skill-craft form); over-budget surfaces flagged.

**Proposed solution (strong input, NOT locked — investigated as a design premise):** "bundle clippy's WHOLE render-debt into one run: ALL semantic carries since pre-session-6 + the de-pollution vocab tail + the SKILL.md de-bloat fresh-rewrite + the CLAUDE.md seed re-point." → This premise rests on the backlog capability-matrix's claim that clippy lags since pre-session-6. **Contradicted on evidence this cycle** (F1): clippy is current to 2026-06-02; the pre-session-6 carries, de-pollution tail, worktree binding, and CLAUDE.md seed are already rendered. The solution is re-scoped to the true source-delta (D1).

---

## F-track (findings)

- **F1 [VERIFIED — non-issue]** The backlog `instance-reinstantiation.md` capability-matrix claim "clippy lags everything since pre-session-6" is a stale secondary source (§3.2.3), contradicted by clippy's actual render history. basis: `git -C coding-clippy log -1` → HEAD `56414d6` "v0.9.94 … render core §4.2 Integrity check + §5.2 contract surface", dated 2026-06-02 (observable fact: last-render date 2026-06-02, not pre-session-6).
- **F2 [VERIFIED — non-issue]** The pre-session-6/early "heavy" debt the brief listed is already rendered: de-pollution vocab (`target-existence`/`target-dependents`/`target-behavior`), worktree isolation slot, CLAUDE.md "second source" seed. basis: `grep -rniE 'target-existence|target-dependents|target-behavior' plugin/skills/clippy/` → 3 matches; `grep -rniE 'worktree|separate copy|escape-resist|isolation slot'` → 10 matches; clippy `CLAUDE.md` carries the "rendered, not authored" + "name instance spec as second source" seed (git `d9fc233`).
- **F3 [VERIFIED — addressed by D1]** The true source-delta clippy lacks = 8 framework spec commits dated 2026-06-05→06-07, all postdating clippy's 2026-06-02 render: `819e84e` (§3.1 bind/surface relabel), `f74b145` (L1 convergence sequencing), `a6ce126` (campaign ② completeness rigor), `014b7b0` (campaign ③ §5.1(c)), `d0f479a` (run-state tracked-by-default), `24fdb5f` (Purpose Mechanism clause), `49cb453` (§3.2.2 co-producer+emitted-effect), `cd2fd2c` (§3.2.1 provenance). basis: `git log --since=2026-06-05 -- spec/ foundation.md anneal-dev/spec/` (executable query; the 8 commit IDs are its output) + per-delta confirming greps returning empty in `plugin/skills/clippy/` for `819e84e`/`f74b145`/`24fdb5f`/`49cb453`/`cd2fd2c`/`014b7b0` tokens.
- **F4 [PENDING]** Clippy `SKILL.md` is 2579 words, over skill-craft's 1500–2000 ideal (~580 over) — a structural-budget concern independent of staleness (a fresh-rewrite / work-kind-2 question, not a propagation). basis: `wc -w plugin/skills/clippy/SKILL.md` → 2579 (observable fact: word count 2579).

### Cycle 2 (corrections + corrected scope)
- **F2 [INVALIDATED]** (cycle 2) The cycle-1 claim "de-pollution vocab already rendered" is contradicted: clippy uses the OLD coupling-shape names `target-shape`/`target-uses` throughout, none of the new `target-existence`/`target-dependents`. basis: `grep -rniE 'target-shape|target-uses' plugin/skills/clippy/` → 7 matches; `grep -rniE 'target-existence|target-dependents'` → 0 matches (observable fact: 7 old, 0 new). The cycle-1 grep that found 3 matches caught only `target-behavior` (unchanged in both naming schemes), misread as confirming the rename.
- **F3 [INVALIDATED]** (cycle 2) The cycle-1 delta boundary (`--since=2026-06-05`) was too narrow — a true-unit-basis error (search narrowed to an assumed boundary). The correct boundary is clippy's last-render commit-time. basis: `git -C coding-clippy log -1 --format=%ci 56414d6` → 2026-06-02 10:01:04; `git -C coding-clippy log --oneline --since='2026-06-02 10:01'` → empty (observable fact: zero clippy commits after 2026-06-02 10:01).
- **F5 [VERIFIED — addressed by D1]** Clippy has rendered nothing since 2026-06-02 10:01; the framework has ~28 spec/foundation/anneal-dev-spec commits since (`5f4ed74`…`cd2fd2c`). basis: `git log --since='2026-06-02 10:00' -- spec/ foundation.md anneal-dev/spec/` (executable query; 28-line output).
- **F6 [VERIFIED — addressed by D1]** Clippy lacks the major SEMANTIC capabilities added 06-02→06-07: intent-falsification pass + `[VERIFIED — surfaced]` (`432e618`), requirements record + coverage check (`1d93e58`), §3.1 bind/surface relabel (`819e84e`), L1 convergence sequencing (`f74b145`), campaign② completeness rigor (`a6ce126`), campaign③ §5.1(c) (`014b7b0`), purpose-mechanism (`24fdb5f`), §3.2.2 co-producer/emitted-effect (`49cb453`), §3.2.1 provenance (`cd2fd2c`). basis: per-token greps in `plugin/skills/clippy/` → 0 matches for `intent-falsification`, `requirements record`, `VERIFIED — surfaced`, `meaning or provenance`, `co-producer`, `structural, not willed` (observable fact: 0 each).
- **F7 [VERIFIED — non-issue]** Clippy DOES carry the pre-06-02 capabilities, so the delta is bounded (not "from scratch"): worktree isolation, the MECHANICAL falsification pass (old coupling names), dispatch decomposition, integrity check, always-loopback, basis-cardinality. basis: `grep -rniE 'worktree|separate copy|escape-resist|isolation slot'` → 10 matches; `phases/investigate-design.md:171` "Falsification pass" section present (observable fact: 10 worktree matches; mechanical-pass section at :171).
- **F8 [VERIFIED — addressed by D1]** 4 sites carry the OLD `validation-watch.md` file path (kernel-consolidation `e6abcc8` repointed it to the `validation-watch/` folder). basis: `grep -rniE 'validation-watch\.md'` → 4 matches (`investigate-design.md:144,256`, `post-run-review.md:162`, `SKILL.md:209`) (observable fact: 4 matches).
- **F9 [VERIFIED — non-issue]** model-tier (`3d03d0c`) is correctly absent from clippy — it is an anneal-dev-only binding. basis: `grep -rniE 'model-tier'` → 0 matches (observable fact: 0; correct per the anneal-dev-only scope in `instance-reinstantiation.md`).
- **F10 [VERIFIED — non-issue]** (cycle 3, Over-claimed-verification lens) The [VERIFIED] re-render decisions for the not-fully-read targets (D6/D8/D9/D13) are honest under the framing: the claim is "faithful re-render to current spec §§", whose completeness is verify-discharged (`foundation.md` contract 2), with the impl subagent reading the full current target; the delta lists are guidance, not an exhaustive design-time enumeration. basis: `foundation.md:21-30` (contract 2 — render fidelity verified by a separate context) — located read + observable fact (the contract names the separate-context verify as the fidelity check).
- **F11 [VERIFIED — non-issue]** (cycle 3, Missed-dependents lens) The coupling-shape rename's 7 occurrences all fall within D5/D6/D9/D12 target scope; re-rendering all files together keeps cross-file references coherent. basis: `grep -rniE 'target-shape|target-uses' plugin/skills/clippy/` → 7 matches, all in `phases/investigate-design.md`/`references/tracker.md`/`references/foundations.md` (observable fact: 7, all in D5/D6/D9 files).
- **F2 [VERIFIED — non-issue]** (closure) Retracted cycle-1 claim; its premise is contradicted by F5/F6 (clippy rendered nothing since 06-02; old coupling names present). basis: F5 + F6 (contradicting evidence). Does not hold the phase.
- **F3 [VERIFIED — non-issue]** (closure) Retracted cycle-1 boundary error; corrected by F5 (true boundary 2026-06-02 10:01). basis: F5. Does not hold the phase.
- **F4 [VERIFIED — addressed by D14]** SKILL.md over-budget is addressed by the D14 re-render's de-bloat (D2 fold-in). basis: D14 + D2.

### Cycle 4 (convergence — intent-falsification pass)
- **F12 [VERIFIED — surfaced → ACTIONED: amends D6/D14]** D6/D14 committed to REPOINT the validation-watch V-5/V-9 breadcrumb sites, but the framework DELETED those V-N breadcrumbs from its own source — faithful render is a STRIP, not a path-repoint (rendering source-deleted content + crossing framework-private provenance into a downstream plugin). basis (intent artifact, `cycle-4.intent-falsification.md`): `git show bf32f9c -- spec/core.md` → `-(V-5)`; `git show -s e453678` → "glossary zero V-N"; `grep -rniE 'V-[0-9]+' plugin/skills/clippy/` → 3 surviving breadcrumbs (observable fact: V-5/V-9 absent from live source, present in clippy).
- **F13 [VERIFIED — surfaced → ACTIONED: adds D15]** R5's "over-budget flagged" was scoped to SKILL.md only (F4 = single-file `wc -w`), but the re-render adds content to three already-large files. basis: `wc -w` sweep — `references/tracker.md` 3015, `phases/investigate-design.md` 2364, `phases/implement.md` 1988 (observable fact: 3 files ≥1988 words, grown by D9/D6/D7).
- **F14 [VERIFIED — surfaced → ACTIONED: sharpens D12]** bindings.md carries ~15 `core.md`/`modules.md` §-number citations; the citation-firewall convention covers instance sources, but D12 folded it under vague "citation touch-ups". basis: `grep -niE 'core.md §|modules.md §' coding-clippy/spec/bindings.md` → ~15 matches (observable fact: ~15 §-number citations).
- **Mechanical falsification pass: SKIPPED cycle 4** — design amended this cycle (D6'/D12'/D14'/D15 from F12/F13/F14); running mechanical on about-to-change [VERIFIED] entries would be stale (the L1 intent-delta skip rationale, `core.md §4.1.4`). Mechanical runs in the next convergence cycle after amendments settle.
  - basis-correction (cycle 5): F14's "~15" §-cites in bindings.md is actually **11** (6 `core`/`modules` + 5 `instantiation-guide`); cosmetic, does not change D12'. The `core.md §` exact grep missed them (cites spelled `` core.md` §`` ); the firewall regex `(core|modules)\.md[^\n]{0,4}§` catches them.

### Cycle 5 (convergence re-converge — intent-falsification pass)
- **F15 [VERIFIED — surfaced → ACTIONED: amends D3/D10]** `lens-set.md` (sibling instance spec, edited by D10) carries 3 framework §-citations (`modules.md §2.1` at :10,:204; `core.md §3.2` at :75) violating the citation-firewall, which covers instance specs AND rendered files; cycle-4 D12' converted `bindings.md` only. basis (cycle-5 intent artifact): `rg '(core|modules)\.md[^\n]{0,4}§' coding-clippy/spec/lens-set.md` → 3 hits (observable fact: 3 §-cites in the sibling source); `instantiation-guide.md:187-188` (firewall covers instance repo spec + rendered files).
- **F16 [VERIFIED — surfaced → ACTIONED: adds D16]** The firewall coherence check (`rg` §-numbers → empty), R4's acceptance gate per `instantiation-guide.md:190`, is not part of the render-fidelity verify the design defers completeness to (D1'/D5–D14); no D-entry runs it at verify, so firewall regressions would escape this run's safety net. basis: `grep -rniE 'firewall|rg .\(core' coding-clippy/plugin/skills/clippy/phases/verify.md` → empty; `instantiation-guide.md:190` (located read: render-verify runs the coherence check).
- **Mechanical falsification pass: SKIPPED cycle 5** — design amended this cycle (D3'/D10'/D16 from F15/F16); intent-delta skip (`core.md §4.1.4`). Mechanical runs next convergence cycle.

### Cycle 6 (convergence — intent CLEAN + mechanical falsification RAN)
- Intent-falsification pass: **CLEAN** (`cycle-6.intent-falsification.md`, 5/5 R# served, zero findings) → mechanical pass ran (no skip).
- **F17 [VERIFIED — addressed by D1''/D17]** (cycle 6, mechanical falsification) The mechanical pass FALSIFIED D1' on the target-dependents shape: `coding-clippy/spec/README.md:27` carries a stale `validation-watch.md` path, an un-enumerated dependent outside D1''s 10-target scope. Root cause (loopback triage, `core.md §4.2.7`): **adherence-class** — F8's enumeration grep was scoped to `plugin/skills/clippy/` only, excluding the co-located `spec/` dir (the true-unit-basis error `core.md §3.2.1` names). NOT a framework spec/render gap; noted, no source fix. basis (`cycle-6.falsification.md`): orchestrator-run corrected sweep `rg -n 'validation-watch\.md' .` → 5 sites, the 5th = `spec/README.md:27` (observable fact: 5 sites, 1 outside scope).
- **Mechanical pass otherwise: all 16 other entries HOLD** — every cited source clause extant; coupling-rename (7 sites), V-N strip (3), firewall conversions (14 across 6 active files; archived `docs/` out of scope) fully enumerated within converting decisions. D13 re-computed HOLDS (orchestrator coverage-check: `spec/README.md:27` is a scope-D1' dependent, not a dependent of D13's target `post-run-review.md`).

## D-track (design decisions)

- **D1 [INVALIDATED]** (cycle 2) The cycle-1 scope ("render only the 8 since-06-05 commits; moderate not heavy") rested on F2+F3, now invalidated. Reopened → re-formed as D1' below.
- **D1' [PENDING]** **Scope = a near-full faithful re-render of ALL clippy plugin files from the current live spec** — `references/foundations.md`, `phases/{investigate-design,implement,verify}.md`, `references/{tracker,lenses,post-run-review}.md`, `SKILL.md` — plus the instance source sections in `coding-clippy/spec/{bindings.md,lens-set.md}` that the relevant rendered files derive from. Driven by the source-delta (F5: 28 commits 06-02→06-07), NOT commit-by-commit replay: each file is re-rendered to the current spec and a render-fidelity verify (separate context) is the safety net (work-kind 1, faithful propagation). The clippy-effective capability carries are F6's semantic set + the c-safe coupling-rename (F2) + the validation-watch repoint (F8) + citation-firewall (cite glossary, not framework §-numbers); the T1/T2/T3 vocab abstractions are largely no-ops for clippy (file:line / grep / code are clippy's legit domain re-bindings). basis: F5+F6+F7+F8 (true scope on evidence); render-debt batch policy unit = source-delta (`instance-reinstantiation.md`). Per-file render mapping + the lens-set instance-source edits = next cycle's [VERIFIED] work.
- **D2 [CONDITIONAL]** **SKILL.md de-bloat (F4) is folded into the SKILL.md re-render unit, NOT split.** Committed default: since the full re-render touches SKILL.md wholesale and invokes skill-craft regardless, folding the ~580-word de-bloat in is low marginal cost and avoids a second skill-craft pass on the same file. **Assumption (operator-resolvable):** the operator may prefer to split it (keep this run pure faithful-propagation). basis: F4 (word count 2579 over budget) + D1' (SKILL.md is already a re-render unit); skill-craft invocation is mandatory on the SKILL.md edit either way (`CLAUDE.md` Rule-corpus edits).

### Cycle 3 (per-target render design)
- **D1' [VERIFIED]** Scope locked = re-render all 8 clippy plugin files + 2 instance-spec source sections (10 targets, enumerated in D5–D14). basis: F5 (28-commit source-delta) + F6 (missing semantic set) + F7 (bounded — pre-06-02 carried) + the per-file scope search (cycle 2: `grep -rnl` per contract → each contract's file set) (observable fact: 10 distinct target files).
- **D3 [CONDITIONAL]** **Citation-firewall folded into each file's render** — rendered plugin files cite glossary *terms*, not framework `§`-numbers (convention `b56f7d8`); e.g. `foundations.md:104` `core.md §3.2.2`, `lenses.md:70` `core.md §3.2` re-point to the glossary term. Default: fold in (we touch every file; glossary is current and carries every term — `glossary.md` read). **Assumption (operator-resolvable):** operator may split it to keep the run semantic-only. basis: `b56f7d8` citation-firewall convention + `glossary.md` (terms present); current §-number cites located by cycle-2 search.
- **D4 [VERIFIED]** **Run-state binding: clippy KEEPS `.clippy/` gitignored** (legitimate downstream binding — `d0f479a` D3 lets a downstream instance bind off; a code instance must not commit run-state into user product repos), AND renders the explicit **integrity-check run-state exclusion** (`core.md §4.2.4`) + keeps worktree run-state provisioning. basis: `d0f479a` D3 (downstream may bind off) + `foundation.md` contract 3 (instance domain-binding scope) + `core.md:811-820` (the §4.2.4 run-state exclusion clause, located read).
- **D5 [VERIFIED]** Re-render `references/foundations.md` from `core.md` §3.1 (bind/surface relabel: "binds when producer-independently re-derivable" / "strong surfacer" / operator-terminus), §3.2.1 (provenance edge: meaning/provenance label grounded at producer), §3.2.2 (full current: non-reference structural dependents path-hardcoder/producer/co-producer + emitted-effect "emits rather than returns" + behavior-CHANGE spike clause), §3.2.3 (secondary sources) + **add a Purpose section** carrying the two goals + **Mechanism: structural, not willed** (`24fdb5f`; matches anneal-dev foundations structure; render-completeness contract 2). Acceptance: render-fidelity verify clean clause-by-clause vs cited §§. basis: `core.md:22-31` (Mechanism clause), `:139-173` (§3.1 relabel), `:216-232` (§3.2.1 provenance), `:247-316` (§3.2.2 full) — located reads; current target `foundations.md` has no Purpose + old §3.1 framing (read).
- **D6 [VERIFIED]** Re-render `phases/investigate-design.md` from `core.md` §4.1 (**Requirements record** section — currently ABSENT; rivals-during-formation; scope) + §4.1.4 (**intent-falsification pass** + mechanical pass + **L1 intent→mechanical sequencing** + [CONDITIONAL]/[AUTO-ACCEPTED] technical-shape exemption-narrowing) + `modules.md` §3.3 dispatch-brief (intent-clean input). Update coupling-shape names `target-shape/target-uses`→`target-existence/target-dependents`; repoint `validation-watch.md`→`validation-watch/` (`:144,:256`). Acceptance: render-fidelity clean. basis: `core.md:392-404` (requirements record), `:476-679` (§4.1.4 convergence), `:364-379` (rivals); current target lacks intent pass + uses old names (read).
- **D7 [VERIFIED]** Re-render `phases/implement.md` from `core.md` §4.2 — **loopback root-cause triage** (§4.2.7, currently absent), **green-on-commit checkpoint** (§4.2.8), **run-state integrity exclusion** (§4.2.4 per D4), self-check (§4.2.5). Acceptance: render-fidelity clean. basis: `core.md:903-917` (loopback triage), `:919-942` (checkpoint green), `:811-820` (run-state exclusion) — located reads; current-target deltas from cycle-2 scope search (`grep -rnl 'loopback|checkpoint'`).
- **D8 [VERIFIED]** Re-render `phases/verify.md` from `core.md` §4.3 — **requirements-coverage check** (currently absent), loopback root-cause triage on [ISSUES FOUND], design-completeness audit, de-prioritization-requires-artifact. Acceptance: render-fidelity clean. basis: `core.md:981-991` (requirements coverage), `:1020-1022` (loopback triage at verify), `:998-1011` (de-prioritization) — located reads.
- **D9 [VERIFIED]** Re-render `references/tracker.md` from `core.md` §5 (finding states incl **[VERIFIED — surfaced]** + **deferred (c) pending-authoring**; design-decision states) + `modules.md` §3.4/§3.4.1 (**intent-falsification-pass artifact**; mechanical artifact + **intent-clean per-artifact header field**) + §3.1 (**considered required-where-≥2-rivals**). Acceptance: render-fidelity clean. basis: `core.md:1055-1102` (finding dispositions incl c + surfaced), `modules.md:440-510` (intent-falsification artifact), `:186-196` (considered required) — located reads.
- **D10 [VERIFIED]** Edit instance source `coding-clippy/spec/lens-set.md` — add the §3.2.2 **co-producer** (sibling-producer-of-shared-sink: search the sink's writers, not the symbol) + **emitted-effect** (read the body, not the callers) forms to the Coupled-change + Consumer-enumeration lenses; this mirrors the framework's own two-place structure (basis rule §3.2.2 + Missed-dependents lens), so not new fragmentation. Acceptance: lens-entry shape held; render-fidelity vs `core.md §3.2.2` + `glossary.md` Coupling-shape. basis: `glossary.md:98-135` (Coupling-shape co-producer), `core.md:269-289` (emitted-effect/co-producer method) — located reads; current `lens-set.md` Consumer-enumeration lacks both (read).
- **D11 [VERIFIED]** Re-render `references/lenses.md` from the D10-updated `spec/lens-set.md` (lenses.md renders FROM lens-set.md). **Dependency: after D10.** Acceptance: render-fidelity vs lens-set.md. basis: `CLAUDE.md` (clippy) "lens set lives in spec/, renders into the instance's plugin files"; current `lenses.md` is the render of current lens-set.md (read).
- **D12 [VERIFIED]** Edit instance source `coding-clippy/spec/bindings.md` — coupling-shape binding to new names where referenced; run-state binding per D4 (keep gitignored; note integrity-exclusion); citation touch-ups. Acceptance: bindings table coherent; consumed by D5–D14 renders. basis: `bindings.md:21-34` (binding table), `:198-203` (run-state provisioning) — located reads; D4.
- **D13 [VERIFIED]** Re-render `references/post-run-review.md` from framework `post-run-review.md` — **validation-watch folder** (`dev-notes/validation-watch/`, was `validation-watch.md` at `:162`) + the **Q7 lifecycle walk** (WATCHING/FIX-SHIPPED opportunity-handled/not-exercised/evaded; archive scan) + Q-set sync. Acceptance: render-fidelity vs framework `post-run-review.md`. basis: `post-run-review.md:164-195` (Q7 walk), `:222-235` (outcomes land, folder) — located reads; current target `:162` old path (cycle-2 search).
- **D14 [VERIFIED]** Re-render `SKILL.md` from `core.md` §1/§6 + `modules.md` §1 — **convergence-cycle status** in closed-artifact State-summary carrying intent-falsification-pass + mechanical-pass citations (`modules.md §1.1`); repoint `validation-watch.md` (`:209`); + **de-bloat per D2** (skill-craft-gated, target ≤~2000 words). Acceptance: render-fidelity + skill-craft form pass. basis: `modules.md:45-62` (closed-artifact convergence status), `SKILL.md:209` (old vw path), F4 (word count) — located reads.

### Cycle 4 (convergence — design amendments from the intent pass)
- **D6 [INVALIDATED]** (cycle 4) validation-watch handling amended per F12.
- **D6' [VERIFIED]** As D6, except: the validation-watch V-5 sites (`investigate-design.md:144,256`) are **STRIPPED**, not repointed — the framework deleted those V-N breadcrumbs from its source, so faithful render removes them (the V-5 references to the false-[READY] failure shape become plain prose without the breadcrumb). All other D6 content unchanged. basis: F12 + `git show bf32f9c -- spec/core.md` (V-5 removed from §4.1.2).
- **D14 [INVALIDATED]** (cycle 4) validation-watch handling amended per F12.
- **D14' [VERIFIED]** As D14, except: the V-9 breadcrumb at `SKILL.md:209` is **STRIPPED**, not repointed (same basis as D6'). All other D14 content unchanged. basis: F12 + `git show -s e453678` (glossary zero V-N).
- **D12 [INVALIDATED]** (cycle 4) citation scope sharpened per F14.
- **D12' [VERIFIED]** As D12, plus: explicitly **convert bindings.md's ~15 framework §-number citations to glossary-term references** per the citation-firewall convention (the binding *table*'s framework-term mappings are already firewall-conformant — they name terms, not §-numbers; the conversion targets the prose §-number citations). Where no glossary term covers a specific mechanism reference, keep a minimal named reference. basis: F14 + `b56f7d8` (firewall convention covers "an instance").
- **D15 [CONDITIONAL]** **Budget: FLAG the three already-large files** (`references/tracker.md` 3015, `phases/investigate-design.md` 2364, `phases/implement.md` 1988 words) as over/near skill-craft's ideal — R5's "flag" requirement, now satisfied across the file set, not just SKILL.md. **De-bloat of these reference/phase files is DEFERRED** (committed default): skill-craft's tight 1500–2000 ideal targets the always-loaded SKILL.md (D14); reference/phase files carry more latitude, and de-bloating three more files is a work-kind-2 expansion that would mix with this faithful-propagation run. **Assumption (operator-resolvable):** operator may pull the de-bloat into this run. basis: F13 (`wc -w` sweep) + skill-craft budget-targets-activation-file rationale; a deferred-but-flagged item → backlog follow-up `clippy-reference-file-debloat` if not pulled in.

### Cycle 5 (re-converge — design amendments from the intent pass)
- **D3 [INVALIDATED]** (cycle 5) firewall scope sharpened per F15.
- **D3' [VERIFIED]** Citation-firewall scope = **rendered plugin files AND instance spec sources** (not just rendered files) — per `instantiation-guide.md:187-188` the firewall covers an instance's spec + rendered files. Carried by: D5–D14 (rendered files), D12' (`bindings.md`), D10' (`lens-set.md`). Acceptance: the firewall coherence check (D16) is empty across all clippy instance specs + rendered files. basis: F15 + `instantiation-guide.md:187-188`.
- **D10 [INVALIDATED]** (cycle 5) firewall conversion added per F15.
- **D10' [VERIFIED]** As D10, plus: convert `lens-set.md`'s 3 framework §-citations (`:10,:75,:204`) to glossary-term references (firewall, per D3'). All other D10 content (co-producer/emitted-effect forms) unchanged. basis: F15 + `rg` 3 hits.
- **D16 [VERIFIED]** **This run's render-fidelity verify (R4) includes the firewall coherence check** — `rg '(core|modules)\.md[^\n]{0,4}§'` over clippy's instance specs (`spec/bindings.md`, `spec/lens-set.md`) + rendered plugin files → must be empty; a non-empty result is a verify finding (`instantiation-guide.md:190`). This is a **verify-acceptance criterion for THIS run** (the meta-verify of the clippy render), NOT content rendered into clippy's runtime `verify.md` (which verifies coding tasks — the firewall is a clippy-render-time concern, not a clippy-runtime one). basis: F16 + `instantiation-guide.md:190` (render-verify runs the coherence check).

### Cycle 6 (mechanical falsification — scope re-formed)
- **D1' [INVALIDATED]** (cycle 6) scope falsified by F17 (`spec/README.md` un-enumerated).
- **D1'' [VERIFIED]** Scope = **11 targets**: the 8 plugin files + `spec/bindings.md` + `spec/lens-set.md` + **`spec/README.md`** (the active instance-spec README, missed by the cycle-1/2 plugin-scoped search). Completeness basis is now the **corrected un-narrowed whole-repo sweep** (orchestrator-run): old coupling-names (7, all in D6'/D9), firewall §-cites (active spec+rendered only — `docs/archived/` is out of scope, archived not live), V-N breadcrumbs (3, all in D6'/D14'), validation-watch paths (5, now all in scope incl spec/README.md). basis: F17 + `rg -n 'validation-watch\.md|target-shape|target-uses|(core|modules)\.md[^\n]{0,4}§|V-[0-9]+' .` whole-repo (observable fact: only `spec/README.md` newly surfaced; rest covered).
- **D17 [VERIFIED]** Repoint `spec/README.md:27` `validation-watch.md` → `validation-watch/` (folder) — a legitimate stale-path cross-reference (the framework renamed the file to a folder), repoint not strip (it is not a V-N breadcrumb; it references where framework uncertain-decisions are tracked). basis: F17 + `sed -n '26,27p' spec/README.md` (the stale path text).

### Cycle 7 (CONVERGENCE — both passes clean) → [READY]
- **Convergence cycle CLEAN:** intent-falsification pass CLEAN (`cycle-7.intent-falsification.md`, 5/5 R# served) + mechanical falsification pass ALL-HOLD (`cycle-7.falsification.md`, every [VERIFIED] entry + D2/D15 technical shapes aggregate `holds`) + **zero D-track deltas**. Standardized pass vacuously clean (no design produced this cycle).
- **[READY] supporting facts (§4.1.1):** all 17 findings terminal (no [INVALIDATED] open, no load-bearing finding < [VERIFIED]); all design decisions [VERIFIED] (D1'',D4,D5,D6',D7,D8,D9,D10',D11,D12',D13,D14',D16,D17) or [CONDITIONAL]→[AUTO-ACCEPTED]-on-next-phase (D2,D3 already [VERIFIED] as D3',D15); standardized lens set accounted for whole across cycles 3–7; every [VERIFIED] decision's target/count premises carry a re-runnable basis (re-confirmed by the cycle-7 mechanical target-existence holds).
- **Fresh-session implementability: PASSED** — per-implementer-step external evidence: each of the 11 impl-plan units renders from a source clause confirmed EXTANT by the cycle-7 mechanical target-existence candidates (D5: `core.md:22/222/275/320`; D6': `core.md:392/489`; D7: `core.md:903/922/813`; D8: `core.md:981/1006`; D9: `core.md:1078`+`modules.md:51/187`; D10': `core.md:261`+`glossary.md:122`; D12'/D10' firewall sites located; D13: framework `post-run-review.md` vw-folder+Q7; D14': `modules.md:49`+`SKILL.md:209`; D17: `dev-notes/validation-watch/` exists) into a located target file (D1'' 11-target sweep). The softest step (D9/U9 SKILL.md de-bloat) is skill-craft-guided realization, not a new design decision. Result line recorded for post-run review.
- **Impl plan:** `clippy-reinstantiation.impl-plan.md` — 11 units, 10 parallel-eligible (distinct files) + U2(lenses)←U1(lens-set); disjointness basis = pairwise-distinct paths (D1'').

### implement phase
- **D2 [AUTO-ACCEPTED]** (operator selected `next phase` without override) — SKILL.md de-bloat folded into U9.
- **D15 [AUTO-ACCEPTED]** (operator selected `next phase` without override) — budget flag only; reference/phase de-bloat deferred to follow-up `clippy-reference-file-debloat`.

#### implement complete — all 11 units + 4 reconciliation fixes rendered (uncommitted, working tree)
- 11 targets written (`git status --short` → 11 M files). Per-unit self-checks clean (skill-craft PASS each; Lossy/Missed/Undefined/Over-claimed clean). U9 SKILL.md de-bloated 2579→2182 words, no rule dropped (enumerated). Word counts: foundations 2216, tracker 3880, investigate-design 3545, implement 2319 (the large reference/phase files — D15 flag, de-bloat deferred).
- Reconciliation fixes (cross-file coherence, within locked scope): A (verify.md triage pointer → implement.md), B (lens-set.md line-wrapped §2.2 → glossary term), E (investigate-design.md 6× "falsification-pass artifact" → "mechanical …"), F (tracker.md [CONDITIONAL]/[AUTO-ACCEPTED] technical-shapes-only line rule added).
- **Orchestrator mechanical checks CLEAN:** firewall D16 (`rg '(core|modules)\.md[^\n]{0,4}§'` single-line + wrap-tolerant → empty across all 11); zero old coupling-names / validation-watch.md / V-N breadcrumbs in the rendered set.
- **Residual for VERIFY to adjudicate (impl-surfaced, not pre-fixed):** `investigate-design.md:249,435` summarize the mechanical artifact as "one line per [VERIFIED] entry" deferring the [CONDITIONAL]/[AUTO-ACCEPTED] rule to tracker.md (Fix F home) — possibly fine as summary-defers-to-authoritative, possibly a clause-gap; the independent render-fidelity verify decides.
- **Render-follow obligations surfaced (OUT of scope — file as backlog):** sibling instance **daneel** lacks the same §4.2 deltas (loopback-triage, green-checkpoint) + likely the intent-pass/requirements-record/etc. — a `daneel-reinstantiation` follow-up. Also: the D16 firewall-check regex is not wrap-tolerant (missed B's line-wrapped cite) — a check-robustness note for the kernel/instantiation-guide.

### Cycle 8 (verify [ISSUES FOUND] → loopback)
- **VERIFY result: [ISSUES FOUND]** (isolated subagent, opus) — 1 Lossy-render finding; all else faithful (planned-vs-actual all D carried, R1–R5 covered, firewall battery clean single+wrap-tolerant, SKILL.md de-bloat dropped no rule [enumerated pre=post], the impl-surfaced residual adjudicated faithful summary-defers-to-authoritative — NOT a gap).
- **F18 [PENDING]** Lossy-render: `foundations.md:149-150` (D5 §3.2.2 render) carries the emitted-effect falsification verdict ("an effect with no successor falsifies") but DROPPED the co-producer half. basis: source `core.md:284-289` pairs them ("a co-producer whose emitted format diverges from the target's falsifies — target-dependents/any-match"); `grep -niE 'co-producer' coding-clippy/...` → 5 sites all carrying the enumeration obligation only, `grep 'diverge'` → 0 (observable fact: co-producer falsification-verdict absent, emitted-effect's present — asymmetric drop).
- **Loopback root-cause triage (§4.2.7):** **render-root** → re-render in-run. The spec is correct + the §3.2.2 rule is clear (not spec/adherence); D5's design specifies "§3.2.2 full incl co-producer" — the impl (U3) dropped one sub-clause. Disposition: re-render foundations.md's §3.2.2 to add the co-producer falsification verdict. basis: F18 (the verify finding). D5 design unchanged (impl-fidelity miss, not a design defect).
- **F18 [VERIFIED — addressed]** Fix dispatched + applied: `foundations.md:150-151` now carries "A co-producer whose emitted format or value diverges from the target's falsifies." — symmetric with the emitted-effect verdict (both untagged per clippy's convention), faithful to `core.md:284-289`. Correctly scoped to foundations.md (the lens entries are enumeration-scope, no verdict — adding there would fragment the basis-rule). basis: the fix subagent's render + delta-verify confirmation.
- **DELTA VERIFY: [PASSED]** (isolated subagent) — F18 closed (clause present + symmetric + faithful) + minimal regression (firewall clean single+wrap-tolerant; §3.2.2's other deltas intact — path-hardcoder/producer/co-producer enumeration, emitted-effect read-the-body, behavior-CHANGE spike; well-formed, no conflict markers). Fix changed exactly the one clause.

### RUN COMPLETE — verify [PASSED]
- Run terminal reached: verify [PASSED] (delta verify, after the cycle-8 loopback fix). All 18 findings terminal; all design decisions [VERIFIED]/[AUTO-ACCEPTED].
- **Post-verify extensions:** `render-and-open-diff` does not apply — this run IS the render (the extension re-renders plugins from a verified *spec change*; this run is an instance render, not a spec change). No extension fired.
- **Work product:** 11 clippy files re-rendered, UNCOMMITTED in `coding-clippy` working tree — held for operator review + release per "operator owns the render commit" + the recursion-check (rule-edit subagent PASSes may self-validate; pause before push). Operator owns: review diff → commit to coding-clippy → version bump + `claude plugin update` (per the `anneal-dev-reinstantiation` precedent).
- **Spawned backlog items (file per no-silent-deferral):** `daneel-reinstantiation` (sibling lags the same deltas) · `clippy-reference-file-debloat` (D15-deferred: tracker 3880 / investigate-design 3545 / implement 2319 / foundations 2216) · `firewall-regex-wrap-tolerance` (the D16/instantiation-guide firewall-check regex misses newline-split cites — caught B only via U1's manual flag).
- **instance-reinstantiation.md update:** clippy's render obligation DISCHARGED; the capability-matrix "clippy lags since pre-session-6" was a stale secondary source (clippy was current to 2026-06-02; this run rendered the 06-02→06-07 source-delta).
- **RELEASED (operator-authorized):** `coding-clippy` `298bf88` (v0.9.95, 11 files + plugin.json bump) committed on `main` + pushed to github; `claude plugin marketplace update` + `claude plugin update` → cache **0.9.95 LIVE** (verified carries the render: SKILL.md 2182w, intent-falsification 19 hits, requirements-record 7, F18 co-producer-falsification clause present, firewall clean, 0 old coupling-names). Operator runs `/reload-plugins` to swap in-session. (Recursion-check honored: isolated verify + orchestrator re-read before push.)
