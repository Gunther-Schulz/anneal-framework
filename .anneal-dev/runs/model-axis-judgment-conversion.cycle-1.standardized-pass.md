# model-axis-judgment-conversion — cycle 1 standardized pass

Lens set: core (anneal-dev/spec/lens-set.md rendering) ∪ project supplement
(anneal-dev.config/lenses.md = placeholder, empty — parsed clean, no supplement entries).

- **Bloat** — clean: every kernel addition carries a namable load-bearing function (D2 header
  field = residual-visibility artifact; D4 member-disposition = completeness claim; D5 example =
  implementability + verify-executable check; D6 re-derivation = primary-source grounding).
  This-cycle basis: D2-D6.
- **Fragmentation** — finding-resolved-in-design: the checker-capability principle risked a second
  statement beside the instance rule; D1 resolves to one kernel statement + instance concretion.
  Corpus search for existing statements: `rg -ln 'Dispatch model tier|model-tier'` (F2) — the only
  in-corpus statement is bindings.md + its renders; pbs GOVERNANCE Nr. 8b is out-of-corpus
  (report-only per R6). This-cycle basis: F2, D1.
- **Leakage** — clean: D2/D4/D5/D6 kernel wording is harness- and domain-agnostic (no model
  names, no domain vocabulary; "closed value set", "boundary example", "primary source" pass the
  framework-arbitrariness test; model names stay in bindings.md per D1). This-cycle basis: F1, D1.
- **Missed-dependents** — clean via scope searches: §4.1.4/§3.4.1 dependents (`rg -l
  'intent-falsification'` → 11 files enumerated in D8), §3.2.2 dependents (`rg -ln '3\.2\.2'`),
  Body-shape dependents (`rg -ln 'acceptance criteria|Body shape'`), tier-rule dependents (F2).
  Plugin renders queued (F5/D8); modellwahl rule 3 in-cycle (F6). This-cycle basis: D8.
- **Unenforced-rule** — clean: each new rule names its artifact — D2: §3.4.1 header field
  (malformed-if-absent); D4: member-disposition in the basis (falsifiable expected-match);
  D5: example presence in D-body (checkable); D6: refutation-field primary-source citation.
  This-cycle basis: D2, D4, D5, D6.
- **Undefined-shorthand** — finding-resolved-in-design: "checker-capability relation" is a new
  load-bearing term → D7 glossary entry same commit (practice 10); "attention-class" avoided in
  kernel text (F4). This-cycle basis: F4, D7.
- **Lossy-render** — out of scope this cycle: no render committed; render-debt queued (D8, F5).
- **Over-claimed-verification** — clean: every tracker basis field carries the executable query
  or located-read + one fact (F1-F7); no "checked all" without query. This-cycle basis: F1-F7.
