# Cycle 1 — standardized inspection pass (design-lock cycle)

Lenses applied to the design committed this cycle (D1–D7). This-cycle basis cited.

- **Fragmentation** — cited-clean (load-bearing): the design *resolves* a fragmentation (F2 — Q7 restates
  the README closing logic; D7 has Q7 defer to the README, one canonical home). The split (D5) introduces
  two watch-kinds but defines them once (README Vocabulary, D5) — no second copy. Basis: F2 + D5 + D7.
- **Bloat** — cited-clean, watched: the design coins terms (correctness-watch / quality-watch / catcher /
  opportunity-test). Each is load-bearing — the split (operator-chosen), the two exhibition mechanisms,
  the closing-evidence shapes. Economy enforced at impl (README Vocabulary, terse). No motivational prose
  added; the closing rule replaces the broken one (net even, not additive). Basis: D2/D4/D5.
- **Undefined-shorthand** — cited-clean: every new term (correctness-watch, quality-watch, catcher,
  opportunity-test) is committed to the README Vocabulary (D5) with a single definition; "opportunity-
  exercised" is the closing rule (D2), defined in the Entry lifecycle. Basis: D5 + D2.
- **Unenforced-rule** — cited-clean: the closing rule (D2) is enforced by Q7's per-entry walk (D7 — the
  post-run review asks "did an opportunity arise + did the fix handle it?"); name-your-catcher (D4) is
  enforced by the entry carrying the catcher/opportunity-test (an entry lacking one is malformed or
  correct-by-construction = not-a-watch). The artifact: the catcher-name in the entry + the Q7 walk's
  cited counterfactual. Basis: D2/D4/D7.
- **Leakage** — cited-clean: the lifecycle is dev-process-general (validation-watch machinery); no domain
  (code/clippy/daneel) concretion introduced. Basis: D1 scope (README + Q7 + V-13, all dev-process).
- **Lossy-render** — out of scope: the instance post-run-review Q7 render is DEFERRED (D1 spec-only); this
  run edits source (README + the framework post-run-review.md), no render committed.
- **Over-claimed-verification** — cited-clean: D-entry bases are operator-locked design + located reads
  (README:40-42, :12-21, V-13, Q7:173-191) — true-unit-covering, not proxies; D2 explicitly *generalizes*
  the existing README:40-42 "counter-factually shown" rather than over-claiming a new mechanism. Basis: D2–D7 basis fields.
- **Missed-dependents** — cited-clean: the closing-rule change's dependent set = README + Q7 (F1 corpus
  search); the `glossary.md:377` pointer carries no closing logic (unchanged); the instance Q7 renders are
  deferred debt. Basis: F1 (re-runnable search).

No new fragmentation/bloat introduced; the design resolves the gap + a fragmentation. No load-bearing finding. Clean.

**D8 addendum (archive-check, added mid-cycle):**
- **Fragmentation/Bloat** — clean: D8 adds a new step (scan archive before a new entry), restates nothing; load-bearing (closes the recurrence-blindness archival creates). "archive-check" coined clean (no pre-existing use).
- **Unenforced-rule** — clean: enforced by the new-entry procedure (the README's "before a new entry" step + the existing post-run-review outcomes-land path); the artifact is the reopened-INVALIDATED entry (or the new V- only after a clean archive scan).
- **Missed-dependents** — clean: D8 lands in `README.md` (archival convention + new-entry step), within D1 scope; it supplies the trigger for the *existing* RESOLVED→INVALIDATED transition (README:47-56), no new dependent.
- **Over-claimed-verification** — clean: basis = operator-locked + README:68-72/47-56 reads.
