# f0-render-conventions — cycle 3 (convergence) standardized-pass

The convergence cycle's investigation pass (the intent-falsification dispatch) touched new surfaces:
guide `:18,:108,:120,:135-140`, the bindings-table left-column citations (`anneal-dev/spec/
bindings.md` rows 39/42/43), and the guide §3 firewall check. Lenses whose scope that touched:

- **Fragmentation** — FINDING (→D5): the template-file-vs-instance-consolidation reconciliation is
  stated in `instance-template/README.md:53-59` but NOT in guide §2, which instead presents the
  mechanism slots' worked examples as `bindings.md` sections (`:92,:108,:120`) — two surfaces of the
  same principle that have drifted (the guide contradicts the file scaffold). Resolve to one coherent
  statement carried in both. basis: F-B/F-C, `README:53-59` vs `guide:18,108`.
- **Missed-dependents** — FINDING (→D3/D4 revision): D3's amendment missed a dependent enumeration
  surface (guide `:18`, the "every slot" claim); D4's amendment missed the in-cell `core.md §3.2`
  citations (the left-column cells are dependents the firewall close must re-point). basis: F-C, F-D.
- **Undefined-shorthand / Bloat / Leakage / Lossy-render / Over-claimed-verification** — no NEW
  in-scope trigger this cycle beyond the reopened decisions (which return to design at cycle 4); the
  convergence findings above are the cycle's load-bearing output.

Convergence NOT clean — D-track delta (D2/D3/D4 reopened, D5 outlined). Mechanical falsification pass
skipped this cycle (intent-delta). Another cycle required.
