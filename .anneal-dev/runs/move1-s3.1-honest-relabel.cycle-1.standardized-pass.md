# Cycle 1 — standardized inspection pass

Cycle 1's investigation = the scope/dependent-enumeration (no rule-text added or amended this cycle — only design decisions). Lenses whose scope this cycle touched:

- **Missed-dependents** — CLEAN. The dependent enumeration (D1) is search-established (F1) across the whole corpus, all five dependent reference-classes covered (literal / cross-reference / paraphrased-restatement / rendered-clause / glossary-or-defined-term + self-classification-label). Basis: F1, D1.
- **Over-claimed-verification** — CLEAN. F1's completeness claim (the whole corpus) rests on the re-runnable wrap-tolerant searches (the executable queries) + a newline-flattened pass + cross-repo coverage — not a recalled corpus model; true-unit = the whole corpus, basis covers it. Basis: F1.

Out of scope this cycle (no rule-text added/amended — these fire on the change-set's prose, which impl produces): **Bloat, Fragmentation, Leakage, Unenforced-rule, Undefined-shorthand, Lossy-render**. Accounted for whole at [READY] once impl-target text exists / a render is committed.
