# Intent-falsification-pass artifact — enumeration-target-blindness, convergence cycle 3

Fresh-context subagent (opus), criteria-first against the requirements record R1–R6.
Result: design sound-for-intent; two findings (INT-1, INT-2) fold into D4/D5 as
acceptance criteria (D-delta) → convergence NOT reached this cycle. Mechanical pass
SKIPPED (intent-delta this cycle).

## Per-R# attack lines

- **{R1, refutation:** D3 (`:tracker`) vs bare clause `core.md:256-257`. **outcome: served}** — D3 names the emit-not-return form + read-the-body method as a genuine delta against the bare clause.
- **{R2, refutation:** D4 vs existing producer↔consumer `core.md:250-254`, `glossary.md:117-122`. **outcome: served}** — distinct form + general method; residual distinctness-clarity = INT-2 (does not unseat served).
- **{R3, refutation:** D5 consolidation/Pareto claim. **outcome: served}** — shared meta-shape binds both forms; extends two existing clauses, no new section.
- **{R4, refutation (KEYSTONE):** D2 mapping vs closed-set invariant `glossary.md:98-127`, `lens-set.md:118-119`; vs mediation-by-artifact precedent `glossary.md:117-122`. **outcome: served}** — co-producer dependency resolves through the shared sink's FORMAT CONTRACT (not a bare symbol↔symbol edge); symmetric to the closed producer↔consumer mediation. No fourth shape. (Licensing must be carried inline → INT-1.)
- **{R5, refutation (TEETH):** D3 `expected-match` + D4 `any-match` candidates vs `modules.md:383-390` + the actual bugs. **outcome: finding}** — INT-3 (expected-match catches missing, not wrong/partial successor) + INT-4 (co-producer candidate is recognition-gated).
- **{R6, refutation:** D1/D5 dependent set vs F5 corpus search + F4 modules read. **outcome: served}** — dependents enumerated (search-grounded) + each dispositioned; modules-clean + §5.2-reconcile have located-read bases.

## Per-finding lines

- **{INT-1, criterion: R4, refutation:** `core.md:250-254` (existing rendering carries its shape-licensing inline) vs D4/D5 deltas (form+pointer, no inline licensing). **route: [VERIFIED — surfaced]}** — realization-adequacy; the rendered co-producer sentence must state WHY it is not a fourth shape (dependency mediated through the format contract), or a coherence-audit re-opens the closed-set question. → **fold as D4/D5 acceptance criterion.**
- **{INT-2, criterion: R2, refutation:** `core.md:250-251` ("producer of an artifact the target consumes") vs D4 (new "target produces a value… enumerate every producer/writer") — same "producer" token, inverted target-role; the bug IS this confusion (`co-producer-format-parity.md:38-49`). **route: [VERIFIED — surfaced]}** — the rendered sentence must disambiguate by target-role (target-as-consumer → enumerate its producer; target-as-producer → enumerate sibling producers of the shared sink). → **fold as D4/D5 acceptance criterion.**
- **{INT-3, criterion: R5, refutation:** `modules.md:377-382` (`expected-match` = pattern-presence) vs multi-call partial drop `replacement-side-effect-behavior-parity.md:15`. **route: [VERIFIED — surfaced]}** — mechanical-floor limit the source item already acknowledges (`:73-76`); D3 correctly claims a floor, not completeness → NOT a D-defect. Stated limitation.
- **{INT-4, criterion: R5, refutation:** `co-producer-format-parity.md:38-49` (bug = a recognition miss; sibling surfaced but classed as consumer) vs D4 candidate (input = the sink, whose recognition is the missed step). **route: [VERIFIED — surfaced]}** — KEYSTONE TEETH-LIMIT: the falsification candidate is recognition-gated (takes the sink as input → empty writer-set → vacuous `holds` on the unrecognized-sink failure mode). R2's recurrence-reduction rests on whether the rule's PHRASING makes the AI recognize the equality-compared sink — a judgment property, not candidate-guaranteed. Weigh at the soundness gate.
- **{INT-5, criterion: R2/D6, refutation:** `co-producer-format-parity.md:72-77` (structural cure = "the actual fix") vs D6 (excludes it, promises a separate file). **route: [VERIFIED — surfaced]}** — D6's exclusion is sound (completeness-rule ≠ design-heuristic) ONLY if the promised structural-cure backlog item is actually filed; an unfiled promise re-creates the silent-deferral that enabled the original bug (Refactor #58). → confirm the item exists (no-silent-deferral). Fold as a D6 completion obligation.
- **{INT-6, criterion: R6/D7, refutation:** `core.md:1123-1124` (paraphrase is a parenthetical pointer-in-prose; canonical legs at `core.md:254-257`). **route: [VERIFIED — surfaced]}** — de-dup to a pointer is lossless (reachability preserved, drift-source removed). Clean attack, no D-change.

## Bottom line
Design sound-for-intent. INT-1/INT-2 → D4/D5 acceptance criteria (D-delta; re-converge).
INT-4 (recognition-bounded teeth) + INT-5 (file the cure item) are load-bearing for the
operator's step-4 soundness verdict. INT-3 a stated floor; INT-6 clean.
