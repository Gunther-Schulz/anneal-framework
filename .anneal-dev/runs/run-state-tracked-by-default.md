# anneal-dev run: run-state-tracked-by-default

## Header
- Status: PASSED (2026-06-06) — kernel edit: run-state tracked by default (per-instance binding, D3) + in-place integrity / separate-copy integration exclude the run-state dir (work-product-vs-bookkeeping, D4/D7). Operator soundness given at [READY] (next-phase); implement (5 units, working-context) + isolated verify [PASSED] (subagent `a4ab6fd41de57299c`). Shipped: `instantiation-guide.md §5` + `persistence.md` + `bindings.md` + `core.md §4.2.4`; render-debt row filed (D5); F-prov-framing follow-up filed. Awaiting operator commit-approval. **Dogfood:** this run's own tracker/artifacts are tracked-in-place by the very policy it formalizes (the commit-5910cf4 this-repo behavior).
- Phase: verify (terminal — [PASSED])
- Mode: interactive. **METHOD-KERNEL edit** (`instantiation-guide.md` §5 + `anneal-dev/spec/*`) → verify = skill-craft FORM review + **irreducible OPERATOR SOUNDNESS** verdict + foundation-invariant register check. Self-hosting: grounded in live co-located spec.
- Task: formalize the runs-data decision (`dev-notes/backlog/runs-data-preservation.md`, DECIDED) at the framework level — the run-state-directory slot defaults to TRACKED (committed history), not gitignored; only transient local override-flags are gitignored. Rendered to all instances.

### Requirements record
**Operator verbatim:** "can we do this now? -> the framework formalization of the runs-data decision (bootstrap defaults the slot to tracked, rendered to all instances)." Prior decision (runs-data-preservation): track the run-state slot in place; stop gitignoring it; `foundation.md`'s own words — run-state = "the accumulated anneal-dev history."
**Goals (R#):**
- **R1** — the framework filesystem-layout rule (`instantiation-guide.md` §5) no longer mandates run-state gitignored; it defaults run-state to TRACKED (accumulated history), gitignoring only transient local override-flags.
- **R2** — anneal-dev's binding (`persistence.md` filesystem layout, `bindings.md` First-run bootstrap) matches: run-state tracked; bootstrap gitignores only the override-flag.
- **R3** — the **edit-permission / distinct-location contract** is PRESERVED (operator-editable config vs instance-written run-state stay distinct; operators don't hand-edit run-state) even though both are now tracked — i.e. the policy change must not collapse the contract §5 rule 1 also carries.
- **R4** — the parallel-isolation **provisioning** dependent (`bindings.md` :187-195) is reconciled: it must no longer rest on "run-state gitignored, so absent from a copy."
- **R5** — render-follow: the rendered anneal-dev plugin re-renders the changed bootstrap/persistence/foundations (queued to `instance-reinstantiation` render-debt; spec-only this run).

## F-track (cycle 1 — investigation pass; scope search)
- **F1 [VERIFIED — addressed by D0]** Scope (foundational). Load-bearing edit sites encoding the "run-state gitignored" binary: `instantiation-guide.md` §5 rule 1 (`:359-364` "runtime-state paths are gitignored"), rule 3 convention (`:371-376` "`.<plugin>/runs/` (gitignored)"), Project-init (`:385-387` "runtime-state directory is added to `.gitignore`"); `anneal-dev/spec/persistence.md` Filesystem layout (`:102-105` "gitignored — git check-ignore true"); `anneal-dev/spec/bindings.md` First-run bootstrap (`:295-296` "adds `.anneal-dev/` to `.gitignore`") + Operator-editable (`:282` "gitignored runtime state"). basis: `rg "gitignore|runtime state|run-state|check-ignore" foundation.md spec/ anneal-dev/spec/ instantiation-guide.md` (this session).
- **F2 [VERIFIED — surfaced → ACTIONED: D4]** Missed-dependent: the parallel-isolation **Provisioning** rule (`bindings.md :187-195`) rests on run-state being gitignored — "the run-state directory `.anneal-dev/runs/` … gitignored and so not carried by a copy — are provisioned into the separate copy." Flipping run-state to tracked falsifies the "gitignored, so absent from a copy" premise. basis: read `bindings.md :187-195`.
- **F3 [VERIFIED]** §5 rule 1 conflates TWO things: the **commit policy** (tracked vs gitignored) AND the **edit-permission contract** (operators don't hand-edit instance-written run-state; instances don't overwrite operator files). The decision changes only the former. basis: read `instantiation-guide.md :359-367`.
- **F4 [VERIFIED — non-issue]** Incidental "runtime state" mentions (`lens-supplement.md:15`, `extensions.md:17` "`.anneal-dev/runs/` runtime state"; `spec/core.md:764` in-place reading; `lens-set.md:113` `.gitignore` as a generic lens example) do NOT encode the gitignore-of-run-state policy → no-edit (the term "runtime state" still describes run-state the run uses at runtime; it's tracked history AND runtime-read). basis: read each cited line.

## D-track (cycle 1 — design)
- **D0 [VERIFIED] Scope.** Edit sites per F1 + the F2 dependent. No-edit: F4 incidentals; `persistence.md :55-95` (names the dir/accumulation, not the gitignore — already calls it "accumulated history," which the change ALIGNS with). Render-follow (D5). basis: F1/F2/F4.
- **D1 [VERIFIED] (R1/R2) The policy.** (b) delta: replace "runtime-state paths are gitignored / run-state gitignored" (§5 rule 1 `:363-364`, rule 3 `:373`, Project-init `:386-387`; `persistence.md :105`; `bindings.md :295-296`, `:282`) with: the run-state slot is **TRACKED** (the accumulated run history); only **transient local override-flags** (e.g. anneal-dev's `allow-adhoc-kernel-edit` run-gate bypass) are gitignored — committing an active override would re-arm it on every checkout. (c) acceptance: no surviving "run-state is gitignored" claim; the bootstrap adds only the override-flag to `.gitignore`. (e) basis: F1 + `foundation`/persistence "accumulated history" + the backlog DECISION.
- **D2 [VERIFIED] (R3) Preserve the edit-permission/distinct-location contract.** (b) delta: §5 rule 1 keeps "operator-editable and instance-written run-state live at observably distinct paths; operators don't hand-edit run-state, instances don't overwrite operator files" — but its OBSERVABLE marker changes from tracked-vs-gitignored to **namespace/path** (`<plugin>.config/` operator-editable ∥ `.<plugin>/runs/` instance-written history; both tracked) + the don't-hand-edit convention. Rule 3's "visible-vs-hidden maps to commit-vs-gitignore" → "maps to operator-editable-vs-instance-written" (both committed). (e) basis: F3.
- **D3 [CONDITIONAL → operator soundness] (R1 scope) Universal default-on.** All instances default to tracked run-state; the per-project opt-out is a manual `.gitignore` of the run-state dir (gitignore IS the toggle — NO new config slot). assumption: the recoverable-vs-unrecoverable asymmetry (lose-by-default is unrecoverable; clutter is a one-line opt-out) + discoverability (off-default is invisible) outweigh downstream-repo-courtesy. **Operator-soundness focus:** the downstream-clutter counter (a product repo gets instance run-trackers committed by default) — accept universal-on, or carve downstream default-off? considered: per-instance config slot (rejected: gitignore already is the per-project toggle; a slot duplicates it).
- **D4 [VERIFIED] (R4) Reconcile the provisioning dependent.** (b) delta: `bindings.md :187-195` — the provisioning rationale shifts from "run-state gitignored, so not carried by a copy" to "the **uncommitted current** run-state is not in a base-commit copy." The MECHANISM is unchanged (provision the current run-state into the copy, exclude from integration) — it stands because a base-commit separate copy lacks the run's *uncommitted* live tracker regardless of gitignore. (e) basis: read `bindings.md :187-195` + the separate-copy-at-base-commit mechanic (`core.md` §4.2).
- **D5 [VERIFIED] (R5) Render-follow.** The rendered anneal-dev plugin (SKILL.md First-run bootstrap + foundations + persistence reference) re-renders the changed policy; queued to `instance-reinstantiation` render-debt (spec-only this run, per the render-cadence policy). basis: the rendered SKILL.md First-run bootstrap carries "adds `.anneal-dev/` to `.gitignore`."
- **D6 [VERIFIED] (kernel-edit) Foundation-invariant register check.** This change touches NONE of INV-1 (basis-rule) / INV-2 (complete-state) / INV-3 (verify-isolation) / INV-4 (loopback-not-settled) / INV-5 (exclusion-via-named-falsifier) — it is a filesystem/gitignore-policy change, orthogonal to the kernel-soundness invariants. Verify-isolation (INV-3) reads the tracker regardless of its gitignore status → untouched. **Focusing artifact for operator soundness: focus on D3 (downstream-courtesy default) + D4 (provisioning reconcile); invariant-preservation is N/A.** basis: the 5 invariant titles (`dev-notes/foundation-invariants/`) vs the change's nature.

### Cycle 1 — standardized inspection pass (artifact: `.cycle-1.standardized-pass.md`)
In-scope (edits to `instantiation-guide.md` + `anneal-dev/spec/*` — domain-general framework + instance-spec) — all clean; see artifact.

### Cycle 1 — fresh-session implementability: PASSED
- D1/D2: `instantiation-guide.md :359-376`/`:385-387` + `persistence.md :102-105` + `bindings.md :282`/`:295-296` (located reads this session). PASSED.
- D4: `bindings.md :187-195` (located read). PASSED.

**Cycle 1 status:** design D0–D6 [VERIFIED] (D3 [CONDITIONAL] → operator soundness); standardized pass clean; fresh-session PASSED. → convergence cycle (cycle 2): new-surface investigation + intent-falsification (attack R1–R4, esp. D3 downstream-courtesy + D2 contract-preservation + D4 dependent) + mechanical falsification over [VERIFIED] D-entries.

### Cycle 2 — convergence: intent-falsification → D-DELTA (artifact `.cycle-2.intent-falsification.md`; subagent `a2640839dba9ff35c`, opus)
Found a genuine unsoundness (not a nitpick). Mechanical pass SKIPPED (intent-delta). Design NOT [READY]. **Operator confirmation sought before cycle 3** (operator request).
- **F-integrity-interaction → reopens D4 + D0** (load-bearing): tracked run-state collides with the in-place integrity check (`core.md :763-772`) — gitignore implicitly hid the orchestrator's mid-run tracker writes; tracked, they break "clean before dispatch" + a restore `reset --hard` would discard the live tracker. D4 only reconciled the separate-copy path.
- **F-persistence-58 → reopens D0** (scope grows to the in-place integrity sites).
- **F-contract-marker, F-leakage, F-render-debt-shape, F-D3-downstream-courtesy, F-D6-invariants-confirmed [VERIFIED — surfaced]** (see artifact).
- **D0 [INVALIDATED] → [PENDING]; D4 [INVALIDATED] → [PENDING].** D1/D2/D5/D6 stand (D6 confirmed). D3 [CONDITIONAL] → rework proposed.

### Cycle 3 — PROPOSED direction (pending operator confirmation — NOT yet locked)
- **D0 (scope+):** add the in-place integrity sites — `core.md §4.2` (`:763-772`) + `bindings.md :175-180` + `persistence.md :55-77`.
- **D7 (NEW — the integrity reconcile):** the in-place integrity check + restore EXPLICITLY exclude the run-state dir (`.anneal-dev/runs/`): clean-before-dispatch ignores it, "no other modification" ignores it, restore (`reset --hard`/remove-untracked) PRESERVES it. Makes explicit the protection gitignore previously gave implicitly. (Relates to `self-hosting-inplace-integrity-clean-precondition` — same friction family.)
- **D3 (reworked — PROPOSED): per-instance default binding, NOT universal-on.** The framework provides the run-state-tracking slot (gitignore-is-the-per-project-override); each instance binds its default; **anneal-dev binds default-ON** (run history = methodology provenance); the framework does NOT mandate downstream/product instances to track (they bind their own default — repo-courtesy). Resolves F-D3 (universal-on too strong for downstream; the asymmetry licenses on for corpus repos, not universally).
- **D2 (impl note):** rewrite §5 rule-1's `:362-367` git-observable-marker sentence + rationale (F-contract-marker) so it doesn't self-contradict.
- **F-leakage / F-render-debt-shape:** §5 stays domain-general; file the render-debt row.

### Cycle 3 — design LOCKED (operator confirmed: integrity-fix sound + resume-durability framing; 2026-06-06)
- **D0 [VERIFIED] (scope+, reopened per F-integrity/F-persistence).** Edit sites = the cycle-1 set (`instantiation-guide.md` §5 rule 1 `:359-364` / rule 3 `:371-376` / Project-init `:385-387`; `persistence.md :102-105`; `bindings.md :282`/`:295-296`/`:187-195`) **PLUS the in-place integrity sites**: `core.md §4.2` in-place integrity (clean-before-dispatch + no-other-modification + restore) + `bindings.md :175-180` (in-place HEAD marker + restore) + `persistence.md :55-77` (run-state-spans-containers). basis: F1 + F-integrity-interaction + F-persistence-58.
- **D1 [VERIFIED] (rationale STRENGTHENED; policy unchanged).** Policy: run-state TRACKED; only transient local override-flags gitignored. **WHY (sharpened, operator 2026-06-06):** track run-state as **git-durable, self-describing, resumable checkpoints** (in-flight AND completed), not merely "history" — in-flight commits make resume survive clone/clean/machine-loss; the transparent status header (IN-PROGRESS + phase) makes a committed in-flight tracker unambiguously resumable; consistent with the existing `anneal-checkpoint:` per-unit discipline. This is also the decisive reason track-in-place beats commit-on-completion (the latter throws away in-flight resume-durability). basis: operator + `core.md §4.2` Checkpoint + `persistence.md` resume.
- **D2 [VERIFIED] (unchanged + impl note).** Preserve the edit-permission/distinct-location contract; **rewrite §5 rule-1 `:362-367`** (the "runtime-state paths are gitignored" observable-marker sentence + the blur-the-contract rationale) so it doesn't self-contradict the tracked policy — the observable marker becomes namespace/path (`<plugin>.config/` ∥ `.<plugin>/runs/`, both tracked) + the don't-hand-edit convention. basis: F3 + F-contract-marker.
- **D3 [VERIFIED] (reworked: per-instance default; was [CONDITIONAL]) — THE OPERATOR-SOUNDNESS ITEM at [READY].** The framework provides the run-state-tracking slot (gitignore = the per-project override); **each instance binds its default**; **anneal-dev binds default-ON** (run history = methodology provenance + resume-durability). The framework does NOT mandate downstream/product instances to track — they bind their own default (repo-courtesy: a consumer's product repo shouldn't get a tool's operational logs committed without opt-in). considered: universal default-on (rejected per F-D3 — too strong for downstream; the asymmetry licenses on for corpus repos, not universally; gitignore-toggle works regardless of default direction). basis: F-D3-downstream-courtesy + the asymmetry argument scoped to corpus-repos.
- **D4 [VERIFIED] (reworked: separate-copy provisioning only).** `bindings.md :187-195` — rationale shifts from "run-state gitignored, so not carried by a copy" to "the **uncommitted current** run-state isn't in a base-commit copy"; mechanism unchanged. (The in-place path is now D7.) basis: read `bindings.md :187-195` + `core.md §4.2` separate-copy-at-base-commit.
- **D7 [VERIFIED] (NEW — the integrity reconcile).** The **in-place integrity check + restore EXPLICITLY exclude the run-state dir** (`.anneal-dev/runs/`): clean-before-dispatch (`git status --porcelain`) excludes run-state; "advanced by exactly the unit's change / no other modification" excludes run-state; restore (`reset --hard` + remove-untracked) **PRESERVES** run-state (never discards the live tracker). (b) delta: `core.md §4.2` in-place integrity + `bindings.md :175-180` state the run-state-dir exclusion. (c) acceptance: a dirty/committed in-flight tracker no longer fails clean-before-dispatch nor trips no-other-modification; a restore never wipes run-state. (d) rationale (the SOUNDNESS point, not a workaround): the integrity check guards the **work product** against **unit** contamination; run-state is **orchestrator bookkeeping**, not work-product → correctly out of the check's scope. Gitignore enforced this implicitly; D7 states it. (e) basis: F-integrity-interaction + `core.md :763-772` + `bindings.md :175-180` + the work-product-vs-bookkeeping boundary. Relates to `self-hosting-inplace-integrity-clean-precondition` (composes, doesn't fight).
- **D5 [VERIFIED] (render-follow + render-debt row).** Rendered anneal-dev plugin re-renders the changed policy; **file an actual `instance-reinstantiation` render-debt row** (F-render-debt-shape), not tracker-only. Spec-only this run.
- **D6 [VERIFIED] (invariant check, confirmed).** Touches none of INV-1..5. D7 touches `core.md §4.2` (contamination guard) — INV-3-adjacent (actor/verifier independence) but NOT INV-3 (verify-isolation): a binding-completeness statement, not an invariant breach. Focusing artifact for operator soundness: **D3 (downstream default) + D7 (integrity-exclusion correctness)**; invariant-preservation N/A.
- **F-leakage (impl constraint):** §5 states "transient local override-flags" abstractly; the `allow-adhoc-kernel-edit` concretion lives only in the anneal-dev binding (`bindings.md`), never in domain-general §5.

### Cycle 3 — standardized pass (artifact `.cycle-3.standardized-pass.md`) + fresh-session: PASSED
In-scope lenses clean (see artifact). Fresh-session: D0/D1/D2/D7 located reads (`instantiation-guide.md :359-387`, `persistence.md :55-105`, `bindings.md :175-300`, `core.md :763-772`) — PASSED.

**Cycle 3 status:** D0–D7 [VERIFIED] (D3 = the operator-soundness item at [READY]); standardized pass clean; fresh-session PASSED. → convergence cycle (cycle 4): fresh intent-falsification (attack the reworked D0/D3/D4/D7 + D1 rationale) + mechanical falsification over [VERIFIED] D-entries.

### Cycle 4 — convergence: intent-falsification → D-DELTA (artifact `.cycle-4.intent-falsification.md`; subagent `aff951b0fb877741b`, opus)
One actionable finding + judgment residuals (brake applied to the residuals). Mechanical SKIPPED (intent-delta). Not [READY].
- **F-a [VERIFIED — surfaced → ACTIONED: reopens D4]** D4's separate-copy provisioning rested on "uncommitted" — wrong under tracked run-state (a base-commit copy CARRIES committed run-state); sound discriminant = work-product-vs-bookkeeping (D7's boundary); Integration lacked a run-state exclusion (asymmetric with D7's in-place exclusion). → rework D4.
- **F-b/F-c/F-d/F-e [VERIFIED — surfaced]** resume-precedence (resolved by append-only latest-line), commit-cadence (operational, not a spec mandate — D1 rationale tightened), extensions.md ref (stays true for work-product), D7 detection-gap (parity-preserved). Not actioned (brake — none fails a requirement). Feed operator soundness.
- **D4 [INVALIDATED] → [PENDING].** D0/D1/D2/D3/D5/D6/D7 stand (D1 rationale tightened per F-c).

### Cycle 5 — D4 rework (align with D7's boundary)
- **D4 [VERIFIED] (reworked per F-a; supersedes the cycle-3 line) Separate-copy run-state on the work-product-vs-bookkeeping boundary.** (b) delta: `bindings.md :187-199` — (i) **Provisioning**: the orchestrator provisions the **current** run-state into the copy (the copy's base-commit run-state may be stale committed-prior; the unit needs the current live state) — run-state is **orchestrator bookkeeping, not work-product** (the SAME boundary as D7), not "uncommitted"; replace "not part of the tracked work product … gitignored and so not carried by a copy" with the bookkeeping framing. (ii) **Integration**: "only the unit's **work-product** changes" integrate back; **run-state changes in the copy are NOT integrated** (run-state is the orchestrator's, authored in the orchestrator's context) — the explicit separate-copy parallel to D7's in-place exclusion. (c) acceptance: the separate-copy path treats run-state as bookkeeping (provisioned current, excluded from integration), consistent with D7's in-place exclusion; no reliance on "uncommitted" or "gitignored." (e) basis: F-a + `bindings.md :187-199` + D7's work-product-vs-bookkeeping boundary. considered: keep "uncommitted" discriminant (rejected per F-a — false for a base-commit copy carrying committed run-state).
- **D1 [VERIFIED] (rationale tightened per F-c).** "git-durable resumable checkpoints" = run-state is **committable** as durable resume points; durability is realized at commit points (the existing `anneal-checkpoint:` discipline + operator commits), NOT a newly-mandated commit cadence. basis: F-c.

### Cycle 5 — standardized pass (artifact `.cycle-5.standardized-pass.md`) + fresh-session: PASSED
In-scope lenses clean (D4-rework is a reconcile, no net add; the run-state exclusion is now stated symmetrically in-place (D7) + separate-copy (D4)). Fresh-session: D4 `bindings.md :187-199` (located read) PASSED.

**Cycle 5 status:** D0–D7 [VERIFIED] (D4 reworked, now consistent with D7; D3 = operator-soundness item); standardized pass clean; fresh-session PASSED. → convergence cycle (cycle 6): fresh intent-falsification + mechanical falsification over [VERIFIED] D-entries.

### Cycle 6 — convergence cycle: CLEAN → [READY] (artifacts `.cycle-6.intent-falsification.md` + `.cycle-6.falsification.md`)
- **Intent-falsification CLEAN** (subagent `aad9560d8da0d0684`, opus): no D-delta; one surfaced residual (F-prov-framing — 4 framework sites use "non-tracked," now mildly misleading but the rule stays true; [VERIFIED — surfaced], not actioned per the brake).
- **Mechanical falsification CLEAN** (subagent `af9b1c3c3afb6c3d3`, opus; orchestrator-computed): all 8 [VERIFIED] D-entries (D0–D7) HOLD. D6 confirmed: no foundation invariant breached.
- **ZERO D-track delta → convergence observed clean → [READY].** Impl plan: `.impl-plan.md` (5 units, parallel-eligible by distinct files; working-context implementation per self-hosting).
- **Header → [READY]; awaiting operator `next phase` = the irreducible kernel-edit operator-soundness verdict.** Soundness-focus (per the D6 focusing artifact): **D3** (per-instance default — anneal-dev on, framework doesn't mandate downstream) + **D7** (integrity-check excludes run-state — work-product-vs-bookkeeping boundary). Invariant-preservation N/A (D6).