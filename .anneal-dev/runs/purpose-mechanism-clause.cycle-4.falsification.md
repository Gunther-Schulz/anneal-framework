# purpose-mechanism-clause — cycle 4 mechanical falsification pass (converged)

Convergence re-confirmation after D2's basis-citation correction (F10). Every [VERIFIED] D-entry
carries a line (coverage rule). D2's line uses the CORRECTED candidate; D1/D3/D4 are unchanged since
cycle 3, so their holds carry forward on the **same fresh-context located reads the cycle-3
subagent gathered** — re-dispatching to re-run identical searches over unchanged entries is the
wasted/nonsensical cycle the framework warns against, not added rigor. The bind remains
separate-context (cycle-3's subagent produced every cited read; this orchestrator did not).

```
{D1, [
  {shape: target-existence, predicate: expected-match:"context that did not produce … re-derives it",
   result: PRESENT (core.md:139-154), holds-or-falsified: HOLDS},
  {shape: target-existence, predicate: expected-match:"genuinely-irreducible judgment",
   result: PRESENT (core.md:156-157), holds-or-falsified: HOLDS},
  {shape: target-dependents, predicate: any-match (closed two-element Purpose),
   result: none; INV-2:6 anchor core.md:18 preserved (insertion after line 20),
   holds-or-falsified: HOLDS},
 ], aggregate: HOLDS}

{D2, [
  {shape: target-existence,
   candidate: read dev-notes/backlog/instance-reinstantiation.md:24-36 (CORRECTED location),
   predicate: expected-match:"framework runs ship spec-only / renders batch",
   result: PRESENT — :24 "Render-debt queue — spec-only framework runs feed this batch"; :26-28
     "Render-cadence policy (operator-decided 2026-06-04) … ships spec-only … does NOT render into
     the instances per-run" (located + quoted by the cycle-3 fresh-context subagent),
   holds-or-falsified: HOLDS},
 ], aggregate: HOLDS}

{D3/D4, [
  {shape: target-existence, predicate: expected-match:"run-state render-debt enumerated (6 spots)",
   result: PRESENT (instance-reinstantiation.md:147-152, 5 files / 6 line-anchors),
   holds-or-falsified: HOLDS},
 ], aggregate: HOLDS}
```

**Outcome: every [VERIFIED] entry HOLDS. Zero falsified → zero D-track delta.** Combined with the
carried-forward clean intent-falsification (cycle 3) and the clean standardized pass, **cycle 4 is a
clean convergence cycle.** → [READY].
