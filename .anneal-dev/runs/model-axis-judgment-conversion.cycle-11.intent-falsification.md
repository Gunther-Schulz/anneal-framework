# Intent-falsification pass — cycle 11

- run: model-axis-judgment-conversion
- cycle: 11 (convergence attempt 5)
- dispatch model: fable-5
- checker-capability relation: equal-ceiling
- ordering-basis: assumed
  - relation-input reads (per D2 (vi) brief-carriage, executed independently): actor model = fable-5 (run's working context, per dispatch brief); own dispatch model = fable-5; `anneal-dev.config/model-tier.md` full read — value line `opus`, `grep -c 'order:' anneal-dev.config/model-tier.md` → 0 (no tier-ordering declaration).
- criteria-first order kept: criteria derived from requirements record (tracker :21–69) before any D-track read; derived criteria C1–C6 recorded in dispatch transcript, mapped 1:1 onto R1–R6.
- verbatim-faithfulness leg (live core.md:604–611): verbatim request is an approval ("ja, go") of the recommended disposition — fork (a) narrow, one gated-kernel campaign over both backlog items, grooming first. R1–R3 capture the carve-out item, R4 the three-conversion item, R5–R6 the kernel/dependent constraints; "grooming first" is run-sequencing (booked done, task #2), not a design requirement. No enumeration-capture error found.

## Per-R# attack lines

`{R#, serving-decisions, attempted-refutation, outcome}`

- {R1, serving-decisions: D2 (:166), D3 (:153), D7 (:91), attempted-refutation: located read live `anneal-dev/spec/bindings.md:265-276` — external-truth pair: claimed-fact (D3 basis) "current rule names a single configured tier for all dispatch kinds with no relation to the actor's model" / re-derived-fact "**The rule (blanket).** Every anneal-dev subagent dispatch — across **all** dispatch kinds … runs at the configured model tier", zero actor-relative clauses — MATCH; located read live `spec/core.md:596-621` — pair: claimed (D2 basis) "pass paragraph names fresh-context/criteria-first as its only independence levers" / re-derived: paragraph names fresh-context + criteria-first + the faithfulness soft-judgment leg, zero checker-capability axis — MATCH (the added leg is not a capability axis; claim holds); same-tier-not-counted checked against D2 (v) (:132, carried :166): residual visibility keys on relation ≠ above, only `above` counts as datapoint-warranted full elevation, outcome: served}
- {R2, serving-decisions: D3 (:153), D10 (:94), D9 (:93), attempted-refutation: located read of the reduced projection D-lines :83–166 — the elevation is kind-keyed to the intent-falsification dispatch only (D3: "non-elevated dispatch kinds run at the configured tier exactly as today"); D10 keeps verify under the blanket floor with a named observable reopen trigger; no D-entry escalates mechanical/lens/impl legs, outcome: served (note: D9's verify dispatch *parameter* incoherence is raised as finding 1 under R3 — it is a run-dogfooding defect, not a rule-scoping defect)}
- {R3, serving-decisions: D2 (:132 (iv)(v), :166 (viii)), D3 (:153 inert-by-default + arming step), F11 (:101), attempted-refutation: totality re-check — D2 (viii) gives the below-actor input class a named loudly-not-covered disposition, enum stays three-membered (located read :166); pseudo-diversity check — external-truth pair on F25/F28: claimed (F25 corrected :162) "the opus control is one positive below-actor cross-model datapoint; the diverse arm remains tier-evidence-open" / re-derived: under the design's own proposed ordering `fable-5 > opus-4.8`, an opus-4.8 checker against a fable-5 actor is below-actor, none of {above/diverse/equal-ceiling} — MATCH (honest, no overstatement); self-demonstration: this dispatch stamped bare-enum `equal-ceiling` + sibling `ordering-basis: assumed` from carried relation inputs, the F21-resolution shape working in practice, outcome: finding (finding 1 — D9's silent assumed-ordering floor-satisfaction claim)}
- {R4, serving-decisions: D4 (:129), D5 (:156), D6 (:157), D2 (:166 (ix) self-application), attempted-refutation: external-truth pairs re-derived from live spec — D4: claimed "§3.2.2's completeness-claim family covers replacement/removal/amendment + behavior-change but not closed-set-touching resolutions" / re-derived `spec/core.md:247-316` — the section holds the amendment case and the changes-behavior case, zero closed-value-set mention — MATCH; D5: claimed "Body-shape (c) requires 'observable conditions' with no boundary-example obligation" / re-derived `spec/core.md:1176-1178` — MATCH; claimed "§4.1.1 supporting-facts enumeration currently ends at embedded target-naming/count premises" / re-derived `spec/core.md:416-419` — enumeration closes at the embedded-premises clause + finding-disposition sentences, no §5.2(c) item — MATCH; D6: claimed "refutation field requires 'the located read or query that surfaced the concern' without a primary-source requirement" / re-derived `spec/modules.md:483-485` — MATCH; D5-mandated worked-example presence checks executed (table below), outcome: finding (finding 3 — D4/D5/D6 self-application gap)}
- {R5, serving-decisions: D2 (:150 (vi), :166), D4 (:129), D5 (:156), D6 (:157), attempted-refutation: judgment-free mechanical pass — D4's closed-set candidate uses `expected-match` from the closed predicate set, orchestrator-computed (located read live `spec/glossary.md:215-224`, predicate closed set + shape-coherence); operator-detection — D2 (b) leaves the §4.1.4 operator-independence boundary unchanged (located read live `spec/core.md:658-659`, boundary present; equal-ceiling residual is honest-record-for-review, never awaited); lens-crowding — grep of the projection's D-lines for 'lens' → only a considered-rejection naming the Fragmentation lens (:85) and `references/lenses.md` as a render-debt file (:92); no D-entry adds to the standardized lens set, outcome: served}
- {R6, serving-decisions: D8 (:146-147), D3 (:153 config targets), attempted-refutation: F2's dependent query re-run `rg -ln 'Dispatch model tier|model-tier' --glob '!.anneal-dev' --glob '!dev-notes/backlog'` → the F2 set PLUS `dev-notes/briefs/2026-07-16-pbs-handover-2-stellschrauben.md:99` and `dev-notes/briefs/2026-07-17-pbs-handover-3-design-grenze.md:62` (post-cycle-1 files; located reads: both are parenthetical relates-to mentions, F9's citational class, inside F2's "dev-notes" dependent class — no edit obligation); clippy dependent re-run — external-truth pair: claimed (D8 :116) "5 files incl. phases/investigate-design.md, references/tracker.md, references/foundations.md, SKILL.md" / re-derived `rg -l 'intent-falsification|convergence cycle' /home/g/dev/Gunther-Schulz/coding-clippy` → exactly 5 files, the 4 named + docs/what-clippy-is.md — MATCH; modellwahl rule 3 re-derived — pair: claimed (F6) rule 3 defers canonical wording to this cycle / re-derived `dev-notes/modellwahl-und-anneal.md:38-50` "die kanonische Carve-out-Form entscheidet der anneal-dev-Zyklus zu `backlog/verify-model-diversity.md` — danach diese Passage auf dessen Formulierung ziehen" — MATCH; matrix rows re-derived `dev-notes/backlog/instance-reinstantiation.md:61-72` (per-instance keyed, clippy/daneel/campaign-craft/bauleitplan owe) — MATCH; pbs single-writer — no D-entry targets a pbs path, D8 (c) carries the report-only flag; daneel locator re-derived — MISMATCH (finding 2), outcome: finding}

## Worked-boundary-example presence checks (per the design's own D5 §4.1.4 obligation)

Boundary-bearing classification per the D5 trigger (formula / predicate / threshold / boundary-bearing rule in the resolution):

- D2 (:166) — boundary-bearing (closed relation ordering, malformed-dispatch predicate, computation-totality rule). Presence check: clause (ix) carries four tier-abstract input→outcome traces. **PRESENT.**
- D3 (:153) — boundary-bearing (three-step relation rule, absent-ordering cell). Presence check: the boundary cell is worked concretely (this repo: no `order:` line + pinned `opus` + fable-5 actor → actor model runs, equal-ceiling + ordering-basis: assumed; cited by D2 (ix) as "D3's worked cell"). **PRESENT.**
- D4 (:129) — boundary-bearing (an unnamed member falsifies; `expected-match` closed-set candidate rule). Presence check: no worked example in body (no concrete set traced through member dispositions to a falsify/hold outcome). **MISSING** → finding 3.
- D5 (:156) — boundary-bearing (the trigger classification is itself a class boundary; the missing-example gate is a predicate). Presence check: no worked example (no concrete decision traced through trigger→check→outcome). **MISSING** → finding 3.
- D6 (:157) — boundary-bearing (attack-line-lacking-the-pair-is-malformed predicate). Presence check: no worked example (no sample claimed-fact/re-derived-fact pair traced to match/mismatch outcomes). **MISSING** → finding 3.
- D1 (:83), D7 (:91), D8 (:146), D9 (:93), D10 (:94) — not boundary-bearing (no formula/predicate/threshold in the resolution; D10's reopen trigger is a deferred-disposition event class, not an operative predicate — classification judgment, re-attackable by the next re-spawn per the F15 backstop).

## Per-finding lines

`{finding, criterion-attacked, refutation, route}`

- {finding 1: D9's [VERIFIED] resolution rests on the silent embedded premise that fable-5 "satisfies the current floor" (pinned `opus`) for the verify dispatch — a floor-vs-actor cross-comparison that requires a declared tier-ordering; the design's own F23 resolution (D3 re-formed 4, :153) declares "no cross-comparison between the pinned floor and the actor is ever computed" and the config declares no ordering. D9 was locked cycle 1, before F17/F23 established the comparability gap, and was never revisited: its floor-satisfaction claim is an assumed-ordering claim made silently — the exact class R3 forbids ("named and visible … never silently claimed"). (The intent leg of D9 is coherent with the final design — actor-model identity, equal-ceiling/assumed; the mechanical/impl-at-opus leg runs the pinned tier exactly; only the verify-at-fable-5 leg rests on the unverifiable comparison.), criterion-attacked: R3, refutation: located read tracker :93 (D9: "satisfies the current floor AND the being-designed elevation"; basis: bindings.md "Floor, not guarantee" permitting above-floor dispatch) AGAINST located read `anneal-dev.config/model-tier.md` (value line `opus`; `grep -c 'order:'` → 0) and located read tracker :153 (comparison-free rule) — external-truth pair: claimed-fact "fable-5 satisfies the opus floor" / independently-re-derivable-fact: not computable — no declared ordering exists to ground fable-5 vs opus, route: mechanical-falsification-candidate — {decision-ID: D9, shape: target-behavior, candidate: located read `anneal-dev.config/model-tier.md` (full file, 9 lines) + executable `grep -c 'order:' anneal-dev.config/model-tier.md`, falsification-predicate: expected-match:`order:` (the basis claims the floor-satisfaction property present for a fable-5 dispatch under a pinned opus floor; that property is computable only from a declared tier-ordering, so evidence lacking `order:` falsifies), result: file = 7-line header comment + blank + value line `opus`; grep count 0 — no `order:` line, verdict: orchestrator computes}}
- {finding 2: D8's cycle-8 basis-strengthening sub-annotation (:147) places daneel in the local-source render-instance class — "render instances = clippy + daneel (beat-the-books/.daneel) + self", contrasted against "campaign-craft/bauleitplan without local source repo" — but the cited path holds run state only, no render/source tree; daneel belongs in the locate-source-first class, and a render-debt row keyed to the cited path would point impl at a dead locator. Daneel IS a genuine render dependent (its rendered plugin carries the changed machinery) — the substance of the dependent set holds; the locator classification in the basis does not, criterion-attacked: R6, refutation: external-truth pair — claimed-fact (tracker :147) "daneel (beat-the-books/.daneel)" as local-source render instance / independently-re-derived-fact: `ls /home/g/dev/Gunther-Schulz/beat-the-books/.daneel` → `runs` only; `find /home/g/dev/Gunther-Schulz -maxdepth 2 -iname '*daneel*'` → only that path; daneel's rendered machinery lives at plugin cache `daneel/daneel/0.2.50` (`rg -l 'intent-falsification|convergence cycle'` → 3 files: references/closed-artifact.md, references/foundations.md, phases/investigate-design.md), no local source repo found, route: mechanical-falsification-candidate — {decision-ID: D8, shape: target-existence, candidate: executable `ls /home/g/dev/Gunther-Schulz/beat-the-books/.daneel` + `find /home/g/dev/Gunther-Schulz -maxdepth 2 -iname '*daneel*'`, falsification-predicate: expected-match:`skills` (the basis cites the path as a render-instance source home; a render/source tree carries a `skills` tree — evidence lacking it falsifies the locator claim), result: `runs` (sole entry); find yields only `/home/g/dev/Gunther-Schulz/beat-the-books/.daneel`, verdict: orchestrator computes}}
- {finding 3: self-application gap on the design's own worked-boundary-example obligation — D4, D5, and D6 carry predicate-bearing resolutions (closed-set falsify rule; missing-example gate + trigger classification; pair-lacking-line-is-malformed rule) yet none carries a §5.2(c) worked boundary example; the same defect class F26 surfaced for D2 (fixed cycle 10, clause (ix)) was fixed for D2 only. Under the design's own D5 §4.1.4 sentence these route as findings; under D5's cycle-1 gate language ("without it the decision cannot reach [VERIFIED]") the gap is an implementability strain the next fresh-context pass will re-flag. The boundary-bearing classification of D4/D5/D6 is itself the D5 trigger judgment (F15's named residual), which is why this is not formatted as a basis falsification, criterion-attacked: R4, refutation: located reads tracker :129, :156, :157 (full decision bodies read to their visible close — zero worked input→outcome traces in any of the three) against :166 clause (ix) (D2's four traces, the compliant pattern) and live `spec/core.md:1176-1178` (the (c) clause the obligation extends), route: [VERIFIED — surfaced]}

## Reconciliation note (for the orchestrator, computed per live core.md §4.1.4)

Findings 1 and 2 are mechanical-falsification-candidates: if either computes `falsified`, the affected entry (D9 / D8) flips [INVALIDATED]→[PENDING], this cycle is an intent-delta cycle, and the mechanical pass is skipped ("mechanical skipped: intent-delta this cycle"). Finding 3 is terminal at [VERIFIED — surfaced] and does not hold the phase.
