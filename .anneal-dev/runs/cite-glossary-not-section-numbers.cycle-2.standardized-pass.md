# Standardized-pass — cycle 2

This cycle locked D1–D5 to [VERIFIED] (scope search-established; the firewall rule;
the 2-headword interface completion; enforcement check; README reconciliation) and
recorded D6 [CONDITIONAL] (file binding-table completeness). F1–F7 resolved
[VERIFIED — addressed]. Realization rule-text still deferred to impl. In-scope lenses:

- **Bloat** — CLEAN. D3 adds exactly 2 glossary headwords + D2 the rule; each
  load-bearing — the 2 headwords close real §-cite gaps (F6: evidence-bearing-rule,
  post-run review, each only in prose today), the rule closes F1. No restatement/hedge.
  Model §1 deliberately NOT added (re-points to existing Operator headword) — avoids a
  redundant entry. — basis: D3, F6 (glossary:432, 268).
- **Fragmentation** — CLEAN. The rule is stated in no other corpus location (F1). The
  2 new headwords consolidate rather than duplicate: "evidence-bearing-artifact rule"
  promotes prose (glossary:432) to one canonical headword; "post-run review" has no
  existing definition (only its output classifications are defined, glossary:426). —
  basis: F1; glossary:432, 426.
- **Leakage** — CLEAN (N/A by content). Edits land in domain-general files
  (glossary.md, instantiation-guide.md, README.md); the rule + both headwords are
  domain-general (no language/tool/path concretion). — basis: Leakage Scope; D2/D3.
- **Missed-dependents** — CLEAN. D2/D3 change the glossary + add a rule; dependents:
  (a) the §-citing instance spots = the re-point targets, enumerated complete (F2),
  handled in Phase B per D1; (b) spec/README "definitions-only" (README:77-81) =
  reconciled by D5; (c) the binding-table non-glossary terms = D6. No un-handled
  dependent. — basis: D1, D5, D6; F2.
- **Unenforced-rule** — CLEAN (resolved). D2 now carries D4's mechanical grep-check
  (instance files = zero framework §-cites) — the evidence-bearing artifact in which
  violation is visible. — basis: D4.
- **Undefined-shorthand** — CLEAN. "instance-facing interface" gets its definition at
  D5's home (README); the 2 new glossary headwords ARE the definitions for the terms
  previously used only in prose. — basis: D5, D3.
- **Over-claimed-verification** — CLEAN. F6's GAP claims (Model→Operator, the 2 real
  gaps, the 4 non-headword binding terms) were spot-verified by orchestrator located
  reads + greps, NOT left resting on the secondary-source subagent report. — basis:
  F6 (glossary:258/268/432/426 located reads + 4 `rg` negatives).

Out of scope this cycle (no plugin render committed — re-renders are Phase B):
**Lossy-render** (no design decision targets a rendered plugin clause this run).
