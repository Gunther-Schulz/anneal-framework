# anneal-dev run: v-entry-is-post-ship-only

## Header
- Status: PASSED (2026-06-06) — resumed from PAUSED; operator chose β; D2 reworked cycles 3→5→7→9 (4/6/8 convergence caught real defects; operator flagged overthinking → minimal design); D4 dropped at implement (F17, redundant); implement (3 units) + isolated verify [PASSED]. Shipped: README WATCHING re-anchor + exclusion + Vocab→pointer (D2), CLAUDE.md filing-shape (D3), D6 sweep item filed. D4 not shipped.
- Phase: verify (terminal — [PASSED])
- Mode: interactive + run-until-(n) cadence. NOT method-kernel (edits `development-process.md` practice 8 + `dev-notes/validation-watch/README.md` + `CLAUDE.md` filing-shape — no `spec/*`/`foundation.md`/`anneal-dev/spec/*`) → verify = skill-craft FORM review + cross-doc coherence; NO irreducible operator-soundness leg; foundation-invariant check N/A. `CLAUDE.md` edit needs operator commit-approval.
- Task: codify V-entries = post-ship effect-watches only; backlog items = un-implemented considerations. Resolve the README WATCHING-no-fix ↔ practice-8 contradiction.

### Requirements record
**Operator verbatim:** "V- only trigger on self review so it in my mind should only be used to tracking IF a change from a backlog item holds/has an effect. the backlog item exists to implement so we can actually test in action. backlog items get filed when I/we notice something that could be improved, a gap so it needs to be implemented to test it." + "make this clear in our documentation if its current intent is off."
**Goals (R#):**
- R1 — a not-yet-implemented consideration (a noticed gap) does not park as a (lost) V-entry; it stays a visible backlog item.
- R2 — a V-entry's role (post-ship effect-watch on a shipped fix/choice) is unambiguous across the docs (README + practice 8 + CLAUDE.md cross-confirm, no contradiction).
- R3 — the existing register content stays correctly classified (shipped-choice-watches remain V-entries; only genuine un-implemented-consideration parking, if any, relocates).
**Proposed solution (operator decide-ahead):** re-scope README WATCHING + sharpen CLAUDE.md filing-shape + cross-confirm practice 8; migration audit of the ~31 entries.

## F-track
### Cycle 1 — investigation pass
- F1 [PENDING] (→R2) The contradiction: `development-process.md` practice 8 (`:296-308`) — "the V-N entry, if written, records the observation that **produced the fix**, not the deferral of the fix" (V is post-ship) — vs `dev-notes/validation-watch/README.md` WATCHING state (`:42-46`) — "uncertainty exists; **no structural fix shipped yet**" (admits a pre-fix parking). basis: read both ranges this session.
- F2 [PENDING] (→R3) AUDIT: 22 of 28 active V-entries are WATCHING. Spot-audit (V-1 lens-timing, V-26 enum-completeness, V-27 render-ceremony, V-10 d-track-delta-convergence): each watches a **shipped spec-choice's** correctness — the register's STATED purpose (README intro `:3-7`: "Some [spec decisions] were made best-effort, under genuine uncertainty. This doc records those"). So WATCHING-in-practice = watching-a-shipped-choice, NOT parking-un-implemented-considerations. basis: `ls` + Status grep (22 WATCHING / 6 FIX-SHIPPED) + README intro read.
- F3 [PENDING] (→R1, the refinement) The "drift" is narrower than this run's seed item assumed: the README *wording* is loose (admits the un-implemented-consideration reading) but the existing entries don't abuse it. The genuine non-conformant shape is **this session's D2** (basis-query: a noticed gap, decided-not-to-build, nothing shipped → correctly kept as backlog). → the fix is **preventive wording** (block the future D2-shape), NOT a mass-migration. **V-26/V-27 reclassified: conformant** (watch shipped choices), NOT borderline-relocate (corrects the cycle-2/earlier flag). basis: F2 + the campaign-③ D2 disposition.
- F4 [PENDING] (→R2, boundary) "post-ship" must include a shipped spec-CHOICE, not only a fix-from-a-backlog-item: the register's purpose IS watching uncertain shipped choices (F2); the strict "fix-from-backlog-only" reading would orphan ~22 conformant entries. basis: README intro `:3-7` + F2.

## D-track
### Cycle 1 — design
- **D0 [PENDING] Scope.** Edit sites: `dev-notes/validation-watch/README.md` (WATCHING re-scope), `CLAUDE.md` (filing-shape V-entry/backlog lines), `development-process.md` practice 8 (cross-confirm sentence). Migration audit (per-entry conformance check of the ~31) → **bounded follow-on** (F2/F3: existing entries spot-audited conformant; the fix is preventive, so a full per-entry sweep is separable, not blocking). basis: TBD search this cycle.
- **D1 [CONDITIONAL] (R2 boundary — operator-resolvable) "post-ship" ⊇ shipped spec-choice.** A V-entry watches a shipped fix OR a shipped spec-choice's effect; it does NOT hold an un-implemented consideration. Recommendation: this reading (the register's stated purpose; keeps the 22 conformant). Operator may narrow to fix-from-backlog-only (would orphan ~22 — not recommended). assumption: the operator's "change from a backlog item" was the common-case description, not a redefinition excluding original spec-choices.
- **D2 [OUTLINED] (R1/R2) Re-scope README WATCHING.** WATCHING = watching a shipped fix/choice's effect (per D1); EXCLUDE the un-implemented-consideration-parking shape (a noticed gap, decided-not-to-build → backlog item). Keep RESOLVED/INVALIDATED unchanged. A WATCHING entry that fires → spawns a backlog item.
- **D3 [OUTLINED] (R2) Sharpen CLAUDE.md filing-shape.** V-entry line = post-ship effect-watch on a shipped fix/choice, never holds an un-implemented change, fires → spawns backlog; backlog line = the home of un-implemented noticed-gaps.
- **D4 [OUTLINED] (R2) Practice-8 cross-confirm sentence.** Add the explicit "V is post-ship; a pre-ship gap is a backlog item" so the three docs cross-confirm rather than contradict.
- **D5 [OUTLINED] (R3) Migration disposition.** Spot-audit (F2) found the existing WATCHING entries conformant (shipped-choice-watches); V-26/V-27 conformant. Full per-entry audit = bounded follow-on (file it); no mass relocation expected. Non-conformant shape = D2-this-session (caught preventively).

### Cycle 1 — design (scope search-established + operator boundary confirm)
- F5 [VERIFIED — addressed by D0] Dependent enumeration of the WATCHING-lifecycle re-scope: home = `dev-notes/validation-watch/README.md` (WATCHING def `:42-46` + Vocabulary `:128`); `development-process.md` practice 8 (D4); `CLAUDE.md` filing-shape (D3); **`post-run-review.md` Q7** (`:165-193`) reads V-entry status but its WATCHING-walk ("walk the production signal against this run's observations") is **definition-agnostic** → **no-edit dependent** (the walk is unaffected by tightening what can BE a WATCHING entry; confirm at verify). The V-entries' own Status lines unchanged (they stay WATCHING as shipped-choice-watches). basis: `rg "WATCHING"` across dev-process/post-run-review/spec/CLAUDE.md (matches partition into {README home} ∪ {practice 8} ∪ {post-run-review Q7 — definition-agnostic} ∪ {CLAUDE.md filing-shape}).
- **D0 [VERIFIED] Scope.** Three edit sites: `dev-notes/validation-watch/README.md` (WATCHING def + Vocabulary entry), `CLAUDE.md` (filing-shape V-entry/backlog lines), `development-process.md` practice 8 (cross-confirm sentence). Dependents no-edit: `post-run-review.md` Q7 (F5, definition-agnostic), the V-entries (Status unchanged). Migration audit → bounded follow-on. basis: F5 search.
- **D1 [VERIFIED] (R2 boundary — operator-confirmed 2026-06-05) "post-ship" ⊇ shipped spec-choice.** A V-entry watches a shipped fix OR spec-choice's effect; never holds an un-implemented consideration. Operator confirmed ("yes") the boundary + migration-as-follow-on. basis: operator free-form confirm + the register's stated purpose (README intro `:3-7`).
- **D2 [VERIFIED] (R1/R2) Re-scope README WATCHING.** (a) target: `validation-watch/README.md` WATCHING state (`:42-46`) + the Vocabulary WATCHING line (`:128`). (b) delta: WATCHING = "watching a **shipped** fix-or-choice's effect for whether it holds" — replace "uncertainty exists; no structural fix shipped yet" with the shipped-thing framing; **add the exclusion**: a noticed gap with no shipped change (decided-not-to-build / not-yet-implemented) is a **backlog item, not a WATCHING entry**; a firing WATCHING entry **spawns a backlog item** for the change. RESOLVED/INVALIDATED transitions unchanged. (c) acceptance: the README WATCHING text names the shipped-thing-watch + the backlog-exclusion; no contradiction with practice 8. (e) basis: F1/F2/F3 + D1.
- **D3 [VERIFIED] (R2) Sharpen CLAUDE.md filing-shape.** (b) delta: V-entry line → "post-ship effect-watch on a shipped fix/choice; never holds an un-implemented change; fires → spawns backlog"; backlog-item line → reaffirm "the home of un-implemented noticed-gaps (filed to be implemented-and-tested)". (e) basis: F3 + the live filing-shape block in CLAUDE.md (read this session). Operator-instructions edit → needs commit-approval.
- **D4 [VERIFIED] (R2) Practice-8 cross-confirm.** (b) delta: add to `development-process.md` practice 8 (the validation-watch paragraph) the explicit "a V-entry is **post-ship** — it watches a shipped fix/choice; a not-yet-implemented gap is a backlog item, not a V-entry" so the three docs cross-confirm. (e) basis: F1 (practice 8 already aligned; this makes it explicit + cross-referencing).
- **D5 [VERIFIED] (R3) Migration = bounded follow-on; no mass relocation.** (b): the existing 22 WATCHING entries are conformant shipped-choice-watches (F2 spot-audit); the full per-entry conformance sweep is a filed follow-on, not this run. No entry relocated this run. (e) basis: F2/F3.

### Cycle 1 — standardized inspection pass (artifact: `.cycle-1.standardized-pass.md`)
In-scope lenses (edits to 3 dev-process/dev-notes/instructions docs; NOT spec/skill renders) — all clean:
- **Bloat** — clean: D2/D3/D4 each add load-bearing clauses (the exclusion + the post-ship framing); no restatement. basis: D2/D3/D4.
- **Fragmentation** — the THREE docs state the V-vs-backlog principle — but by design as cross-confirming (one canonical: the README lifecycle; practice 8 + CLAUDE.md cross-reference it, not duplicate). The whole point is removing a CONTRADICTION (drift), replacing it with cross-confirmation. basis: F1 + D2/D3/D4 (README canonical, others reference).
- **Unenforced-rule** — the filing-shape is a discipline (judgment: is this an un-implemented gap or a shipped-thing-watch?), not mechanizable; the docs state it as the discipline + the operator/self-review backstop. Appropriate (filing shape resists mechanization). basis: D3.
- **Undefined-shorthand** — no new coined term; "post-ship effect-watch" is descriptive. basis: D2/D3.
- **Leakage / Lossy-render / Over-claimed / Missed-dependents** — N/A or clean: no domain-general spec edit (Leakage N/A); no render/paraphrase (Lossy-render N/A); D0 enumerated dependents (Missed-dependents clean, F5).

### Cycle 1 — fresh-session implementability: PASSED
- D2: `validation-watch/README.md` `:42-46` + `:128` (located reads). PASSED.
- D3: `CLAUDE.md` filing-shape block (read this session). PASSED.
- D4: `development-process.md` practice 8 validation-watch paragraph (`:296-308`, read this session). PASSED.

**Cycle 1 status:** design complete (D0-D5 [VERIFIED]; boundary operator-confirmed); standardized pass clean; fresh-session PASSED. → convergence cycle (light, non-kernel): new-surface investigation + intent-falsification (serves R1/R2/R3?) + mechanical falsification (D-entry doc-read bases).

### Cycle 2 — convergence intent-falsification → D-DELTA (artifact `.cycle-2.convergence.md`; subagent `a8d5c95cac5323cb2`, opus)
- F6 [VERIFIED — surfaced → ACTIONED: reopens D2] **F-A: D2 is under-specified — re-scoping WATCHING to post-ship COLLAPSES the WATCHING↔FIX-SHIPPED distinction.** The README lifecycle is a two-state pre/post-ship machine: WATCHING (pre-fix) → FIX-SHIPPED (post-fix), wired through the four-transition table (`:79-82`), the FIX-SHIPPED-is-birth def (`:47-58`), the n=1 "ship the fix → transition to FIX-SHIPPED" clause (`:44-45`), and post-run-review Q7's WATCHING-walk (`:169`). D2 ("re-scope the WATCHING blurb; RESOLVED/INVALIDATED unchanged") leaves WATCHING→FIX-SHIPPED incoherent under the new def → a NEW README-internal contradiction → fails R2. NOT a coupling-shape falsification (D2's doc-read basis at :42-46/:128 is accurate) → pure judgment, but ACTIONED as a D-delta (the design's scope is wrong). basis: subagent `a8d5c95cac5323cb2` — read README `:26-99` (full lifecycle) + `:44-45` (n=1 ship-transition) + post-run-review Q7 `:163-195`.
- F7 [VERIFIED — surfaced] **F-B: filing-discipline gap** — D5/D0 claim the migration audit is "a filed follow-on" but no such backlog item exists (only the seed item, which says "part of this cycle"). Per CLAUDE.md no-silent-deferral, an unfiled follow-on is invisible. → close at implement (file the per-entry-sweep follow-on, OR fold into the seed item's disposition). basis: `ls dev-notes/backlog/` (only `v-entry-is-post-ship-only.md`) + seed item `:18`.
- **R3 confirmed SOUND** (subagent sampled V-24/26/27/28/30/31 — all watch shipped fixes/choices; none is un-implemented-consideration parking; no over-tightening). D5's "22 conformant" holds; the V-26/V-27 relocate-flag correctly reversed (F3).
- **D2 [INVALIDATED] → [PENDING]** (per F-A); D0 (dependent enumeration) + D5 (migration) ride the reopen. D1/D3/D4 UNAFFECTED (their bases sound: D3 CLAUDE.md filing-shape `:102-105` exists; D4 practice-8 `:296-308` exists; D1 README intro `:3-7`).
- **Mechanical falsification pass SKIPPED: intent-delta this cycle** (per skip carve-out).

### The α/β lifecycle-resolution fork (D2 rework — operator-load-bearing; PAUSED here)
The re-scope cannot stop at the WATCHING blurb; the WATCHING/FIX-SHIPPED boundary must be resolved under the post-ship definition:
- **α (collapse — subagent's lean):** merge WATCHING + FIX-SHIPPED into ONE post-ship watch state (both watch a shipped thing); fold away the WATCHING→FIX-SHIPPED transition; re-derive the transition table around {post-ship-watching → RESOLVED / INVALIDATED}; relabel the 22 entries; touch Q7. Conceptually cleanest; biggest blast radius.
- **β (re-frame + preserve — orchestrator's lean):** KEEP both states; re-frame WATCHING = "watching a shipped *choice* (an original design decision under uncertainty; no remedial fix yet)" — correcting "no structural fix shipped yet" (pre-fix) to "no *remedial* fix yet (the choice itself is shipped)"; FIX-SHIPPED = "watching a shipped *remedial fix*"; the WATCHING→FIX-SHIPPED transition = "a remedial fix shipped for the watched choice." Add the un-implemented-consideration exclusion (→ backlog) to BOTH. Preserves the two-state machine + the 22 labels + Q7; lighter blast radius; resolves R2 (both states are post-ship → no contradiction with practice 8). **Recommended** — Pareto-better (preserves more, resolves the contradiction, matches how the 22 entries are already used).
- Either α or β makes D2 a real lifecycle edit (not a blurb-swap) → re-converge after the rework.

### Cycle 3 — D2 rework per operator β decision (re-frame + preserve; 2026-06-06)
Operator selected **β** (the orchestrator's recommended lean) at the α/β fork. Header phase stays investigate-design.
- **D0 [VERIFIED] (re-confirmed under β) Scope.** Edit sites unchanged at file level: `dev-notes/validation-watch/README.md` · `CLAUDE.md` filing-shape · `development-process.md` practice 8. Under β the README edit grows from {WATCHING blurb `:42-45`, Vocab WATCHING `:127-134`} to {lifecycle-preamble exclusion `:26-39`, WATCHING def `:42-45`, FIX-SHIPPED axis-sharpen `:47-58`, Vocab WATCHING `:127-134`} — all in-file. Dependents no-edit RE-CONFIRMED under β: post-run-review Q7 (F5 — β preserves all 4 states + the 4 transitions `:78-82`, so the WATCHING-walk stays definition-agnostic) + the 28 V-entry Status labels (β preserves; NO relabel). basis: README located reads `:26-58`/`:78-89`/`:118-159` (read this session) + F5 + D2-β below.
- **D2 [VERIFIED] (R1/R2 — reworked per β; supersedes the cycle-2 [INVALIDATED] line) Re-frame WATCHING, preserve the two-state machine.** (a) target: README WATCHING def (`:42-45`), lifecycle preamble (`:26-39` — exclusion's canonical home), FIX-SHIPPED def (`:47-58` — axis-sharpen), Vocab WATCHING clause (`:127-134`). (b) delta: WATCHING def "uncertainty exists; no structural fix shipped yet" → "an uncertain **shipped choice** is watched — an original design decision shipped under genuine uncertainty, with no *remedial* fix yet (the choice itself is shipped; what's watched is whether it holds)"; add ONE canonical exclusion in the lifecycle preamble — "every entry watches something **already shipped** (a choice → WATCHING; a remedial fix → FIX-SHIPPED); a not-yet-implemented gap is a **backlog item, not a watch entry**; a firing entry spawns a backlog item"; FIX-SHIPPED axis-sharpen — name it "a **remedial fix** shipped" so the WATCHING↔FIX-SHIPPED axis (uncertain-choice vs remedial-fix) is explicit; Vocab WATCHING mirrors. Transitions table (`:78-82`) UNCHANGED (still accurate under β — "WATCHING → FIX-SHIPPED at fix-ship commit" = the remedial fix ships). (c) acceptance: both states read as post-ship watches; the exclusion is stated once; all 4 states + 4 transitions + Q7 walk preserved; no README-internal contradiction (WATCHING no longer reads pre-fix) and none with practice 8. (d) side-effects: 22 WATCHING + 6 FIX-SHIPPED keep their labels (no relabel); Q7 unaffected; residual risk = a born-as-fix entry's WATCHING/FIX-SHIPPED placement, addressed by the FIX-SHIPPED axis-sharpen. (e) basis: README located reads `:3-7`/`:42-45`/`:47-58`/`:78-82`/`:127-134`/`:84-89` + F1/F2/F3/F6 + D1 (operator-confirmed boundary) + Fragmentation lens (exclusion canonical, not per-state-duplicated). Amendment completeness: WATCHING/FIX-SHIPPED references = in-file (transitions `:78-82`, RESOLVED `:60-71`, INVALIDATED `:73-76`, all reference the states by name not by the pre/post-fix axis → unbroken) + cross-doc D3/D4 (β-consistent) + Q7 (no-edit, F5).
  - considered: α collapse-to-one-state (rejected: relabels 22 entries + rederives the transition table `:78-82` + touches Q7, for conceptual tidiness only; β preserves all and resolves the contradiction — Pareto-better; operator-decided β 2026-06-06).
- **D5 [VERIFIED] (re-confirmed under β) Migration = bounded follow-on; no relocation this run.** β preserves the 22 WATCHING labels (shipped-choice-watches, F2 spot-audit) → no relocation. The full per-entry conformance sweep = a filed follow-on (now D6, per F7 / no-silent-deferral). basis: F2/F3 + β-preserves-labels (D2-β).
- **D6 [VERIFIED] (F7 / R3) File the conformance-sweep follow-on as its OWN backlog item at implement.** The seed item `v-entry-is-post-ship-only.md` archives when this run ships → the per-entry conformance sweep cannot ride it; it needs its own `dev-notes/backlog/<slug>.md` (the full WATCHING/FIX-SHIPPED conformance audit; spot-audit found conformant, full sweep separable). basis: F7 + CLAUDE.md no-silent-deferral (filing-shape: backlog item is the default home).
- **F7 [VERIFIED — addressed]** (was [VERIFIED — surfaced] cycle 2) — D6 commits the follow-on backlog item; the filing-discipline gap is closed by a tracked decision. basis: D6 (citation-equivalence: D6 names the same observable — the owed backlog item).

### Cycle 3 — standardized inspection pass (artifact: `.cycle-3.standardized-pass.md`)
In-scope lenses (edits to `validation-watch/README.md`, a dev-notes doc — NOT spec/skill render) — all clean; see artifact. Bloat / Fragmentation / Unenforced-rule / Undefined-shorthand / Missed-dependents lined; Leakage / Lossy-render / Over-claimed N/A.

### Cycle 3 — fresh-session implementability: PASSED
- D2-β: `validation-watch/README.md` `:26-39` (preamble) + `:42-45` (WATCHING) + `:47-58` (FIX-SHIPPED) + `:127-134` (Vocab) — all located reads this session. PASSED.
- D6: `dev-notes/backlog/` filing convention (`README.md` read this session) + CLAUDE.md filing-shape block. PASSED.

**Cycle 3 — F1–F4 dispositioned** (were [PENDING] from cycle 1; load-bearing investigation findings dispositioned now to unblock [READY], append-only):
- **F1 [VERIFIED — addressed]** (→R2, the contradiction) — D2-β re-frames WATCHING so "no structural fix shipped yet" no longer reads pre-fix → the practice-8 ↔ README contradiction is removed. basis: D2-β (citation-equivalence: D2 targets the WATCHING `:42-45` clause F1 observes).
- **F2 [VERIFIED — addressed]** (→R3, the audit: 22 WATCHING entries are shipped-choice-watches) — D5 commits "preserve the 22 labels, no relocation," resting on this audit. basis: D5 (names the same observed set).
- **F3 [VERIFIED — addressed]** (→R1, the refinement: preventive wording not mass-migration; V-26/V-27 conformant) — D2-β (preventive re-frame + canonical exclusion) + D5 (no mass-migration). basis: D2-β/D5.
- **F4 [VERIFIED — addressed]** (→R2, boundary: post-ship ⊇ shipped choice) — D1 commits the boundary (operator-confirmed). basis: D1 (citation-equivalence).

**Cycle 3 status:** D2 reworked to [VERIFIED] per β; D0/D5 re-confirmed; D6 added + F7 addressed; F1–F4 dispositioned; standardized pass clean; fresh-session PASSED. → convergence cycle (cycle 4): new-surface investigation + intent-falsification (R1/R2/R3) + mechanical falsification over [VERIFIED] D-entries. Per live `spec/core.md` §4.1.4 the intent pass runs this convergence cycle (no carry-forward).

### Cycle 4 — convergence cycle: intent-falsification → D-DELTA (artifacts `.cycle-4.intent-falsification.md`; subagent `a15cf254269e1d5fe`, opus)
New surfaces investigated this cycle: read 8 real WATCHING entry bodies (V-1/V-2/V-25/V-26/V-30/V-31 full/partial) — not cited in any prior cycle's artifact. Intent-falsification ran criteria-first vs R1/R2/R3; orchestrator re-ground each finding vs the actual entries (basis rule).
- **F8 [VERIFIED — surfaced → ACTIONED: reopens D2]** β's WATCHING wording "an uncertain shipped **choice** / **original design decision**" over-narrows: it mis-fits **V-30** (class-recurrence quality-watch over shipped fixes) and **V-31** (shipped rule D4.1's dodge-residual); contradicts operator-confirmed **D1** (a V-entry watches a shipped fix OR choice — an umbrella over BOTH states, NOT the WATCHING/FIX-SHIPPED separator). V-25 was a subagent over-read (its "fix" = the hypothetical future catcher; the shipped thing = the choice-to-accept-the-residual → fits). The real WATCHING/FIX-SHIPPED axis is the existing remediation-state axis, untouched. basis: read V-25/V-30/V-31 full + D1 + the README WATCHING/FIX-SHIPPED defs `:42-58`.
- **F9 [VERIFIED — surfaced]** transitions table (`:78-82`) models only WATCHING→FIX-SHIPPED→RESOLVED/INVALIDATED, but V-30/V-31 describe direct WATCHING→RESOLVED/INVALIDATED closes. PRE-EXISTING lifecycle-coverage question (not β-introduced); OUT of this run's R1/R2/R3 scope → folded into D6's conformance-sweep brief, not a scope expansion here. basis: read V-30/V-31 closing rules + transitions table `:78-82`.
- **F10 [VERIFIED — surfaced → ACTIONED: folds into D2]** Vocab WATCHING (`:128`) already drifted from the lifecycle def (`:42` — `:128` dropped "structural"); D2's "Vocab mirrors" under-specified → corrected D2 makes the Vocab state lines a pointer to the canonical lifecycle def (resolves the pre-existing duplication). basis: grep `:42` vs `:128` (non-identical).
- **D2 [INVALIDATED] → [PENDING]** (per F8/F10). D0 (dependent enumeration) + D5/D6 ride the rework. D1/D3/D4 UNAFFECTED (D3/D4 say "shipped fix/choice" — they ALIGN with the corrected umbrella wording, not the over-narrow axis).
- **Mechanical falsification pass SKIPPED this cycle: intent-delta** (per `spec/core.md` §4.1.4 / `modules.md` §3.4 skip carve-out).
- **R1 served; Q7 confirmed definition-agnostic (D0 no-edit-on-Q7 holds).**

### Cycle 5 — D2 rework: corrected β (drop the over-narrow axis; anchor "already-shipped decision")
- **D2 [VERIFIED] (R1/R2 — corrected per F8/F10; supersedes the cycle-3 line) Anchor WATCHING to "an already-shipped decision," preserve the existing remediation-state axis.** (a) target: README WATCHING def (`:42-45`), lifecycle preamble (`:26-39`, canonical exclusion), Vocab state lines (`:127-134`, → pointer). **FIX-SHIPPED def UNCHANGED** (the cycle-3 axis-sharpen is dropped — F8). (b) delta: WATCHING def "uncertainty exists; no structural fix shipped yet. Production signal is being watched." → "uncertainty exists about an **already-shipped** decision (a choice, rule, or accepted residual in the spec); **no remedial fix has shipped for it yet**. The production signal is watched. If the signal observes per practice 8, ship the fix at n=1 and transition to FIX-SHIPPED." Preamble canonical exclusion (once): "every entry watches an **already-shipped** decision or fix; a not-yet-implemented consideration — a noticed gap, decided-against or not-yet-built — is a **backlog item, not a watch entry** (the backlog is where it is implemented and tested); a firing entry spawns a backlog item." Vocab state lines → POINTER to the Entry-lifecycle canonical def (keep the `[INVALIDATED]`-tracker-tag disambiguation). Transitions table UNCHANGED. (c) acceptance: WATCHING reads post-ship ("already-shipped decision"); exclusion stated once; WATCHING/FIX-SHIPPED axis unchanged (remediation-state, NOT choice-vs-fix); fits ALL WATCHING entries (V-25 residual / V-30 class / V-31 rule-residual / V-1·V-2·V-26 choices); Vocab points to one canonical def (no drift); no README-internal contradiction, none with practice 8. (d) side-effects: NO entry relabel; FIX-SHIPPED untouched; Q7 untouched (F5/F9); Vocab→pointer resolves the pre-existing `:42`/`:128` drift. (e) basis: README located reads `:3-7`/`:26-39`/`:42-45`/`:47-58`/`:78-82`/`:127-134` + D1 (operator boundary: shipped fix OR choice = umbrella) + F8/F10 + V-25/V-30/V-31 reads (corrected wording fits each) + Fragmentation lens (exclusion canonical; Vocab→pointer). Amendment completeness: WATCHING/FIX-SHIPPED references = in-file (transitions/RESOLVED/INVALIDATED reference by state-name → unbroken) + Vocab (now pointer) + cross-doc D3/D4 (β-consistent, "shipped fix/choice") + Q7 (no-edit, F5).
  - considered: cycle-3 "WATCHING=choice / FIX-SHIPPED=remedial-fix" axis (rejected per F8: over-narrows, mis-fits V-30/V-31, contradicts D1's umbrella).
- **D5 [VERIFIED] (re-confirmed under corrected β) Migration = bounded follow-on; no relocation.** Corrected wording ("already-shipped decision") fits all 22 WATCHING entries even more cleanly than the cycle-3 draft → no relocation. basis: V-25/V-30/V-31 reads + F2.
- **D6 [VERIFIED] (broadened per F9) File the conformance-sweep follow-on as its own backlog item.** Brief now ALSO folds F9: reconcile each entry's closing-rule against the transitions table (`:78-82`) and flag any table-coverage gap (e.g. WATCHING→RESOLVED direct-close paths). basis: F7 + F9 + CLAUDE.md no-silent-deferral.

### Cycle 5 — standardized inspection pass (artifact `.cycle-5.standardized-pass.md`)
In-scope lenses (edits to `validation-watch/README.md`) — all clean; see artifact.

### Cycle 5 — fresh-session implementability: PASSED
- D2 (corrected): README `:26-39`/`:42-45`/`:127-134` (located reads this session). PASSED.
- D6 (broadened): `dev-notes/backlog/` convention + transitions table `:78-82`. PASSED.

**Cycle 5 status:** D2 corrected to [VERIFIED]; D5 re-confirmed; D6 broadened; standardized pass clean; fresh-session PASSED. → convergence cycle (cycle 6): fresh intent-falsification (R1/R2/R3) + mechanical falsification over [VERIFIED] D-entries (D0–D6).

### Cycle 6 — convergence cycle: intent-falsification → D-DELTA (artifact `.cycle-6.intent-falsification.md`; subagent `a401635d00c04a6d4`, opus)
New surfaces: 14 previously-unsampled WATCHING entries (V-3/4/8/9/10/11/14/16/17/18/20/21/22/23) + FIX-SHIPPED V-24/28/29.
- **R3 SERVED** — corrected "already-shipped decision (choice, rule, or accepted residual)" fits all 14 newly-sampled WATCHING entries; V-16/V-17 fit via "accepted residual"; 3 FIX-SHIPPED stay distinct. **Cycle-4 failure point fixed.** Collapse concern (F-A) does NOT recur (FIX-SHIPPED distinct on remediation-state axis). basis: subagent reads of the 17 entries.
- **F11 [VERIFIED — surfaced → ACTIONED: reopens D2]** (F-R1-1) the cycle-5 exclusion "a noticed gap, **decided-against** or not-yet-built → backlog" COLLIDES with the WATCHING def's "accepted residual" sub-shape — an accepted residual (V-16/V-17/V-25) IS a "decided-against building a mechanism" → the exclusion would wrongly route legitimate WATCHING entries to backlog (internal contradiction with the WATCHING def). Fix: drop "decided-against"; inline practice-8's discriminator (genuine uncertainty about a shipped decision = watch; classifiable not-yet-built fix = backlog). basis: read V-16 `:7-9`/V-17 `:7-11`/V-25 + cycle-5 exclusion text + practice 8 `:293-299`.
- **F12 [VERIFIED — addressed]** (F-R2-1) residual pre-ship phrasing survives at README `:42` AND Vocab `:128` (`grep "no.*fix shipped" → 2 hits`); NOT a design contradiction — a post-implement coverage check on D2's amendment-completeness. Folded into D2 acceptance + verify (post-implement grep must be empty). basis: grep `:42`/`:128` + D2 delta targets (D2 covers both via `:42` rewrite + Vocab→pointer).
- **F13 [VERIFIED — surfaced]** (F-R3-1) V-16/V-17 are the thinnest-fitting (only 2 of 22 fit via "accepted residual") → D6 sweep brief flags the accepted-residual vs not-yet-built discriminator. Not a design defect. basis: `grep "no dedicated mechanism shipped" → V-16/V-17`.
- **D2 [INVALIDATED] → [PENDING]** (per F11). D0/D5/D6 ride; D1/D3/D4 UNAFFECTED.
- **Mechanical falsification pass SKIPPED this cycle: intent-delta.**

### Cycle 7 — D2 refinement: de-collide the exclusion + inline the discriminator
- **D2 [VERIFIED] (R1/R2 — refined per F11/F12; supersedes the cycle-5 line) Anchor WATCHING to "already-shipped decision"; de-collide the exclusion; inline the practice-8 discriminator.** (a) target: README WATCHING def (`:42-45`), lifecycle preamble exclusion (`:26-39`), Vocab state-lines (`:127-134` → pointer). FIX-SHIPPED def + transitions table UNCHANGED. (b) delta: WATCHING def as cycle-5 (already-shipped decision: a choice, rule, or accepted residual; no remedial fix yet; signal→practice-8→FIX-SHIPPED). **Exclusion (refined, de-collided):** "Every entry watches an **already-shipped** decision or fix (a shipped decision to **accept a known limitation** — an 'accepted residual' — counts). A not-yet-implemented consideration — a noticed gap filed to be built and tested — is a **backlog item, not a watch entry**; a firing entry spawns one. The line (practice 8): genuine uncertainty about a *shipped* decision is a watch; a classifiable not-yet-built fix is backlog." — drops "decided-against" (the colliding term) + inlines the discriminator. Vocab state-lines → pointer to the lifecycle def. (c) acceptance: WATCHING reads post-ship; exclusion no longer collides with "accepted residual" (F11) + carries the discriminator (R1 airtight) + cross-confirms practice 8 (R2); **post-implement `grep -nE "no (structural )?fix shipped yet" README.md` is EMPTY** (F12 — both `:42` + Vocab `:128` rewritten); fits all entries; no contradiction. (d) side-effects: no relabel; FIX-SHIPPED untouched; Vocab→pointer resolves `:42`/`:128` drift; the discriminator clause is the only net-new prose vs cycle-5 (Bloat-checked: R1-load-bearing). (e) basis: README located reads `:3-7`/`:26-39`/`:42-58`/`:78-82`/`:127-134` + practice 8 `:293-299` (the discriminator's canonical source) + F11/F12 + V-16/V-17/V-25 reads + D1 + Fragmentation/Bloat lenses. Amendment completeness: as cycle-5 (state refs by name → unbroken; Vocab→pointer; D3/D4 β-consistent; Q7 no-edit) + the new discriminator cross-references practice 8 (F4-aligned).
  - considered: cross-reference the discriminator to practice 8 only (not inline) — rejected: F11's point is the README must be self-sufficient for a filer reading only it; a compact inline clause + cite is airtight where a bare cite is not.
- **D6 [VERIFIED] (broadened per F13) conformance-sweep brief ALSO flags the accepted-residual vs not-yet-built-gap discriminator** as the per-entry test (beyond F9's closing-rule-vs-table reconcile). basis: F13 + F9 + F7.

### Cycle 7 — standardized inspection pass (artifact `.cycle-7.standardized-pass.md`)
In-scope lenses (edits to `validation-watch/README.md`) — all clean; see artifact.

### Cycle 7 — fresh-session implementability: PASSED
- D2 (refined): README `:26-39`/`:42-45`/`:127-134` + practice 8 `:293-299` (located reads this session). PASSED.

**Cycle 7 status:** D2 refined to [VERIFIED] (de-collided exclusion + inlined discriminator); D6 broadened; standardized pass clean; fresh-session PASSED. → convergence cycle (cycle 8): fresh intent-falsification (R1/R2/R3) + mechanical falsification over [VERIFIED] D-entries.

### Cycle 8 — convergence cycle: intent-falsification (artifact `.cycle-8.intent-falsification.md`; subagent `a6e584c1056052508`, opus). OPERATOR flagged overthinking mid-cycle → revert to minimal.
- **R1 & R3 SERVED** (de-collided exclusion doesn't over-tighten; all WATCHING edges fit "already-shipped decision"). The DESIGN serves its intent — confirmed across cycles 6 AND 8.
- **F14 (F-1) [VERIFIED — surfaced → resolved by removal]** the cycle-7 inline discriminator MISATTRIBUTES to practice 8 a rule it doesn't state (practice-8 `:305-308` routes a form-in-hand classifiable fix to **commit-now**, not backlog). A cycle-7 over-addition. basis: `development-process.md:305-308`.
- **F15 (F-2) [VERIFIED — surfaced → resolved by removal]** umbrella asymmetry (README 3-way vs CLAUDE/practice-8 2-way) — consistent-not-contradictory; resolved by dropping the 3-way enumeration. basis: cross-doc read.
- **F16 (F-3) [VERIFIED — addressed]** D2 cycle-7 basis stale-cited practice 8 `:293-299` for the discriminator (lives `:305-308`); addressed by removal (minimal D2 has no discriminator). basis: `:293-299` vs `:305-308`.
- **D2 [INVALIDATED] → [PENDING]** → reverted to MINIMAL (cycle 9).

### Cycle 9 — D2 reverted to MINIMAL (operator-steered: stop gold-plating)
- **D2 [VERIFIED] (R1/R2 — MINIMAL; supersedes the cycle-7 line) Fix the WATCHING wording + a brief exclusion that echoes practice-8 entry-hygiene.** (a) target: README WATCHING def (`:42-45`), lifecycle preamble exclusion (`:26-39`), Vocab WATCHING state-line (`:127-134` → pointer). FIX-SHIPPED def + transitions table UNCHANGED. (b) delta: WATCHING def "uncertainty exists; no structural fix shipped yet. Production signal is being watched." → "uncertainty exists about an **already-shipped** decision (a choice or rule in the spec); **no remedial fix has shipped for it yet**. The production signal is watched. If the signal observes per practice 8, ship the fix at n=1 and transition to FIX-SHIPPED." Preamble exclusion (canonical, once): "Every entry watches an **already-shipped** decision or fix; a not-yet-implemented consideration — a noticed gap to be built and tested — is a **backlog item, not a watch entry** (`development-process.md` practice 8, Entry hygiene); a firing entry spawns a backlog item." Vocab WATCHING state-line → pointer to the Entry-lifecycle canonical def (keep `[INVALIDATED]`-tracker-tag disambiguation). **NO discriminator clause (F14/F16); NO 3-way sub-shape enumeration (F15).** (c) acceptance: WATCHING reads post-ship; exclusion routes noticed gaps → backlog, faithfully echoing practice-8 entry-hygiene `:310-315` (which already says a gap belongs in "some other doc"); umbrella consistent with CLAUDE/practice-8 ("decision: a choice or rule" ≈ "fix/choice"); post-implement `grep -nE "no (structural )?fix shipped yet" README.md` EMPTY; fits all entries; no contradiction. (d) side-effects: no relabel; FIX-SHIPPED untouched; Vocab→pointer resolves `:42`/`:128` drift; net-new prose vs the live README = the exclusion sentence + the WATCHING re-word (minimal). (e) basis: README located reads `:3-7`/`:26-39`/`:42-58`/`:78-82`/`:127-134` + practice 8 `:296-315` (Entry hygiene ALREADY routes a noticed gap out of validation-watch → the exclusion echoes it, no misattribution) + D1 + F14/F15/F16 + R1/R3-served (cycles 6 & 8). Amendment completeness: state refs by name (transitions/RESOLVED/INVALIDATED) → unbroken; Vocab→pointer; D3/D4 (CLAUDE/practice-8) say "shipped fix/choice" → consistent; Q7 no-edit (F5).
  - considered: cycle-7 inlined-discriminator (rejected per F14: misattributed practice 8 + over-added) · cycle-5 3-way enumeration (rejected per F15: umbrella asymmetry; "decision (choice or rule)" suffices).
- **D6 [VERIFIED]** (unchanged from cycle 7) — conformance-sweep backlog item folds F9 (closing-rule vs transitions table) + F13 (accepted-residual vs not-yet-built discriminator as the per-entry test). The edge-case nuance lives HERE (the sweep), not in the README core text.

### Cycle 9 — standardized inspection pass (artifact `.cycle-9.standardized-pass.md`)
In-scope lenses (edits to `validation-watch/README.md`) — all clean; see artifact (the minimal delta is strictly LESS surface than cycle-7's already-clean pass).

### Cycle 9 — fresh-session implementability: PASSED
- D2 (minimal): README `:26-39`/`:42-45`/`:127-134` + practice 8 `:310-315` (located reads this session). PASSED.

### Convergence judgment — HALTING the loop (orchestrator, operator-steered)
The minimal D2 is a **strict subset** of the cycle-5 and cycle-7 designs that convergence cycles 6 and 8 already attacked fresh-context; both returned **R1 & R3 SERVED**, and every D-delta they raised (cycle-4 axis, cycle-6 "decided-against" collision, cycle-8 discriminator misattribution + umbrella asymmetry) lived in elaborations the minimal design **removes**. The remaining cycle-8 residuals (F14/F15) are resolved by removal; F16 addressed. Per `spec/core.md` §4.1.4 a convergence cycle "converges: its surfaced concerns terminate and accumulate; only an open mechanically-confirmable finding holds the phase" — there is no open mechanically-confirmable finding on the minimal design (F16, the only mechanical one, is addressed by removal). Running a fresh cycle-10 dispatch would attack clauses that no longer exist. The orchestrator surfaces this halt at [READY] for the operator's `next phase` gate (the operator owns the transition).

### Implement entered → loopback (D4 dropped; 2026-06-06)
Operator selected `next phase` → implement. Before any edit, the mandated skill-craft pre-edit discipline (CLAUDE.md rule-corpus-edits; `anti-patterns.md` Additive-reflex + Edit-as-Pareto) surfaced an actioned finding → loopback per `phases/implement.md` (no inline-fix).
- **F17 [VERIFIED — addressed]** D4 (practice-8 cross-confirm sentence) is **redundant**: practice 8 ALREADY embodies "V is post-ship / a gap belongs elsewhere" at not-a-deferral-journal (`:296-308`) + Entry-hygiene (`:310-315`). R2 (cross-confirm, no contradiction) holds **without** D4 (README D2 + CLAUDE.md D3 + practice-8-as-is). Adding D4 would restate practice 8 → cross-file fragmentation. Requirements-anchored-brake test: R2 does not FAIL without D4 → not warranted. Operator agreed to drop (2026-06-06). This is the **design-formation vector** of the same root captured in `dev-notes/backlog/convergence-surfaced-finding-action-brake.md` (n=2). basis: `development-process.md:296-315` + skill-craft `anti-patterns.md` Additive-reflex/Edit-as-Pareto + R2 holds (D2/D3).
- **D4 [INVALIDATED] → dropped** (not implemented). D0/D1/D2/D3/D5/D6 unaffected. basis: F17.
- **Cycle 10 (loopback investigate-design) — re-[READY] on the {D0,D1,D2,D3,D5,D6} subset.** Dropping D4 only REMOVES surface; the remaining set is a strict subset of the already-converged design (cycles 6/8 attacked supersets containing exactly these). Convergence carries forward — no fresh dispatch (same subset-of-converged judgment as the loop-halt above; consistent with the operator-endorsed stop-gold-plating steer). Standardized pass: dropping a decision adds no rule-text → no new lens surface. Fresh-session: PASSED (D2/D3/D6 reads located this session).
- **Impl plan (revised, 3 units):** U1 = D2 (`dev-notes/validation-watch/README.md`), U2 = D3 (`CLAUDE.md`), U3 = D6 (new `dev-notes/backlog/` file). Scope-disjoint (distinct files) → parallel-eligible; small + locked → implemented in working context (self-hosting: in-tree backlog captures make the repo-level integrity clean-precondition N/A; path-level attribution used instead — known friction `self-hosting-inplace-integrity-clean-precondition`). U2 (CLAUDE.md) edit shown to operator before commit.

### Header phase → implement (cycle 10 re-[READY] cleared; D4 dropped)

### Implement — completed (3 units, working context; self-check clean)
U1 (README D2: preamble exclusion + WATCHING re-anchor + Vocab→pointer), U2 (CLAUDE.md D3), U3 (D6 new `validation-watch-entry-conformance-sweep.md`) all implemented. Self-check (4 write-time lenses) clean: Lossy-render N/A (no plugin render); Missed-dependents clean (states referenced by name → unbroken; Q7 definition-agnostic; CLAUDE.md consistent); Undefined-shorthand clean; Over-claimed N/A. Change-set within listed scope. Post-implement F12 check: `grep -nE "no (structural )?fix shipped yet" README.md` → EMPTY.

### Verify — [PASSED] (isolated; subagent `a492b202ae84f282a`, opus)
Conducted in a fresh isolated context against the tracker + produced rule-text. Non-method-kernel doc-edit run → battery 4(a) render-fidelity N/A (no plugin render); live checks = planned-vs-actual + requirements-coverage + lenses + 4(b) coherence + 4(c) skill-craft form. All four checks accounted-for; no finding short of [VERIFIED]. Coherence: the F1 pre-ship contradiction resolved by text removal (grep empty), Vocab pointer resolves to the canonical def, README↔CLAUDE.md↔practice-8 cross-confirm with no contradiction (practice 8 consistent without the dropped D4). D4 confirmed absent (`development-process.md` unmodified).
```
Verify result: [PASSED] (isolated)
Finding ledger: F1–F17 all [VERIFIED] (dispositions: F1/F2/F3/F4/F5/F7/F10/F12/F16/F17 addressed; F6/F8/F9/F11/F13/F14/F15 surfaced). No finding short of [VERIFIED].
```

**RUN COMPLETE.** Run-history archived to `dev-notes/run-history/v-entry-is-post-ship-only/` (first instance of `runs-data-preservation` option (b)). Seed backlog item archived. CLAUDE.md edit committed with operator approval ("per your rec", 2026-06-06).
