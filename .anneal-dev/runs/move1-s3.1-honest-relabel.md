# Run: move1-s3.1-honest-relabel

- **Status:** PASSED
- **Phase:** verify
- **Mode:** interactive
- **Release:** `819e84e` (operator step-4 soundness approved; D5 no-new-invariant stands; checkpoints 22d306b/247a86a/0fa546d consolidated; pushed). Render-follow queued to the render-debt batch (D7). 438 residual filed to the sweep (`c5adcbb`).
- **Task summary:** Move 1 from the intent-falsification-soundness-sweep — honestly relabel `spec/core.md` §3.1 (the evidence-bearing-artifact rule + "separate checker") so it distinguishes what BINDS from what SURFACES, and reconcile every dependent site. Method-kernel edit.

## Requirements record (task-input, not a track)

**Operator verbatim request:** "as you recommend" (selecting the AI's recommendation to take Move 1 — the §3.1 honest relabel — through a proper anneal-dev cycle) → "yes lets continue".

**Goal, enumerated (separated from the proposed solution):**
- **R1** — §3.1 honestly distinguishes what genuinely BINDS (a checker terminating at an external, re-checkable fact — executed output / a render-fidelity or coherence check exercising the work / a state-marker on the work product — or the operator's irreducible soundness verdict) from what only SURFACES (a self-recorded textual re-derivation whose verdict the producing orchestrator writes into the tracker). It must NOT claim the latter binds ("the guarantee comes from the checker").
- **R2** — §3.1's load-bearing function is preserved or sharpened, never weakened: it remains THE evidence-bearing standard the corpus rests on (cited by practice 1, the basis rule, verify, the falsification passes). The strong/weak gradient §3.1 already carries ("strong = points at external re-checkable truth; weak = a claim about the run's own state") is the lever, not a new concept.
- **R3** — every dependent site encoding the same overclaim contract is reconciled; no orphaned overclaim left corpus-wide (the contract-change completeness claim — practice 4).

**Method-kernel binding (process, not an R#):** verify must add the kernel-independent legs — skill-craft self-review (form) + operator soundness (`development-process.md` §1 method-kernel rule). The sweep finding is SURFACED, not certified — so the operator's soundness ON THIS DESIGN is the irreducible certification of the premise (that §3.1 overclaims and should be relabeled). The foundation-invariant focusing artifact aims that pass.

## F-track (findings)

- **F1** [PENDING] — Scope-search dispatched (the corpus-wide enumeration of every spot encoding the §3.1 overclaim contract). Basis: subagent dispatch in flight; results pending.
- **F1** [VERIFIED — non-issue→established] (cycle 1) — Scope search returned (subagent `a3cf6a118263bf306`). Basis: wrap-tolerant single-token greps across all repos (`evidence-bearing`, `un-fakeable`, `separate checker`, `guarantee comes from`, `structural enforcement`, `is the check`, strong/weak-gradient tokens) + a `perl -0777` newline-flattened pass + cross-repo over skill-craft + 4 instance plugins; reference-classes literal/cross-ref/paraphrase/rendered-clause/glossary/self-classification all covered. Enumeration recorded in D1.
- **F2** [VERIFIED] (cycle 1) — §3.1 is the MOST honest site: it already concedes "no artifact an AI produces is un-fakeable" + carries the strong/weak gradient (`spec/core.md` L115-130, located read). The primary overclaim is DOWNSTREAM — the self-classifications that flatten §3.1's gradient to "the X IS the un-fakeable artifact — its presence is the check" (`development-process.md` L391 practice-10, L468 practice-11, L533 practice-12, L571-573 step-4). Basis: scope-search F1 located reads at those lines.
- **F3** [VERIFIED — surfaced] (cycle 1) — Foundation-invariant touch: "evidence-bearing" is NOT a registered invariant, but **INV-3** (verify-isolation: renderer≠verifier≠actor) encodes the independence/separate-checker lever the relabel sharpens. Basis: `dev-notes/foundation-invariants/INV-3-verify-isolation.md` (scope-search located read). Per the method-kernel rule the focusing artifact tests INV-3; whether the relabel needs a new/amended invariant is the operator's soundness call (surfaced, terminal).

## D-track (design decisions)

- **D1** [PENDING] — **Scope** (foundational): the set of corpus spots encoding the §3.1 overclaim contract. Basis: scope search (F1, in flight).
- **D1** [VERIFIED] (cycle 1) — **Scope established** (search-established by F1). The dependent set, by class:
  (i) SOURCE-edit — the contract: `core.md` §3.1 body (L111-151); its in-`core.md` cross-ref/contrast sites (L421, L479, L861, L966, L1055 + pointer sites); `glossary.md` Evidence-bearing-artifact-rule term (L56-59) + separate-checker (L165) + restatements (L214, L476); `README.md`:124; `development-process.md` self-classification cluster (practice-8 L282-294, practices 10/11/12 L391/L468/L533/L539, step-4 L571-573, restatements L111/L436); `foundation.md`:25-26; `instantiation-guide.md`:72,394; `anneal-dev/spec/lens-set.md` L134/141/144 + `lens-supplement.md`:27 + `README.md`:7; `dev-notes/validation-watch/V-25`.
  (ii) RENDER-follow (re-render from corrected source, never hand-edit): `anneal-dev/plugin/.../foundations.md` L106-145 (+ installed twin) + its lenses/phases/tracker renders; downstream `coding-clippy` + `daneel` foundations.md (separate repos, normal instance cadence); `bauleitplan-anneal` carries no §3.1 render.
  (iii) OUT (do not touch): `modules.md §3.1` (a DIFFERENT §3.1 — the tracker); derivation-rationale + dev-notes/research (rationale, not contract).
- **D2** [VERIFIED] (cycle 1) — **Relabel approach:** amend §3.1 (and reconcile the dependent self-classifications) by sharpening §3.1's OWN strong/weak gradient — a checker whose verdict terminates at an EXTERNAL re-checkable fact (executed output / render-fidelity exercising the render / verify exercising the work / the operator's irreducible verdict) BINDS; a checker that re-derives a textual basis and whose verdict the producing orchestrator self-records is itself a WEAK artifact → a strong SURFACER, not a guarantee. Replace §3.1's "the guarantee comes from the checker, not the artifact" with the bind-comes-from-the-external-terminus-or-operator framing. Contract surface: the AI behavior is the corrected bind/surface distinction; the evidence-bearing artifact is the gradient §3.1 already requires; renders carry it structurally. Basis: F2 (located reads). Realization text produced at impl (not here, per practice 5 / tracker brevity).
- **D3** [CONDITIONAL] (cycle 1) — **Scope DEPTH = FULL reconciliation** of all SOURCE-edit self-classification/restatement sites in D1(i) (thorough-fix per Recommendation discipline — R3 forbids leaving an orphaned overclaim). Cross-references that merely POINT to §3.1 (the pointer sites) inherit the corrected meaning and need NO edit (Missed-dependents: a cross-ref is a dependent that resolves correctly once the target is fixed); only paraphrased RESTATEMENTS that re-encode the overclaim are edited. Assumption (operator-resolvable): the operator accepts the full-reconciliation scope vs a narrower §3.1-plus-worst-offenders slice. Cost is the operator's (Recommendation discipline).
- **D4** [CONDITIONAL] (cycle 1) — **"un-fakeable" word + skill-craft boundary:** keep "un-fakeable" as §3.1's gradient-bounded term (§3.1 already bounds it: "evidence-bearing is a gradient… no artifact is un-fakeable"); the relabel fixes the BIND-vs-SURFACE overclaim, it does NOT delete the word. Skill-craft's upstream "un-fakeable-artifact principle" (`skill-craft` PROCEDURE.md L143-149) is OUT of scope — it states the IDEAL ("evidence that cannot be produced without doing the work"), the correct target, not a claim a specific artifact achieves it. Assumption (operator-resolvable): the operator agrees skill-craft stays out (a clean boundary) vs pulling the upstream sibling in. Basis: scope-search located read of PROCEDURE.md L143-149.
- **D5** [CONDITIONAL] (cycle 1) — **Foundation-invariant disposition:** the relabel SHARPENS the existing independence principle (INV-3 territory), it does NOT add a new registered invariant. The focusing artifact for the method-kernel verify tests INV-3 (verify-isolation independence) against the corrected §3.1. Assumption (operator-resolvable, soundness-class): the operator's soundness pass judges whether the bind/surface distinction warrants its own registered invariant (F3, surfaced). Basis: F3.

### Cycle 2 — per-site reconciliation deltas locked

- **F4** [VERIFIED] (cycle 2) — Per-site overclaim audit: the SOURCE-edit sites split by severity. SUBSTANTIVE overclaims (flatten §3.1's gradient / claim binding): `development-process.md` practice-10 L391 ("glossary entry … presence is the check"), practice-11 L468 ("the table itself is the un-fakeable artifact — presence is the check"), step-4 L571-573 ("the artifact IS the un-fakeable thing"). PARTIAL (one leg binds): practice-12 L533/539 (git-log cycle-count + marker IS external-terminus → binds; the "audit produces findings artifact" leg surfaces). FAITHFUL ALREADY (no overclaim — cross-ref/correct statement, no edit): core.md L421/L479 ("an artifact produced and judged in the same context is not self-enforcing"), L861/L966/L1055 + pointer sites, V-25 ("tags are weak artifacts… enforced by a separate checker"). MINOR-ALIGN (absolute "un-fakeable"): `instantiation-guide.md`:394, `foundation.md`:25-26, README.md:124, glossary term L56-59. Basis: located reads per F1 enumeration.
- **D6** [VERIFIED] (cycle 2) — **Per-site reconciliation delta map** (delta shapes, not realization text — practice 5; impl produces wording via skill-craft). (a) **§3.1 core.md L111-151** — amend the "separate checker" paragraph: the checker's verdict inherits §3.1's own gradient (a self-recorded textual re-derivation is itself a *weak* artifact); name the binding terminus (external re-checkable fact / executed output / operator verdict); recast "the guarantee comes from the checker, not the artifact" → bind-from-external-terminus-or-operator, checker-without-one-surfaces. (b) **practice-10/11 + step-4** — downgrade "the X IS the un-fakeable artifact / presence is the check" to "weak artifact: presence-checkable; correctness surfaced for the separate checker / operator second-judge", and at step-4 affirm "operator approval is the binding gate; the discharge surfaces the checks, render-fidelity (no-in-context) being the one author-side leg that binds". (c) **practice-12** — affirm the git-log/marker leg binds (external terminus), the audit-content leg surfaces. (d) **glossary Evidence-bearing-artifact-rule term + minor-align sites** — align the definition + drop absolute "un-fakeable" → gradient-bounded. (e) **faithful sites + pointer cross-refs** — no edit (inherit). Basis: F4 + D2.
- **F5** [VERIFIED] (cycle 2) — **Fresh-session-implementability: PASSED.** A fresh session with the tracker (D1 scope, D2 approach, D6 per-site delta map, D4 word-boundary, D5 invariant-disposition) implements without surfacing a new DESIGN decision — the exact §3.1/practice wording is realization output (impl, skill-craft-guided), not design. Per-implementer-step external evidence: step "edit §3.1" → `core.md`:111-151 (located read, the gradient + separate-checker para present); step "reconcile practice-10/11/step-4" → `development-process.md`:391/468/571-573 (located reads, the overclaim phrasings present); step "minor-align" → the F4-cited lines; step "keep un-fakeable/skill-craft-out" → D4; step "test INV-3" → `INV-3-verify-isolation.md` (F3). Basis: the cited located reads.

### Cycle 2 — standardized inspection pass (lenses now in scope on the delta design)

- **Bloat** — CLEAN: D6 reduces overclaim prose (downgrades absolute claims to gradient-accurate), net-subtractive at the substantive sites; basis D6.
- **Fragmentation** — CLEAN: the bind/surface distinction is stated ONCE canonically at §3.1; all other sites cross-reference or are reconciled to it, no second copy; basis D6(e) (pointer-cross-refs inherit, not restate).
- **Lossy-render** — CLEAN (design-time): D6 sharpens, does not soften, an enforcement "must" — it makes honest a claim that was *over*-strong; no "must"→"should"; basis D2/D6.
- **Unenforced-rule** — CLEAN: §3.1's enforcement is the separate checker + operator; D6 does not remove enforcement, it names which terminus binds; basis D6(a).
- **Undefined-shorthand** — surfaced as F6.
- Out-of-scope this cycle (fire at impl on actual text): Leakage (domain-general check at impl), Missed-dependents (covered cycle 1, F1), Over-claimed-verification (covered cycle 1).
- **F6** [VERIFIED — surfaced] (cycle 2) — Undefined-shorthand: the relabel introduces "external terminus" / "textual re-derivation" / "strong surfacer" as load-bearing phrases. NOT new coined shorthand needing glossary entries — they are descriptive applications of §3.1's existing strong/weak gradient (`core.md` L121-130, located read), not fixed-meaning terms used across multiple homes. Surfaced for the operator + impl: if impl uses any as a repeated fixed term, practice-10 (glossary entry) fires. Basis: located read of the gradient + D6.

### Cycle 3 — convergence cycle (produced D-track deltas → NOT [READY])

- **Mechanical falsification pass** (subagent `a6ff65d4329ff969e`): D1/D2/D6 all aggregate-HOLDS — no missed dependent (newline-flattened sentence pass surfaced only enumerated sites), no mis-classification (every "faithful" site genuinely gradient-accurate; D2's claimed §3.1 gradient + "guarantee comes from the checker" L145 confirmed present verbatim). Artifact: `.cycle-3.falsification.md`.
- **Intent falsification pass** (subagent `a436f7cee784997f8`): R1/R2/R3 all `served` (genuine operational distinction; floor preserved via author-side fresh-context termini; all source sites reconciled). 3 findings → F-A/F-B/F-C. Artifact: `.cycle-3.intent-falsification.md`.
- **F-A** [PENDING→addressed by D7] (cycle 3) — render-follow tier lags source: R3's corpus-wide completeness must be SOURCE-scoped (practice-4), with the RENDER-follow set (D1(ii): plugin foundations.md L106-145 + downstream clippy/daneel) tracked as a dependent so the installed overclaim isn't silently dropped during render-lag. Route was mechanical-falsification-candidate (dependent-completeness on D1). Basis: D1(ii) located read + the self-hosting build-artifact rule (renders batch as hygiene).
- **F-B** [VERIFIED — surfaced] (cycle 3) — operator-verdict-as-binding-terminus risks the forbidden operator-detection-as-enforcement (`core.md` L69-78). REFUTED for the relabel itself (operator-approval-as-gate pre-exists at step-4 L573; the bound action is permitted menu-selection, not detection) — but the §3.1 realization text MUST fence "operator surfaces / second-judges" from "an author-side check a fresh-context checker could carry, downgraded to the operator" (the malformed form). Terminal; feeds the method-kernel operator-soundness pass. Basis: `core.md` L69-78 vs L573 located reads.
- **F-C** [PENDING→addressed by D2-amend] (cycle 3) — "external terminus" under-defines the bind test; render-fidelity is itself a subagent JUDGMENT that binds only if re-derivable by a non-producing context. The real bind property is §3.1 L141-142 ("a context that did not produce the artifact re-derives it") = producer-independence (INV-3), NOT "externalness". Route mechanical-falsification-candidate (coupling-shape on D2). Basis: `core.md` L141-142 located read.

### Cycle 4 — deltas applied

- **D2** [INVALIDATED] (cycle 4) — amended per F-C (amendment-is-contradiction): the bind test as "external terminus" is replaced.
- **D2** [VERIFIED] (cycle 4, re-formed) — **Relabel approach (sharpened):** the BIND test is **producer-independence — a verdict re-derivable by a context that did not produce the artifact** (`core.md` L141-142, the separate-checker property; INV-3 verify-isolation), terminating at a re-checkable fact (executed output / the work exercised by verify / a render re-derived by a non-producing context / git-log) OR the operator's verdict. A verdict the producing orchestrator self-records, NOT independently re-derivable, is a WEAK artifact → strong SURFACER, not guarantee. Recast §3.1's "the guarantee comes from the checker, not the artifact" accordingly. This sharpens (does not weaken) §3.1 L139-145. Basis: F2 + F-C (`core.md` L141-142 located read). D6 unaffected (the per-site map is unchanged by sharpening the bind *test*).
- **D7** [VERIFIED] (cycle 4) — **Render-follow tracking** (per F-A): the SOURCE reconciliation queues its source-delta into the existing render-debt mechanism (`dev-notes/backlog/instance-reinstantiation.md` — the batched re-render of plugin foundations.md + downstream clippy/daneel from the corrected §3.1), per the operator-decided render-cadence policy (framework runs ship spec-only; renders batch). R3's completeness is thereby SOURCE-scoped with the render-follow set explicitly tracked, not silently dropped. Basis: `dev-notes/backlog/README.md` render-cadence policy (located read, this session) + D1(ii).

### Cycle 5 — convergence cycle (CONVERGED — zero D-track delta → [READY])

- **F-C** [VERIFIED — addressed] (cycle 5) — addressed by D2 (re-formed): producer-independence bind test grounded in `core.md` L141-142 (existence-confirmed PRESENT verbatim), non-circular, no new conflict.
- **F-A** [VERIFIED — addressed] (cycle 5) — addressed by D7: `dev-notes/backlog/instance-reinstantiation.md` existence-confirmed PRESENT (an OPEN, gated render-debt queue) — render-follow tracked, not silently dropped.
- **F-B** [VERIFIED — surfaced] (cycle 5, carried) — terminal; pure operator-soundness/realization-wording judgment, no runnable check against the design; feeds the method-kernel operator-soundness pass.
- **Convergence verification** (subagent `a02a6ad56c2572097`, fresh-context): R1/R2/R3 all `served` on the amended design; D2's L141-142 basis PRESENT; D7's render-debt basis PRESENT; **CONVERGED — no new D-track delta.** New surfaces this cycle: `core.md` L141-142 (producer-independence property), `INV-3-verify-isolation.md`, `instance-reinstantiation.md` (none cited in prior cycles). Artifact: `.cycle-5.convergence.md`.
- **F5** [VERIFIED] (cycle 5, basis-strengthened) — Fresh-session-implementability re-affirmed on the AMENDED design: D2's sharpened bind test (L141-142) + D7's explicit render-tracking make it MORE precise, not less; a fresh session implements from D1/D2/D6/D7 without surfacing a new design decision (exact wording = realization output at impl). Per-step external evidence per cycle-2 F5 + the L141-142 / instance-reinstantiation.md confirmations (cycle 5).
- **Standardized coverage accounted whole (at [READY]):** Missed-dependents (cycle 1, F1) · Over-claimed-verification (cycle 1, F1) · Bloat/Fragmentation/Lossy-render/Unenforced-rule/Undefined-shorthand (cycle 2, on the delta design) · **Leakage — CLEAN** (the §3.1 relabel adds no domain-specific concretion; §3.1 is domain-general and stays so — basis D2/D6, no named language/tool/path introduced). The six rule-text lenses re-fire at IMPL on the actual produced text (the standardized pass's design-time + battery compounding, `lenses.md`).
- **[CONDITIONAL] decisions at [READY]:** D3 (full-reconciliation scope), D4 ("un-fakeable"/skill-craft-out boundary), D5 (sharpen INV-3, no new invariant) → become [AUTO-ACCEPTED] on the operator's `next phase` selection unless overridden (surfaced for review, `tracker.md`).

### [READY] → implement transition (operator selected `next phase`)

- **D3** [AUTO-ACCEPTED] (operator `next phase`, no override) — full-reconciliation scope.
- **D4** [AUTO-ACCEPTED] (operator `next phase`, no override) — keep "un-fakeable" as gradient term; skill-craft out.
- **D5** [AUTO-ACCEPTED] (operator `next phase`, no override) — sharpen INV-3; no new invariant.
- **Operator design-shape soundness:** the `next phase` selection originates the method-kernel design-shape soundness verdict (per `development-process.md` §1; the rendered-wording soundness is due at step-4 verify — `method-kernel-soundness-verdict-locus` backlog item). Phase → implement.

### implement — unit completions

- **Unit 1** [COMPLETE] (subagent `ab03c891777da7141`) — `spec/core.md` §3.1 amended per D2/D4/D5/F-B. skill-craft invoked pre-edit; self-check 4 lenses CLEAN; no loopback. Integrity check PASSED (only `spec/core.md` §3.1 changed; HEAD snapshot `c79b888` held during edit). Persistence reference: checkpoint `22d306b` (`anneal-checkpoint:` — discharge-skipped, working-progress save). Operator approved the §3.1 wording ("its good").
- **Units 2-5** [COMPLETE] (subagent `a19a6740f306e7cab`) — downstream reconciliation: `development-process.md` practices 10/11 + step-4 (overclaim→weak-artifact + binding-party named) + practice-12 (binding git-log leg / surfacing audit-content leg split); `glossary.md` term gradient clause; `foundation.md`:25 ("un-fakeable"→"evidence-bearing"); `instantiation-guide.md`:394 ("un-fakeable signal"→"strong signal, producer-independent re-checkable"); `spec/README.md` + `anneal-dev/spec/*` + V-25 no-edit (inherit/faithful). skill-craft invoked pre-edit; self-check 4 lenses CLEAN; no loopback. Integrity check PASSED (only the 4 D6 files). Persistence reference: checkpoint `247a86a`.

### verify (isolated) → [ISSUES FOUND] → loopback

- **Verify** (subagent `a1578e71593ad09d4`, ISOLATED) — 4 checks run. Planned-vs-actual D1-D7, Requirements R1/R2/R3, lenses, battery (a) render-fidelity N/A cited (spec-only; D7 queues render-follow), (b) coherence, all clean EXCEPT one finding. F-B fence VERIFIED present + effective; R2 confirmed (no "must" softened). **[ISSUES FOUND]** on F-V1.
- **Skill-craft self-review** (subagent `a7eb7d2406609748c`, ISOLATED, kernel-independent FORM leg) — PASS: §3.1 sharpening does not flatten (every "must" preserved; bind test anchored to the enumerated termini, not naked judgment; Pareto-improving). Notables: dev-process:438 + :538 (the un-fakeable residuals); the form-review judged :538 keep-as-is (marker binds), splitting with verify.
- **F-V1** [PENDING] (verify) — `development-process.md:538` "the marker IS the un-fakeable artifact" left absolute — an in-scope D6(c) miss (Unit 3 reconciled the classification para but not this body phrase), inconsistent with §3.1's gradient + the run's parallel reconciliation (`instantiation-guide:394`). Behavior-preserving (the marker binds). Basis: verify located read + the parallel reconciliation.

### Loopback → investigate-design (verify [ISSUES FOUND], single locus) → cycle 6

- **D8** [VERIFIED] (cycle 6) — **Fix F-V1:** reconcile `development-process.md:538` "the marker IS the un-fakeable artifact" → gradient-accurate "the marker is a **strong, producer-independent** artifact (`core.md` §3.1)" (it binds — git-log terminus), matching the practice-12 classification para + §3.1. Operator second-judged the disposition (fix-538, file-438) and authorized. Basis: F-V1 + §3.1 (located read) + the parallel `instantiation-guide:394` reconciliation. **Behavior-preserving** → the prior convergence-clean (cycle 5) CARRIES FORWARD (re-trigger rule: a behavior-preserving fix does not re-open the intent-falsification pass); no new convergence cycle. **438** (practice-9 named-loss, OUT of D6 scope) filed to `intent-falsification-soundness-sweep` root-move-1, not fixed here.
- Phase → implement (re-run, D8 only).
- **Unit D8** [COMPLETE] (subagent `a2bce88bf6f4729a7`) — `development-process.md`:538 fixed. skill-craft invoked; self-check CLEAN; no loopback; Integrity PASSED (one phrase, one file). Persistence: checkpoint `0fa546d`.

### delta-verify (isolated, behavior-preserving) → [PASSED]

- **Delta-verify** (subagent `af99cf131c9394d5b`, ISOLATED) — **F-V1 closed** ("the marker IS the un-fakeable artifact" → "strong, producer-independent artifact (`core.md` §3.1)"; coheres with the practice-12 classification para); **regression clean** (git-log/cycle-count mechanism + MUST-emit enforcement intact; one phrase, one file). **Result: [PASSED] (isolated).**
- **F-V1** [VERIFIED — addressed] (delta-verify) — addressed by D8.

### verify [PASSED] — finding ledger (all terminal)

```
Verify result: [PASSED] (isolated, after one loopback)
  F1 [VERIFIED — non-issue]    F2 [VERIFIED — addressed]   F3 [VERIFIED — surfaced]
  F4 [VERIFIED — addressed]    F5 [VERIFIED]               F6 [VERIFIED — surfaced]
  F-A [VERIFIED — addressed]   F-B [VERIFIED — addressed]  F-C [VERIFIED — addressed]
  F-V1 [VERIFIED — addressed]
```

### Foundation-invariant focusing artifact (method-kernel edit — aims the operator's soundness pass; NOT a soundness verdict)

- **Touched invariant: INV-3 (verify-isolation — "renderer ≠ verifier ≠ actor")**, kernel home `core.md` §4.3 + `foundation.md`; external anchor = the independence principle (an actor cannot certify its own work). **Holds / STRENGTHENED:** the §3.1 amendment makes **producer-independence** (a verdict re-derivable by a context that did not produce the artifact) THE bind test — which *is* INV-3's independence principle, now named explicitly as the evidence-bearing rule's binding condition. The change reinforces INV-3 against its anchor; it does not violate it. Basis: F3 (scope-search located read of `INV-3-verify-isolation.md`) + D2 (the producer-independence bind test) + the verify F-B-fence confirmation (operator-as-terminus fenced from operator-detection, preserving INV-3's actor≠verifier).
- **No other registered invariant touched** (INV-1 basis-rule, INV-2 complete-state, INV-4 loopback, INV-5 exclusion-via-named-falsifier — none in the change's surface; "evidence-bearing" is not itself a registered invariant, F3).
- **Surfaced for the operator's soundness verdict (D5/F3, terminal):** does the **bind-vs-surface distinction** (producer-independent re-derivability) warrant its OWN registered foundation invariant, or is sharpening INV-3 sufficient? The kernel cannot self-certify this — operator's call.
