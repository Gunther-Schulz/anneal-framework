# campaign3-enforcement-fidelity — cycle 1 standardized-pass artifact

Lens set: anneal-dev core (`lens-set.md`) ∪ project supplement (`anneal-dev.config/lenses.md`, if any). One line per in-scope lens; this-cycle basis cited.

- **Bloat** — CLEAN. D3 adds one load-bearing sub-case (c) to §5.1 `deferred` (forces owed-artifact existence); no restate/hedge/over-illustration. basis: D3 (b)/(c) this cycle.
- **Fragmentation** — CLEAN. D3 = the §3.1 evidence-bearing principle *applied* to the `deferred` disposition, not a second copy. basis: read `core.md` §3.1 (111-169) + D3 this cycle.
- **Leakage** — CLEAN (design guard). D3 in domain-general `core.md` §5.1 states the owed-artifact **kind instance-defined** (does not hardcode "V-entry"). basis: D3 (b)/(d) this cycle.
- **Missed-dependents** — CLEAN. D0 search enumerated the §5.1 `deferred` dependents (glossary "Finding disposition" indexes; generic-cite `[VERIFIED — deferred]` refs unaffected; plugin render → ⑥); none restate the sub-cases. basis: D0 wrap-tolerant search this cycle (`rg "deferred"` / `rg "surfaced"`).
- **Unenforced-rule** — CLEAN. D3's violation is visible (cited `<path>` checked present; absent → cannot reach `[VERIFIED — deferred]`). basis: D3 (c) acceptance this cycle.
- **Undefined-shorthand** — CLEAN. "deferred-pending-authoring" is a single-home sub-case label in §5.1 (the glossary four-name disposition enum unchanged); single canonical defining sentence suffices, no glossary entry (practice 10). basis: read `glossary.md` "Finding disposition" (343-347) + practice 10 this cycle.
- **Lossy-render** — OUT OF SCOPE this cycle. No render/paraphrase committed (the §5.1 render is deferred to campaign ⑥); cited reason.
- **Over-claimed-verification** — OUT OF SCOPE this cycle. No "verified/checked" completeness claim recorded beyond the D0 search (which carries its re-runnable query); cited reason.

Last-cycle pass: no load-bearing finding.
