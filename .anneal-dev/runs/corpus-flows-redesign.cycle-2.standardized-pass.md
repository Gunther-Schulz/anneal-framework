# corpus-flows-redesign — cycle 2 standardized inspection pass

Cycle 2 advanced D2/D3 to [VERIFIED], opened D6, deepened D4/D5, and
surfaced F10-F12. It wrote NO rule-text and committed NO render/paraphrase.

## In scope this cycle

- **Fragmentation — CLEAN.** D2/D3 consolidate the routing principle into
  ONE channel statement (addressing F9's 3-home fragmentation); they do
  not introduce a new duplicate copy. — basis: F9 + D2/D3 (the one-channel
  model replaces the 3 parallel statements with one + cross-refs).
- **Unenforced-rule — OPEN (D4 [PENDING]).** The routing rule's enforcement
  is the live open question; cycle 2 established that a naive
  invocation-scan gate fails (blocks anneal-dev's own implement subagents,
  F11/hook lines 186-209) and that existing enforcement (skill-craft gate +
  release-loop render-fidelity discharge) may already suffice
  (Additive-reflex risk). — basis: D4 + F3 + F11.
- **Missed-dependents — FORWARD NOTE (flagged for impl).** D2's routing-doc
  rewrite + D5's merge will change dependents: the "anneal-dev repo,
  sibling to this one" phrasing (`development-process.md` line 11), and the
  re-render method-definition in `spec/glossary.md` + `spec/modules.md`
  (D1). Enumerated at implement. — basis: D5 coupling-note + D1 scope.
- **Undefined-shorthand — FORWARD NOTE (flagged for impl).** Cycle 2 coins
  "entry-condition", "render-tail", "the channel". If these enter rule-text
  (vs staying tracker-design-narrative), each load-bearing term needs a
  `spec/glossary.md` entry (practice 10). None in rule-text yet. — basis:
  D2 + `development-process.md` practice 10.
- **Over-claimed-verification — CLEAN, with one watch.** D2 [VERIFIED] rests
  on located reads (verify.md 58-67, extensions.md 26-38, §6) — strong.
  F5 [VERIFIED — non-issue] rests on foundation contracts (zero repo-count
  reference) — strong. WATCH: D3 [VERIFIED] rests partly on a FIT-JUDGMENT
  ("derivation fits investigate-design"), softer than a pure located-read;
  basis cites the phase definition but the fit itself is design reasoning.
  Re-test at convergence (falsification pass). — basis: D2/D3/F5 basis fields.

## Out of scope this cycle (cited)

- **Bloat — N/A.** No rule-text added/amended; cycle 2 is design decisions
  + findings (tracker artifacts). — basis: no file edits to corpus.
- **Leakage — N/A.** No rule-text added to a domain-general file. — basis: same.
- **Lossy-render — N/A.** No cycle-2 decision targets a rendered plugin
  clause or commits a source-rule paraphrase; D2/D3/D6 target framework-dev
  routing docs, D4 a hook, D5 repo structure. — basis: D2/D3/D4/D5/D6 targets.
