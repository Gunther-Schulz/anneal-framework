# Run: verify-vs-original-requirements

- **Status:** PASSED
- **Phase:** verify
- **Mode:** interactive (operator-requested session cadence: auto-advance cycles; surface at phase transitions + the method-kernel touchpoints)
- **Task:** Close the verify-vs-original-requirements gap — verify (`core.md` §4.3) checks the locked design, not the original task requirements; a requirement dropped at investigate-design escapes the pipeline (audit-confirmed L9-b, handoff `ac39b6ab6d5d929cd`). Method-kernel edit.

## F-track (findings)

- F1 [PENDING] verify's frame is the **locked design + standardized lenses**, not the original task requirements; the design-completeness audit catches *undesigned* material (scope-overflow etc.), not a *missing* requirement — basis: `core.md:686` read ("verify checks the completed work against the locked design and the standardized lenses") + `:708-720` (design-completeness audit = "material elements not covered by any locked decision"). [→ addressed by D2 when locked]
- F2 [VERIFIED — non-issue] no existing "requirements"/"original-ask" check or artifact in the kernel → the fix is an **ADD**, not an amendment of an existing check — basis: search `grep -rni 'requirement' spec/ foundation.md development-process.md` → every hit is another sense (evidence-bearing-artifact / convergence-cycle / citation / per-step-external-evidence requirement); the sole original-ask mention is `core.md:80` ("The operator's request sets the task").
- F3 [PENDING] the verify contract + design-completeness audit **renders into 3 instance `verify.md`** (anneal-dev `:36-39`, clippy `:34-37`, daneel `:43-51`) + glossary `:138` / modules `:282` + clippy/daneel `post-run-review.md` — the change's dependent set — basis: search `grep -rn 'locked design'` + `grep -rniE 'design-completeness|Planned vs actual'` across spec/ + instances. [→ feeds D1 scope]
- F3 [VERIFIED — addressed] dependent set fed the scope decision D1 (search-established) — basis: D1 records the two re-runnable searches.
- F4 [PENDING] (cycle 2) the requirements-artifact's **tracker home is undetermined** — third track vs header field vs a finding/decision class — needs grounding in `core.md` §5 / §5.1 (tracker structure), not yet read this run. Holds D3 short of [VERIFIED].

## D-track (design decisions)

- D1 [VERIFIED] **Scope** = framework `core.md` §4.3 verify (where the check lands; plus `§4.1` [READY] judgment if the coverage is judged design-side), rendering into `anneal-dev`/`clippy`/`daneel` `phases/verify.md` (Planned-vs-actual) + `glossary.md:138` / `modules.md:282` + clippy/daneel `post-run-review.md` — basis: the two corpus-wide wrap-tolerant searches recorded at F3 (re-runnable: `grep -rn 'locked design' …` and `grep -rniE 'design-completeness|Planned vs actual' …`).
- D2 [OUTLINED] **Fix direction** (committed, thorough-fix shape): capture the **original task requirements as a tracked artifact at investigate-design**, and have **verify check the locked design's coverage of it** — a second verify obligation (intent-correct, ≈ APF "Confirm") complementing the existing planned-vs-actual (fact-correct, ≈ "Callback"). Basis: `core.md:694-697` read (verify is isolated + artifact-driven, works from the tracker + work product + lenses, "need nothing from the run's conversation") → verify can only check requirements-coverage if the requirements live in a *tracked artifact*; an isolated verify cannot re-derive the original ask from conversation. Concrete detail NOT yet investigated (→ cycle 2): where the requirements artifact lives (new tracker element vs scope-decision extension vs a [READY] supporting fact); the evidence-bearing/enforcement form (Unenforced-rule); whether "what counts as a requirement" is an instance slot (practice-9(f)); the new-term/glossary need (practice 10); the no-operator-detection interaction.
  - considered: lens-only route (recurring-requirement lenses) (rejected as *primary*: catches known recurring classes, not an arbitrary dropped requirement; viable as a *complement* for domain-recurring classes)
  - considered: operator-at-[READY] catch (rejected: the framework disclaims operator-detection — `foundations.md` Operator-expected-action bound)
- D2 [VERIFIED] (cycle 2) the **two-channel direction is locked**: (i) capture original requirements as a tracked artifact at investigate-design + (ii) verify checks the locked design's *coverage* of it — concretized by D3+D4 below — basis: `core.md:694-697` (verify isolated/artifact-driven) + `:708-720` (the design-completeness audit is the structural mirror — material-element→classify; requirements-coverage is its dual, requirement→covered/uncovered).
- D3 [PENDING] (cycle 2) **requirements artifact** — the original task requirements enumerated as a tracked artifact at investigate-design (so the isolated verify can check against it) — basis: `core.md:80` (the operator's request sets the task) + `:694-697` (verify works from the tracker). **Detail open (F4):** its tracker home (third track / header / decision-class) needs §5.1 grounding. Instance slot: "what counts as a requirement / how elicited" is domain-bound (practice-9(f) delegation) — parallels the existing work-product / executable-verification slots.
- D4 [PENDING] (cycle 2) **verify requirements-coverage check** — a check in §4.3 beside planned-vs-actual: each captured requirement is checked for coverage by the locked design; an uncovered requirement is a finding → [ISSUES FOUND] (the intent-correct / "Confirm" channel beside fact-correct / "Callback") — basis: `core.md:708-720` (mirror of the design-completeness audit's evidence-bearing finding-shape). Amends verify §4.3 → Missed-dependents renders into the 3 instance `verify.md` (F3/D1). **Detail open:** the coverage-finding's exact shape + enforcement form (cycle 3).
- D5 [CONDITIONAL] (cycle 2) recurring-requirement **lens** as an *optional instance complement* (not primary) for domain-recurring requirement classes — committed recommendation; assumption (operator-resolvable): whether any instance wants it now. Basis: the lens-set is already an instance slot (`modules.md` §2.2 family).
- D6 [PENDING] (cycle 2) new terms ("requirements record", "requirements-coverage check") get `glossary.md` entries in the same change (practice 10) — basis: `development-process.md` practice 10 (new defined-shorthand → glossary entry).

<!-- cycle 3 -->
- D1 [VERIFIED] (cycle 3, scope re-searched — grew to §4.1) scope = `core.md` **§4.1** (requirements capture) + **§4.3** (coverage check) → renders into anneal-dev/clippy/daneel `phases/investigate-design.md` + `phases/verify.md` + `foundations.md` + `glossary.md` + modules — basis: re-search `ls */phases/investigate-design.md` (3 instances present) + `grep -rln 'foundational design decision|operator.s request sets the task'` (the capture-site renders).
  - basis strengthened (cycle 4, falsification `a3ce2e1c8a1d14f59`): the corpus is **multi-repo**; the accurate scope-establishing searches are the cycle-1 `grep -rn 'locked design' spec/ development-process.md anneal-dev/ ../coding-clippy/{spec,plugin} ../daneel/{spec,plugin}` + cycle-3 `ls ../coding-clippy/plugin/skills/*/phases/investigate-design.md ../daneel/… anneal-dev/…` (all 3 instance files present, in sibling repos `coding-clippy`/`daneel` + `anneal-dev/`). The earlier `ls */phases/…→3` was an imprecise recording of these (resolves to 1 from the anneal-framework root); resolution (the scope) unchanged.
- D3 [VERIFIED] (cycle 3) **requirements record** home = a header-adjacent **tracked artifact** captured at investigate-design (the original task requirements enumerated R1..Rn, separating goal from any proposed solution), persisted in the tracker so the isolated verify reads it — basis: `core.md` §5 read (two tracks + header, **no third track**; requirements are task-inputs, not findings/decisions) + `tracker.md` (header = mutable run-state, already carries the task summary) + `core.md:80` (operator's request sets the task; may propose a solution). Instance slot: "what counts as a requirement / how elicited" (practice-9(f), parallels the work-product slot).
- D4 [VERIFIED] (cycle 3) **verify design-coverage-of-requirements check** in §4.3, beside planned-vs-actual: each requirement in the record is checked for coverage by a locked decision; an uncovered requirement is a finding → [ISSUES FOUND] — basis: `core.md:708-720` read (the design-completeness audit's evidence-bearing finding-shape, material-element→classify; this is its dual, requirement→covered/uncovered). The sound, primary leg.
- D6 [VERIFIED] (cycle 3) glossary entries for "requirements record" + "requirements-coverage check" land in the same change — basis: practice 10.
- D8 [CONDITIONAL] (cycle 3) **thorough extension** (committed recommendation): also capture the operator's **verbatim request** as a tracked artifact + verify a **record-vs-request completeness** check (the enumerated record covers the raw request) — closes more of the never-captured residual (F5). Assumption (operator-resolvable at [READY]): whether to include the record-vs-request leg now — it is a **soft-judgment check** (no mechanical predicate; couples to the verify-techniques criteria-first / decomposition findings, `verify-techniques-research` archived) — vs ship the enumerated-only version (D3/D4) and accept F5. considered: enumerated-only (rejected as the *recommendation* per thorough-fix default, but it is the operator's cost call).
- F1 [VERIFIED — addressed] by D4 — citation-equivalence: D4's coverage check observes the same gap F1 observes (verify not checking the original requirements).
- F4 [VERIFIED — addressed] by D3 — the requirements-artifact home is determined (header-adjacent tracked record); §5 read grounded it.
- F5 [VERIFIED — deferred] **honest residual:** the fix catches a *captured-but-uncovered* requirement; a *never-captured* one still escapes — the record's capture-completeness is not checkable by the isolated verify (verify "needs nothing from the run's conversation", `core.md:694-697`; the raw request is conversation). D8 reduces it (verbatim-request capture + a soft record-vs-request check) but leaves a soft-judgment leg. Deferred-trigger: a real run observing a never-captured-requirement escape. Surfaced for the operator's kernel-independent soundness review.

<!-- [READY] → implement transition (operator selected `next phase`, 2026-06-04, resumed session) -->
- D5 [AUTO-ACCEPTED] (operator `next phase`, no override) recurring-requirement **lens** = optional instance complement, available as an instance slot but **not populated in this change** — committed-recommendation default taken; assumption (whether any instance wants it now) unverified, surfaced for post-run review — basis: `modules.md` §2.2 lens-set is already an instance slot (the default adds no rule-text in U1).
- D8 [AUTO-ACCEPTED] (operator `next phase`, no override) **thorough-fix default taken: INCLUDE** — U1 also captures the operator's verbatim request as a tracked artifact + a verify **record-vs-request** completeness check (soft-judgment, no mechanical predicate); assumption (include-now vs enumerated-only) unverified, surfaced for post-run review — basis: thorough-fix recommendation discipline (`foundations.md`); reduces F5's never-captured residual, soft leg remains.
- **[READY] fresh-session implementability result: PASSED** — per-implementer-step external evidence (live re-run this session): `U1 §4.1` capture-rule home grounded at `spec/core.md:760-763` ("## 5. The status-state machine / There are two tracks" → header-adjacent, no third track), edit site `spec/core.md:252` (`### 4.1 investigate-design`) present; `U1 §4.3` coverage-check mirror `spec/core.md:710-711` ("material elements not covered by any locked decision (design-completeness audit)"), edit site `spec/core.md:684` (`### 4.3 verify`) present; `U1 glossary` 2-entry rule `development-process.md:357` ("### 10. New terminology needs a glossary entry"); `U2/U3/U4` renders — 3 instance `{investigate-design,verify}.md` present (`anneal-dev/plugin/skills/anneal-dev/…`, `../coding-clippy/plugin/skills/clippy/…`, `../daneel/plugin/skills/daneel/…`), `grep -rln 'locked design'` returns all three. Convergence cycle 4 clean (zero D-delta; falsification subagent `a3ce2e1c8a1d14f59`, all 5 [VERIFIED] D-entries HOLD).

<!-- implement phase — RE-SHAPED to spec-only (operator-directed 2026-06-04, render-cadence policy: framework runs ship spec-only, renders batch into instance-reinstantiation queue). Active impl plan = one-unit (U1). -->
- **U1 [COMPLETED]** framework source spec edit — checkpoint `fd4b060` on branch `anneal-dev/verify-vs-original-requirements` (container: anneal-framework; persistence reference). Change-set: `spec/core.md` §4.1 (requirements record, D3 + D8 verbatim-request) + §4.3 (requirements-coverage check, D4 + D8 soft record-vs-request leg) + `spec/glossary.md` (2 entries, D6); `modules.md` untouched (D3 home expressed by §3.1 cross-reference, not a header field — within scope-rule conditional). Self-check: 4 write-time lenses clean + change-set-vs-listed-scope clean (dispatched subagent `a8a3e1cb141d2ee4f`); orchestrator integrity check passed (`git diff --name-only` = exactly `spec/core.md` + `spec/glossary.md`, ⊆ U1 listed scope). No loopback.
- **U2/U3/U4 [DEFERRED → batch]** instance renders deferred to the batch re-render (`dev-notes/backlog/instance-reinstantiation.md` Render-debt queue); D1 scope preserved as tracked debt, render *cadence* moved — no [VERIFIED] decision invalidated. backlog policy committed `8de3059`.

<!-- verify phase — isolated (subagent ab5bda2f8056d38e4), spec-only -->
- F6 [VERIFIED — non-issue] (verify) design-completeness-audit candidate: the three→four-checks count + the accounting-sentence "an uncovered requirement" edits are material verify-contract-surface changes — checked for cover by a locked decision — basis: D4 body ("a check in §4.3 beside planned-vs-actual") → the count/accounting are entailed consequences of the D4 bullet-add, covered (not an undesigned element).
- F7 [VERIFIED — non-issue] (verify) `modules.md`-untouched candidate: whether the requirements-record header home needed a `modules.md` edit — basis: `core.md` §4.1 reads "extending the header's one-line task summary (`modules.md` §3.1)" = a cross-reference into the existing `modules.md:200` header spec, not a new rule → no `modules.md` edit required (U1 scope-rule conditional honored).
- **Verify result: [PASSED] (isolated)** — subagent `ab5bda2f8056d38e4`, isolation held. (1) planned-vs-actual + design-completeness audit clean (D3/D4/D6/D8 implemented; D5 adds no rule-text; no uncovered material element); (2) standardized lenses clean (Bloat/Leakage/Unenforced-rule/Undefined-shorthand/Fragmentation/Over-claimed-verification; Missed-dependents N/A — ADD + renders deferred); (3) battery — (a) render-fidelity N/A (renders deferred, cited), (b) coherence RUN clean (cross-refs resolve; three→four count matches bullet count), (c) plugin-skill sweep N/A + **method-kernel skill-craft FORM review RUN clean**. Finding ledger: F1–F5 [VERIFIED], F6/F7 [VERIFIED — non-issue]; no finding short of [VERIFIED].
- Post-verify extension `render-and-open-diff`: DISABLED (`anneal-dev.config/extensions.enabled` commented) → no-op (consistent with the spec-only/batch render-cadence policy).
- **METHOD-KERNEL GATE — still open:** isolated verify [PASSED] + skill-craft form review (clean) done; the **operator soundness review** (methodology-correctness — anneal-dev never self-certifies its own foundation) + **commit approval** remain before ship. Status held IN-PROGRESS until the operator's soundness judgment.

<!-- run complete -->
- **RUN PASSED 2026-06-04.** verify [PASSED] (isolated, subagent `ab5bda2f8056d38e4`) + method-kernel kernel-independent review (skill-craft form-review clean + operator soundness approval "sound, ship — as close as we can get") + operator commit approval. Release: spec-only commit `1d93e58` on `main` (Step-4 discharge in commit body, hook-validated). Renders U2/U3/U4 deferred → `instance-reinstantiation` Render-debt queue (source-delta `1d93e58`). Backlog item archived. Push to `origin/main` pending operator go-ahead (outward-facing).
