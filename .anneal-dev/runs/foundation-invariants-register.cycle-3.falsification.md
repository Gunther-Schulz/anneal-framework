# foundation-invariants-register — cycle 3 falsification pass (subagent a7910d90c28a205be)
Unit scope: [VERIFIED] D-entries at convergence start = D1, D2, D3, D3b, D5, D6.
- D1 — target-dependents, full-search any-outside-scope:{3 files} → FALSIFIED. F1's literal "non-empty only in 3 files" wording does not reproduce: 23 dev-notes/backlog/*.md matches exist. Substantive claim (normative encoding only in the 3 files) survives — all out-of-scope matches non-normative; normative kernel + other repos EMPTY. → loopback: correct F1, re-state D1.
- D2 — target-existence, expected-match → holds (5 invariant homes present at cited locations; validation-watch folder shape confirmed).
- D3 — target-existence, expected-match → holds (§2 + step-4 template insertion points present).
- D3b — target-existence, expected-match → holds (REQUIRED_CHECKS 7 entries + KERNEL_PATTERNS 5 regexes present).
- D5 — target-dependents, any-match → holds (nothing requires the excluded machinery; checker "needs (1)/(2) as stable ground").
- D6 — target-existence, expected-match → holds ("invariant" absent from glossary; validation-watch README def-home precedent confirmed).
