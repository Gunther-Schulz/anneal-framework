# Intent-falsification pass — convergence-machinery-batch, cycle 3 (convergence re-cycle)

Fresh-context subagent (opus, lean brief). Criteria-first from R1–R5. Focus: the D3→D3' amendment;
confirm R1/R3/R4/R5 undisturbed. New surface this cycle: the D3' edit-set-anchored trigger +
the lens-Scope cross-check (lenses.md — all 8 Scopes edit-set-phrased).

## Per-R# attack lines
- **R1** — served. D3' edits only pass-2 trigger; "independent of what the investigation attended
  to" negates priming, does not re-introduce it; D2 deletion undisturbed.
- **R2 (primary, amended)** — **served.** (a) inlined "edit-set (the rule-text added or amended)"
  removes the undefined-"work" ambiguity at point of use; (b) ":350 over what this cycle produced"
  is now subordinate to the explicit edit-set trigger (scopes the increment, not the trigger); (c)
  **all 8 lens Scopes in lenses.md are edit-set/commit-phrased, none attention-phrased** → coverage
  fires on the edit-set regardless of investigation breadth; no residual silent-skip path.
- **R3** — served. D4 (modules §3.4.1 + core §4.1.4 gate) disjoint from core.md:348-350; completeness
  bind untouched.
- **R4** — served. D5 (§4.3 + glossary:428-432) disjoint; single-matrix property independent of the
  lens trigger.
- **R5** — served. D6 (core:597-612) disjoint; different pass (intent vs standardized); D6 stays
  [CONDITIONAL], gated-kernel-closed (F8).

## Per-finding lines
None. The F7-driven amendment closes the R2 residual; no new D-delta forced.

## Verdict: INTENT-CLEAN — no mechanical-falsification-candidate, no forced D-delta.
→ Orchestrator resolves D3' [PENDING]→[VERIFIED] (fresh-context intent confirmation + located-read
basis). Intent clean → mechanical falsification pass RUNS this cycle over [VERIFIED] D-entries.
