# clippy-reinstantiation — cycle 3 standardized-pass artifact

Scope this cycle: design decisions D1'–D14 (per-target render design). One line per in-scope lens; this-cycle basis cited.

- **Lossy-render** — CLEAN. Every re-render decision (D5–D14) commits to *faithful* render with acceptance = render-fidelity verify clean clause-by-clause vs the cited source §§; no decision commits to a softened paraphrase. Design-time softening is the lens's target and none is committed. basis: D5–D14 acceptance-criteria fields (this cycle).
- **Over-claimed-verification** — FINDING F10. D6/D8/D9/D13 are [VERIFIED] but their current targets (implement/verify/tracker/post-run-review) were scope-searched, not fully read at design time. Resolved by framing: the [VERIFIED] claim is "*faithful re-render to current spec §§*", whose completeness is **verify-discharged** (`foundation.md` contract 2 — render fidelity verified by a separate context); the per-decision delta lists are known-major guidance, not an exhaustive design-time enumeration. basis: D5–D14 (acceptance=render-fidelity) + `foundation.md:21-30` contract 2.
- **Missed-dependents** — FINDING F11. The coupling-shape rename (`target-shape/target-uses`→`target-existence/target-dependents`) is an amendment with dependents across clippy; all 7 occurrences fall in D5/D6/D9/D12 target scope, and re-rendering all files together keeps cross-file references coherent. basis: `grep -rniE 'target-shape|target-uses' plugin/skills/clippy/` → 7 (cycle 2) ⊂ D5/D6/D9/D12.
- **Bloat** — CLEAN. The one structural add (D5 Purpose+Mechanism) carries load-bearing orientation (matches anneal-dev foundations; render-completeness); D2/D14 de-bloat REDUCES. basis: D5 (Purpose add), D2/D14 (de-bloat).
- **Fragmentation** — CLEAN. co-producer/emitted-effect appearing in both foundations (D5 basis rule) and lenses (D10/D11) mirrors the framework's own two-place structure (`core.md §3.2.2` + Missed-dependents lens) — faithful, not new duplication. basis: D10 note + `core.md:247-289`.
- **Undefined-shorthand** — CLEAN, contingent on D3. The terms rendered (intent-falsification pass, `[VERIFIED — surfaced]`, co-producer, requirements record) are all defined in `glossary.md`; D3 (citation-firewall) points clippy's coined uses to those glossary definitions. basis: `glossary.md` (terms present) + D3.
- **Leakage** — N/A (out of scope). Clippy is a domain instance; domain concretions (file:line, grep, pytest) are its legitimate binding, not leakage (leakage = domain-general files carrying concretions). basis: `foundation.md` contract 3.
- **Unenforced-rule** — CLEAN at design-time. Faithful re-render preserves source enforcement strength (closed enums, must-verbs); the render-fidelity verify + Lossy-render battery check it on the produced artifact. basis: D5–D14 acceptance.
