# v-entry-is-post-ship-only — cycle 2 convergence (intent-falsification → D-delta)

Dispatched: fresh-context subagent `a8d5c95cac5323cb2` (opus). Criteria-first. Combined intent+mechanical (non-kernel light run); intent produced a D-delta → mechanical SKIPPED.

## Intent artifact (per-R#)
- `{R1, refutation: D2 delta vs live README :42/:128 — does it excise the pre-fix framing + add an exclusion?, outcome: finding (F-A — the exclusion is specified but the lifecycle the WATCHING def sits in is left incoherent)}`
- `{R2, refutation: grep "no fix shipped yet" + read README lifecycle :26-99 + post-run-review Q7 :163-195 for residual pre-fix-WATCHING, outcome: finding (F-A — residual contradiction inside the README's own transition table + Q7)}`
- `{R3, refutation: sampled V-24/26/27/28/30/31 — shipped fix/choice or un-implemented parking? + D2 over-tighten check, outcome: SERVED — all 6 watch shipped fixes/choices; none un-implemented; no over-tightening}`

## Per-finding lines
- `{F-A: re-scoping WATCHING to post-ship collapses the WATCHING↔FIX-SHIPPED two-state machine; D2's scope (blurb + "RESOLVED/INVALIDATED unchanged") leaves WATCHING→FIX-SHIPPED (:79), FIX-SHIPPED-as-birth (:47-58), the n=1 ship-transition (:44-45), and Q7's WATCHING-walk (:169) incoherent → new README-internal contradiction → fails R2. route: [VERIFIED — surfaced] (no coupling-shape reduction — D2's doc-read basis is accurate; it's a design-completeness gap) → ACTIONED as a D-delta}`
- `{F-B: D5/D0 claim a "filed follow-on" migration audit that isn't filed (only the seed item, which says "part of this cycle"); no-silent-deferral gap. route: [VERIFIED — surfaced] → close at implement}`

## Verdict: D-DELTA — NOT converged
D2 [INVALIDATED]→[PENDING] (+ D0/D5 ride the reopen); D1/D3/D4 unaffected (bases sound). Mechanical SKIPPED: intent-delta. The rework must resolve the WATCHING/FIX-SHIPPED boundary under the post-ship definition (α collapse / β re-frame-and-preserve — see tracker). R3 sound (no migration relocation needed). New surfaces cited: README :26-99, :12-24, :100-114; post-run-review Q7 :163-195; V-24/28/30/31; practice-8 :310-320; backlog ls.
