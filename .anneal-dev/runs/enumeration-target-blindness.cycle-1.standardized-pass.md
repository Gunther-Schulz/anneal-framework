# Standardized-pass artifact — enumeration-target-blindness, cycle 1

One line per in-scope lens (this cycle's investigation touched §3.2.2's
completeness-claim clause-family + its glossary/lens-set renderings). Each line
cites this-cycle basis (F#/D# from this cycle, or a this-cycle located read/search).

- **Bloat** — cited-clean (conditional on impl): D3/D4/D5 EXTEND existing clauses
  (no new section); each addition's load-bearing function = naming a search-blind
  form the bare clause misses (F1 behaviors, F2 dependents). Realization is impl-time;
  re-checked at the write-time self-check. basis: F1, F2, D5.
- **Fragmentation** — finding-then-resolved: stating the meta-shape risks 3 copies
  (§3.2.2 / glossary / lens-set). D5 resolves to canonical §3.2.2 + cross-ref
  renderings (glossary/lens-set REFERENCE §3.2.2, the established precedent — they do
  not restate the rule). basis: F3 (renderings cross-ref the closed set), D5.
- **Leakage** — **FINDING F6**: the source items are coding-flavored; `core.md`
  §3.2.2 must phrase both forms domain-generally (effect-inside-replaced-code;
  value-to-a-shared-equality-compared-sink), coding concretions → clippy render
  (render-debt). basis: Leakage scope (domain-general file) + the items' vocabulary.
- **Missed-dependents** — cited-clean: the §3.2.2 contract's own dependents are
  enumerated corpus-wide (D1 scope from the F5 searches); dev-process/instantiation
  excluded as non-dependents; plugin/instance renders queued render-debt. basis: F5, D1.
- **Unenforced-rule** — cited-clean: D3/D4 each carry a mechanical fire-point (grep
  the bodies / grep the sink's writers) + a convergence falsification candidate
  (target-behavior / target-dependents, orchestrator-computed) — the enforcement
  artifact in which violation is visible. basis: D3, D4, R5.
- **Undefined-shorthand** — **FINDING F7**: "co-producer" / "shared equality-compared
  sink" / "side-effect" used load-bearingly; need a single canonical defining sentence
  (inline §3.2.2, glossary cross-ref). basis: Undefined-shorthand scope + D5.
- **Over-claimed-verification** — cited-clean: D1's scope basis is the re-runnable
  corpus greps themselves (distinctive single tokens — wrap-tolerant), covering the
  whole-corpus true unit, not a recalled model. basis: F5 (the executable queries).
- **Lossy-render** — cited out-of-scope this run: no render is committed (renders are
  render-debt, deferred to the batch); the obligation to preserve the teeth at render
  time is carried in D1/D5 (render-debt note). basis: D1 (render-debt partition).

Out-of-scope lenses this cycle: none silently — the eight above are the full core set;
all lined.
