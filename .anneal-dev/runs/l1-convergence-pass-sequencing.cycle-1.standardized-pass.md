# Standardized pass — cycle 1 (l1-convergence-pass-sequencing)

Lenses in scope = those the cycle touched: amendments to `core.md §4.1.4` (sequencing prose +
coverage-check) + `modules.md §3.3/§3.4` (dispatch-brief input + mechanical artifact).

- **Bloat** — cited clean. D2-D5 extend the existing dispatch-brief / artifact / coverage-check;
  no new section, no narration — one field (cited intent-clean verdict) + one coverage-check clause.
  — basis: D2-D5 are amendment deltas to existing constructs (this cycle).
- **Fragmentation / Undefined-shorthand** — FINDING **F1** [PENDING]. "intent-clean verdict" must be
  defined **once** by reference to §3.4.1's clean criterion (no `mechanical-falsification-candidate`
  finding / no D-delta), not restated across D2/D3/D4. — basis: §3.4.1 route enum + `modules.md`:396-397 skip-key read this cycle.
- **Leakage** — cited clean. `core.md`/`modules.md` are domain-general; the edit uses framework vocabulary
  ("intent-clean verdict", "coupling shape", "convergence cycle") with no domain concretion. — basis: D2-D5 targets are framework-general spec sections (this cycle).
- **Missed-dependents** — FINDING **F2** [PENDING]. The mechanical-artifact shape change (D3) renders into
  the phase file (`investigate-design.md` (c) + coverage-check) + `tracker.md` artifact description, and the
  closed-artifact State-summary (`modules.md`:51-52) cites the artifact → enumerate + queue render-follow (D6). — basis: scope greps this cycle (phase-file + tracker.md + State-summary hits).
- **Unenforced-rule** — cited clean (the fix is the *opposite*): D4 gives the sequencing an enforcement
  artifact (cited intent-clean verdict + coverage-check) where it previously had none. — basis: D4 adds the coverage-check clause (this cycle).
- **Lossy-render / Over-claimed-verification** — cited clean, with a render-watch. D4's coverage-check claim
  is accurate (it re-verifies the citation against the same-cycle intent artifact — a re-checkable fact, §3.1
  bind). The render-follow (D6) must preserve the new coverage-check **structurally** (not flatten to prose) —
  watched at the batch render, no render produced this cycle. — basis: D4 verifies against the persisted intent artifact (re-checkable); D6 render queued.

**Pass status:** not clean — F1/F2 [PENDING] (point-of-impl / render guards). No load-bearing finding holds
beyond the guards; D1/D6 [VERIFIED], D2-D5 [PENDING] pending convergence + realization wording.
