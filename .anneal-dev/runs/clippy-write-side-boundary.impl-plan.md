# clippy-write-side-boundary — impl plan
2 units, sequential (U2 ← U1). Disjointness: distinct files but content-dependent (lenses.md renders from lens-set.md).
- U1 — `coding-clippy/spec/lens-set.md`: extend Silent-substitution-at-boundary to BIDIRECTIONAL per D1' (add write-side: overwriting write into a stored sink — SQL UPSERT/EXCLUDED as ONE exemplar + object-store/idempotent PUT + cache last-writer-wins + dict/map assignment + ORM save()-nulling; producer emits non-authoritative value on any branch incl referenced-but-unauthored call sites; clobbers a more-authoritative stored value; fix = COALESCE/value-preserving merge or gate-to-authoritative-branch; scope fires at write/merge site + every producer branch). Keep ONE coherent question (operator-ratified bidirectional tenet); keep distinct from Branch-coverage (F-d). INVOKE skill-craft before the edit (one-coherent-question / form). firewall clean. — first
- U2 — `coding-clippy/plugin/skills/clippy/references/lenses.md:167-187`: re-render the Silent-substitution-at-boundary entry from the U1-edited source (D2'). — after U1
After units: isolated render-fidelity verify (lenses.md faithful to lens-set.md + firewall clean + skill-craft form). Then operator owns release 0.9.95→0.9.96.
