# Cycle 2 — standardized inspection pass (design-lock cycle)

Lenses applied to the design decisions committed this cycle (D1–D5, D7). One line per in-scope lens;
this-cycle basis cited.

- **Fragmentation** — cited-clean (the load-bearing lens; this run's *purpose* is de-fragmentation): the
  design consolidates each duplicated principle to ONE canonical home + cross-refs — triage → `development-process.md`
  practice 1 with glossary/modules indexing (D3); the 3 misfiled terms → the validation-watch README (D4);
  "edit cycle" → `development-process.md:177` (D5). No NEW second copy introduced; the relocations remove copies.
  Basis: D3/D4/D5 (this cycle).
- **Bloat** — cited-clean: the change-set is net-removal (relocate terms, compress drifted restatements to
  cross-refs); the only additions are load-bearing — a one-line "Cycle" disambiguation (D5), a one-line
  glossary pointer to the validation-watch home (D4), the brief per-gap gloss retained as the instance
  interface (D3). Each carries a namable load-bearing function. Basis: D3/D4/D5.
- **Leakage** — cited-clean: edits land in `spec/glossary.md`, `spec/modules.md`, `development-process.md`,
  `post-run-review.md` (methodology-general / dev-process) — no domain (code/clippy/daneel) concretion
  introduced; the relocated terms are methodology-maintenance vocabulary, not a domain. Basis: D4/D5.
- **Missed-dependents** — cited-clean (load-bearing): the changed/relocated contracts carry enumerated
  dependents — validation-watch file→folder refs (F1, corpus-wide grep), the 3 term uses (F2), the triage
  3 homes (F4), "edit cycle" uses (F3). Render dependents (clippy/daneel `investigate-design.md` /
  `post-run-review.md` / `SKILL.md`) are the target-behavior render case → recorded as deferred debt (D1
  spec-only; the batch re-render), not missed. Basis: F1–F4 (re-runnable searches).
- **Lossy-render** — cited-clean: D3 compresses the glossary/modules triage to a brief gloss + cross-ref,
  but the **full machinery (the adherence-gap 3-forms enumeration, the enforcement) stays whole in the
  canonical `development-process.md` practice 1** — the cross-ref points at it; no "must"/enum dropped, the
  glossary keeps the instance-facing gloss. The compression removes a *drifted duplicate*, not enforcement.
  Basis: D3 + `development-process.md:94-122` read (the enforcement is in the retained canonical).
- **Undefined-shorthand** — cited-clean: no new coined term introduced (the run relocates existing terms);
  the relocated 3 terms gain a single canonical home (validation-watch README, D4) — strengthening, not
  weakening, their definedness. Basis: D4.
- **Unenforced-rule** — out of scope this cycle: the run states no new load-bearing *behavioral* rule (it
  consolidates definitions + relocates vocabulary); the validation-watch folder/archival convention is a
  dev-notes hygiene convention, not a spec-enforced AI behavior. No new rule whose violation needs an artifact.
- **Over-claimed-verification** — cited-clean: each [VERIFIED] D-entry's basis is a recorded re-runnable
  search (F1–F4) or a located read (development-process.md:94-122, glossary.md:374-467, modules.md:456-470,
  validation-watch.md preamble) — true-unit-covering, not a proxy; D7's "deferred to Run B" cites the
  Run B scope (citation-equivalence). Basis: the D1–D5 basis fields.

Zero new fragmentation/bloat introduced; the run is consolidation. No load-bearing finding. Clean.
