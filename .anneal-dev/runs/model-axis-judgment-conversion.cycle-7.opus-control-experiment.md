# EXPERIMENT (not a protocol pass): opus-4.8 control intent-falsification on the frozen cycle-6 design state (operator-approved, 2026-07-16)

Setup: contamination-free tracker snapshot (cycle-7 content stripped, assertion-checked);
brief verbatim-identical to the cycle-7 fable dispatch except tracker path + artifact-scope
clause. Reference result: `.cycle-7.intent-falsification.md` (fable-5, same design state).
Cost: opus 100,937 tokens / 12 tool-calls / ~6 min vs fable 125,886 / 24 / ~11 min.

Findings diff (same design state, both blind to each other):
- CAUGHT BY BOTH: enum-token coherence (fable f2 = opus finding-B; opus adds the resolution:
  bare enum member + separate basis note) · D6 backstop asymmetry (fable f5 = opus finding-A).
- FABLE-ONLY (both enforcement-depth class): §3.3 brief-carriage / orchestrator-self-stamp —
  the only MECHANICAL candidate, D2-falsifying; opus's R1 line confirmed the same surface
  "served" · D5 checker-absence (opus R4 accepted "checkable presence" as enforcement).
- OPUS-ONLY angle: inert-by-default (absent `order:` declaration the carve-out never elevates —
  deployment consequence; fable's f4 framed the same cell as composition incoherence only).
- Opus's R6 dependent sweep independently confirmed D8(c6): render instances = clippy + daneel
  (beat-the-books/.daneel located) + self; campaign-craft/bauleitplan no local source repo
  (matrix "locate source first"); copywriting-quill README-match, not a render instance.

Reading: second false-accept-side datapoint for the model axis — same-brief cross-checker
split: opus caught surface/coherence, missed the deep structural-enforcement class incl. the
mechanically-confirmable finding; consistent with the pbs 2026-07-16 split. Also one positive
diverse-arm datapoint (cross-model found an angle the stronger model under-weighted).
Caveats: n=1; opus ran half the tool-calls (effort confound possible).
