# Intent-falsification pass — convergence-machinery-batch, cycle 2 (convergence)

Fresh-context subagent (opus, lean brief). Criteria-first: criteria derived from R1–R5
before reading the design. R6 (run-conduct) not attacked.

## Per-R# attack lines `{R#, attempted-refutation, outcome}`
- **R1** — attacked D2 vs live core.md:345-346; deletion targets the exact priming sentence;
  scope-search confirms no second priming site. **served.**
- **R2** — attacked D3 vs live core.md:348-350; two paths: (1) §4.1.1:413/§4.1.3:458 re-couple →
  REFUTED (both attention-neutral); (2) residual seam — "work" undefined + :350 "over what this
  cycle produced" can read as attention-derived in a design cycle. **finding (→ Finding 1).**
- **R3** — attacked D4 vs routing core.md:616-630 + precedent :548-552; empty-row bind sited at
  convergence-gate level (sibling to `falsified`-holds-phase), not as a 3rd per-finding route →
  no collision with the surfaced route. **served.**
- **R4** — attacked D5 vs verify §4.3 core.md:981-991; §4.3 retains its isolated re-check (one
  matrix, two readers; same artifact shape, not a verdict hand-off). **served.**
- **R5** — attacked D6 vs core.md:597-612; criteria *derivation* shifts to verbatim while per-R#
  *coverage* still iterates the enumeration; the faithfulness attack bridges them; no incoherence.
  **served** (D6 [CONDITIONAL], honest-bounded).

## Per-finding lines `{finding, criterion-attacked, refutation, route}`
- **Finding 1** — D3's word-swap re-anchors but does not self-evidence attention-independence:
  "work" left undefined + retained ":350 over what this cycle produced" can re-couple to attention
  in a design cycle (dominant product = investigation findings). criterion-attacked: **R2
  (soundness-critical)**. refutation: located read core.md:348-350; cross-checked glossary.md:301,
  modules.md:245 (neither defines "work" as the edit-set). route: **[VERIFIED — surfaced]** (pure
  judgment; no coupling-shape reduction — the three D3 reads are accurate, the fragmentation IS
  closed; "does the word suffice" has no runnable predicate).
- **Finding 2** — D6 [CONDITIONAL] is R5's only serving decision; the verbatim-derivation shift (a
  kernel-behavior change) can reach [AUTO-ACCEPTED] rather than forcing operator soundness.
  criterion-attacked: **R5**. refutation: located read D6 tracker line; closed by gated-kernel
  run-conduct (every shipped kernel edit routes through operator soundness regardless).
  route: **[VERIFIED — surfaced]** (designed [CONDITIONAL]→[AUTO-ACCEPTED] contract; soundness
  judgment for the operator).

## Verdict: INTENT-CLEAN (protocol routing) — but orchestrator acts on Finding 1.
No finding routes to `mechanical-falsification-candidate`. Per pure protocol the pass is clean and
both findings are terminal `[VERIFIED — surfaced]`. **Orchestrator decision (working context):**
Finding 1 is the soundness-critical R2 leg; a kernel edit warrants self-evidencing rule-text, so
the working context forms a D-track delta (amend D3 → D3') rather than shipping a clarity-judged
clause. This makes cycle 2 a D-delta cycle → **mechanical falsification SKIPPED this cycle:
intent-delta this cycle (design changed via D3 amendment)**. Finding 2 stays terminal surfaced.
Re-converge at cycle 3.
