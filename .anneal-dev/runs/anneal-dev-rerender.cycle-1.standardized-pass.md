# Standardized-pass — cycle 1

This cycle locked D1-D4 [VERIFIED] from the divergence audit. The change-set is a RENDER
(5 §-cite re-points + 3 breadcrumb strips), no new rule-text. In-scope lenses:

- **Lossy-render** — CLEAN. The re-points (D2) convert a §-number reference to its glossary
  TERM — meaning preserved (the term is the trace); no "must"→"should", no clause dropped. The
  strips (D3) remove dead V-N breadcrumbs with no current source — they carry no rule. — basis: D2, D3.
- **Missed-dependents** — CLEAN-pending-convergence. The change-set is the audit's COMPLETE
  mechanical firewall enumeration (5 hits) + clause-level content audit (3 breadcrumbs). The
  completeness rests on a secondary source (audit `a8dfe52f70ab7d08f`); the cycle-2 convergence
  falsification re-checks it in a fresh context (any 6th firewall hit / any other content
  divergence). — basis: F1, F2.
- **Over-claimed-verification** — FLAGGED → convergence. F2's load-bearing claim — "the heavy
  §4-cleanup items have ~zero render impact (de-dup already mirrored; no clause rewrites)" — rests
  on the audit (secondary source). It is NOT taken as established on the audit alone: the cycle-2
  falsification independently confirms the de-dup mirror + absence of other divergence, and the
  render-fidelity verify (the battery's check (a)) is the final backstop. — basis: F2 + the
  audit-as-secondary-source rule (foundations.md Secondary sources).
- **Bloat / Fragmentation / Leakage / Unenforced-rule / Undefined-shorthand** — N/A this cycle:
  a faithful re-render adds NO new rule-text (re-points + strips only); these lenses fire on added
  rule-text. — basis: D1 (change-set = re-points + strips).

The critical check for this render is render-fidelity (verify battery (a)) + the convergence
falsification of the change-set completeness. Both pending.
