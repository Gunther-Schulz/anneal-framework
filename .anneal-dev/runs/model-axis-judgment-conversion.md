# Run: model-axis-judgment-conversion

- **Status:** IN-PROGRESS
- **Phase:** investigate-design
- **Mode:** auto-battle (gated-kernel campaign cadence: cycles self-advance; the run HALTS
  before release for the operator's step-4 soundness verdict — method-kernel edits never
  self-certify; pattern per run `firewall-regex-wrap-tolerance`)
- **RESUMED (2026-07-17, operator: "lass uns den aktuellen lauf zu ende führen"):** cycle 9
  ran (convergence attempt 4 — not converged: orchestrator-adopted D2 amendment from surfaced
  findings); cycle 10 = D2 re-form; cycle 11 = convergence attempt 5 (two mechanical
  candidates confirmed: D9, D8); cycle 12 = re-forms (D9→[CONDITIONAL], D8, D4/D5/D6 worked
  examples); cycle 13 = convergence attempt 6. Prior-pass artifacts: cycles 3/5/7/9/11/13 +
  the opus control experiment (`.cycle-7.opus-control-experiment.md`).
- **CONVERGED (cycle 13, 2026-07-17):** zero-D-delta convergence observed — intent pass
  clean, findings dispositioned (`.cycle-13.intent-falsification.md`: F30/F31 surfaced-
  terminal; zero mechanical candidates); mechanical pass complete, all 9 [VERIFIED] entries
  hold, orchestrator-recomputed (`.cycle-13.falsification.md`); standardized pass
  (`.cycle-13.standardized-pass.md`). [READY] presented with impl plan (`.impl-plan.md`);
  run HALTED at the operator step-4 gate (gated-kernel — awaiting operator's `next phase` /
  free-form override). D9 [CONDITIONAL] becomes [AUTO-ACCEPTED] on `next phase` unless the
  operator declares the ordering first.
- **Task:** Campaign over two backlog items sharing the verify-machinery locus:
  (1) `verify-model-diversity` — implement operator disposition fork (a) NARROW: verify
  carve-out from the model-tier floor, scoped to the judgment-class leg (intent-falsification):
  checker tier ≥ actor tier / cross-model where available; mechanical/lens legs stay in the
  floor. (2) `judgment-to-mechanical-lens-candidates` — adopt the three judgment→attention
  conversions (domain-claim re-derivation incl. data-beats-commentary; worked-boundary-example
  obligation; closed-set sweep), homed where each becomes mechanical. Method-kernel; live spec
  governs (self-hosting).

## Requirements record (R1..Rn — the goal, separated from any proposed solution)

**Operator verbatim request (this session, 2026-07-16):** "ok was empfhilst du wenn wir es
vernünftig machen wollen?" → [AI recommendation: fork (a) narrow; one gated-kernel campaign
over both items; grooming first] → **"ja, go"**. Underlying disposition context: the PBS
handover brief (`BRIEF-anneal-framework-session.md`, 2026-07-16) + the operator's in-chat
selection of fork (a) narrow, booked `c9230a5`.

- **R1** — The corpus's dispatch model-tier rule carves the **judgment-class check**
  (intent-falsification) out of the same-tier floor: it runs on a checker tier **≥ the actor
  tier** (cross-model where available). Same-tier checking no longer counts as covering the
  judgment class (the 2026-07-16 false-accept datapoint: four judgment-class defects passed
  same-tier verify, fell only cross-tier).
- **R2** — The carve-out is **scoped, not blanket**: mechanical/lens legs stay inside the
  same-tier floor (the datapoint shows attention-class catching is tier-independent).
- **R3** — The **top-tier-actor boundary case** is handled honestly: when no higher tier
  exists, the uncovered judgment residual is **named and visible** (operator-soundness as
  backstop), never silently claimed as covered; no pseudo-diversity construction.
- **R4** — The three **judgment→attention conversions** are adopted so same-tier scaffolding
  catches more of the judgment class: (i) domain-claim re-derivation incl. data-beats-commentary,
  (ii) worked-boundary-example obligation, (iii) closed-set sweep.
- **R5** — Kernel constraints preserved: **no judgment inside the mechanical falsification
  pass** (orchestrator computes verdicts from closed predicates); **no
  operator-detection-dependence** as enforcement; **lens-crowding respected** (no reflexive
  addition to the standardized lens set without naming what it reduces/consolidates).
- **R6** — Every **dependent** of the changed rules is updated or loudly reconciled: framework
  spec, anneal-dev spec, rendered-plugin render-debt (queued, not necessarily shipped),
  `dev-notes/modellwahl-und-anneal.md` rule 3 (explicitly defers its canonical wording to this
  cycle, `f0dd08e`), backlog items. **PBS repos are report-only** (single-writer boundary):
  the final report must flag the pbs supplement-lenses Vorgriff section for deletion by a PBS
  session, never write there.

**Proposed-solution inputs (strong inputs, NOT locked design):** homing priors from the
backlog items — closed-set sweep → mechanical falsification pass; worked-boundary-example →
[READY]-gate obligation; domain-claim re-derivation → falsification-dispatch brief discipline.
Carve-out wording candidate: "checker tier ≥ actor tier". All to be confirmed, sharpened, or
replaced on evidence.

**Fresh-session implementability (end of cycle 2): PASSED** — per-implementer-step evidence:
(1) core.md §4.1.4 intent-pass paragraph → located read core.md:596-621 (the paragraph naming
fresh-context/criteria-first); (2) modules.md §3.4.1 header field → located read
modules.md:345-351 (the mechanical artifact's intent-clean header field, the pattern to mirror);
(3) core.md §3.2.2 closed-set paragraph → located read core.md:302-316 (the behavior-change
paragraph it follows); (4) core.md §5.2 Body-shape (c) → located read core.md:1172-1187;
(5) glossary entries → located read glossary.md:226-247 (the entry to extend + insertion
neighborhood); (6) bindings.md §Dispatch model tier + bootstrap sentence → located read
bindings.md:252-325; (7) config artifacts → F8 reads; (8) modellwahl rule 3 → F6 (f0dd08e diff
carries the passage); (9) render-debt row → `ls dev-notes/backlog/` shows
`instance-reinstantiation.md` (row format read at impl — mechanical append per existing matrix).

**Fresh-session implementability (cycle 13, at [READY]): PASSED** — per-implementer-step
evidence (each step a located read with an observable fact, or an executed query; the
cycle-13 artifacts carry the executions): (1) core.md §4.1.4 intent-pass paragraph →
`spec/core.md:596-621` read, fresh-context/criteria-first named, no capability axis
(mech D2 TE); (2) §4.1.4 coverage-check enumeration → `core.md:554-587`, (i)-(v) mirror
pattern (D2 basis); (3) §3.2.2 → `core.md:247-316`, family lacks a closed-value-set
paragraph (c13 R4 re-derivation); (4) §5.2 Body-shape (c) → `core.md:1176-1178`,
"observable conditions", no example obligation (mech D5 TE); (5) §4.1.1 → `core.md:406-426`,
no §5.2(c) item in the enumeration (mech D5 TE, F31-corrected fact); (6) modules.md §3.4.1
refutation field → `modules.md:483-485` (mech D6 TE) + §3.4 always-include mirror →
`modules.md:405-411` (mech D4 TE); (7) §3.3 brief clause (c) → `modules.md:288-329`,
unit-scope clause carries per-dispatch parameters (mech D2 TE); (8) glossary →
`glossary.md:226-236` entry to extend (mech D7 TE) + collision query empty (mech D7 TD);
(9) bindings §Dispatch model tier + bootstrap → `bindings.md:265-289` + `:312-325`
(mech D3 TE); (10) config artifacts → `anneal-dev.config/model-tier.md` read, value `opus`,
no `order:` line (c13 header); (11) dev-notes → modellwahl rule 3 deferral clause read +
`instance-reinstantiation.md:55-70` per-instance matrix rows (c13 R6). Unit list =
`.impl-plan.md` (6 units, file-disjoint).

## F-track

- F1 [VERIFIED — addressed] (cycle 1) The framework spec carries no harness-model vocabulary — the tier rule is instance-side only (feeds D1). Basis: `rg -n 'model' spec/core.md spec/modules.md spec/glossary.md instantiation-guide.md` → only "the AI's model of it" (core.md:385), "intent-model" (core.md:618), "append-only model" (core.md:883); zero harness-model hits.
- F2 [VERIFIED — addressed] (cycle 1) The dispatch model-tier rule's sole spec home is `anneal-dev/spec/bindings.md` §Dispatch model tier; its dependents are the plugin renders + config + dev-notes (feeds D3, D8). Basis: `rg -ln 'Dispatch model tier|model-tier' --glob '!.anneal-dev' --glob '!dev-notes/backlog'` → anneal-dev/spec/bindings.md, plugin SKILL.md + references/foundations.md, anneal-dev.config/{README.md,model-tier.md}, dev-notes/validation-watch/V-29.
- F3 [VERIFIED — addressed] (cycle 1) INV-3's external anchor warrants the carve-out direction — the edit strengthens, not violates, the registered invariant (feeds the step-4 focusing ledger). Basis: located read `dev-notes/foundation-invariants/INV-3-verify-isolation.md` §External anchor — one fact: it names "a verifier sharing the actor's base model inherits the over-scoring tendency (Panickssery, Bowman & Feng, NeurIPS 2024)".
- F4 [VERIFIED — addressed] (cycle 1) "judgment-class" is established kernel vocabulary; "attention-class" is not (feeds D2/D7 wording: avoid the latter in kernel text). Basis: `rg -n 'judgment-class|attention-class' spec/ anneal-dev/spec/` → 3 hits, all "judgment-class" (core.md:1117, glossary.md:227, glossary.md:367); "attention-class" zero.
- F5 [VERIFIED — addressed] (cycle 1) The render-and-open-diff extension is disabled → plugin render-debt queues to `instance-reinstantiation` per repo precedent (feeds D8). Basis: located read `anneal-dev.config/extensions.enabled` — one fact: the only extension name line is commented (`# render-and-open-diff`).
- F6 [VERIFIED — addressed] (cycle 1) `dev-notes/modellwahl-und-anneal.md` rule 3 explicitly defers its canonical carve-out wording to this cycle (feeds D8). Basis: located read of commit `f0dd08e` message — one fact: "defers the canonical carve-out wording to the verify-model-diversity anneal-dev cycle — this passage is to be re-rendered to that wording once decided."
- F7 [VERIFIED — addressed] (cycle 1) The repo's own model-tier config pins `opus` while the session model is fable-5 — under the current blanket rule this run's judgment checks would run BELOW the actor tier, the exact pattern under repair (feeds D9). Basis: located read `anneal-dev.config/model-tier.md` — one fact: the value line is `opus`.

## D-track

- D1 [VERIFIED] (cycle 1) **Home split**: the checker-capability principle homes in the kernel (core.md §4.1.4 intent-falsification pass + modules.md §3.4.1 artifact header field + glossary entry); the model-tier concretion homes in the instance binding (anneal-dev/spec/bindings.md §Dispatch model tier). Basis: F1 + located read `foundation.md:32-40` — one fact: contract 3 enumerates "dispatch-orchestration mechanics" as instance slot.
  - considered: name model tiers in the kernel directly (rejected: F1 + contract 1 framework-arbitrariness — a harness may not offer model choice at all)
  - considered: instance-binding only, no kernel principle (rejected: the pbs instance independently minted the same rule at corpus level — a kernel-absent principle gets re-derived per instance and drifts; one canonical statement + bindings is the Fragmentation-lens resolution)
- D2 [VERIFIED] (cycle 1) **Kernel principle** — (a) target: core.md §4.1.4 intent-falsification-pass paragraph + modules.md §3.4.1 artifact + glossary; (b) delta: the pass's independence is stated as two-axis — fresh context (existing, required) AND checker capability relative to the actor (the context whose design is attacked); a checker no more capable than the actor reliably covers the mechanically-checkable side but under-detects the judgment class the pass exists to catch; the §3.4.1 artifact gains a per-artifact header field **checker-capability relation**, closed enum {above / diverse / equal-ceiling}, computed under the instance's dispatch binding; a pass dispatched below the binding's required relation is a malformed dispatch; equal-ceiling keeps the judgment residual visible (feeds the operator's soundness pass for method-kernel edits; honest-record-for-review for instance runs — the existing §4.1.4 operator-independence boundary, unchanged); (c) acceptance: §4.1.4 carries the two-axis clause; §3.4.1 header-field spec present with closed enum; no operator-detection enforcement introduced; (d) failure modes: one-model harness always records equal-ceiling (degrades gracefully); (e) basis: F3 (INV-3 anchor) + F4 (vocabulary) + located read core.md:596-621 — one fact: the pass paragraph currently names fresh-context/criteria-first as its only independence levers.
- D3 [VERIFIED] (cycle 1) **Instance binding amendment** — (a) target: anneal-dev/spec/bindings.md §Dispatch model tier; (b) delta: the blanket floor stays for all dispatch kinds; ADD the judgment-check elevation — the intent-falsification dispatch runs at checker tier ≥ the actor tier (the run's working-context model), preferring a strictly higher tier; where the actor is top-tier, cross-model within tier where the harness offers it, else same-model with the §3.4.1 header recording equal-ceiling; the elevation is kind-keyed (all intent-falsification dispatches), NOT a per-dispatch sensitivity judgment — the blanket's no-judgment property is preserved; enforcement stays observable (model parameter on the dispatch, now checked against floor AND kind-keyed elevation); (c) acceptance: bindings section states floor + elevation + equal-ceiling record; config header text remains valid; (d) failure modes: config pinned below session (F7) — elevation is relative to the ACTOR, so it self-adjusts regardless of the pinned floor; (e) basis: operator disposition fork (a) narrow (verbatim in requirements record) + located read bindings.md §Dispatch model tier ("The rule (blanket)" paragraph) — one fact: the current rule names a single configured tier for all dispatch kinds with no relation to the actor's model.
- D4 [VERIFIED] (cycle 1) **Closed-set completeness claim** — (a) target: core.md §3.2.2; (b) delta: add a paragraph — a design decision whose resolution reads, branches on, or renders a **closed value set** carries an implicit completeness claim over the set's members; its basis includes the read of the set's defining source to its visible close (the §3.2 construct rule) PLUS each member's disposition named (handled / loudly not-covered); an unnamed member falsifies (target-behavior/expected-match, mirrored by modules.md §3.4 automatically since candidate sets mirror basis shapes); (c) acceptance: paragraph present; §3.4 needs NO edit (mirroring is generic); (d) failure modes: none new — extends the section's existing implicit-completeness-claim family; (e) basis: located read core.md:247-316 — one fact: §3.2.2's completeness-claim family currently covers replacement/removal/amendment + behavior-change but not closed-set-touching resolutions.
- D5 [VERIFIED] (cycle 1) **Worked-boundary-example obligation** — (a) target: core.md §5.2 Body shape clause (c); (b) delta: for a decision whose resolution carries a formula, predicate, threshold, or boundary-bearing rule, (c) includes at least one worked boundary example — a concrete input at the boundary traced to its outcome; without it the decision cannot reach [VERIFIED] (implementability gap); verify gains a concrete exercisable check for free (the example is executable at §4.3 "Executing the verification"); (c) acceptance: clause present in Body shape; presence checkable per D-entry; (d) failure modes: decisions without any formula/predicate are untouched (trigger-scoped); (e) basis: located read core.md:1172-1187 — one fact: Body-shape (c) currently requires "observable conditions" with no boundary-example obligation.
- D6 [VERIFIED] (cycle 1) **Domain-claim re-derivation (incl. data-beats-commentary)** — (a) target: core.md §4.1.4 intent-pass paragraph + modules.md §3.4.1 refutation-field doc; (b) delta: the attack re-derives every external-truth-binding claim the design rests on (a governing text, an arithmetic or formal relation, an interface contract, a source artifact's own content) from the primary source rather than reading it along from the design; where a source artifact's own content and commentary about it disagree, the content governs; the §3.4.1 refutation field for such an attack cites the primary-source read; (c) acceptance: both clauses present; route closed set unchanged; (d) failure modes: designs binding no external truth are untouched; (e) basis: located read core.md:596-621 + modules.md:483-485 — one fact: the refutation field currently requires "the located read or query that surfaced the concern" without a primary-source requirement for external-truth claims.
- D7 [VERIFIED] (cycle 1) **Glossary** — (a) target: spec/glossary.md; (b) delta: new entry "Checker-capability relation" (definition + closed enum {above/diverse/equal-ceiling} + §4.1.4/§3.4.1 citations); extend the existing "Intent-falsification pass" entry with the two-axis independence sentence; (c) acceptance: entry present, existing entry extended, no second definition anywhere (Undefined-shorthand clean); (d) —; (e) basis: F4 + located read glossary.md:226-236 — one fact: the intent-falsification entry names fresh-context/criteria-first only.
- D8 [VERIFIED] (cycle 1) **Dependents plan** — plugin renders (SKILL.md, phases/investigate-design.md, references/{foundations,tracker,lenses}.md, phases/verify.md) = render-debt queued to `instance-reinstantiation` (F5: extension disabled; repo precedent: renders batch as hygiene); `dev-notes/modellwahl-und-anneal.md` rule 3 re-rendered in-cycle (dev-notes, not rule-corpus-gated; F6); backlog items transition at release; pbs Vorgriff section = report-only flag in the final report (R6 single-writer). Basis: F2 + F5 + F6 + scope searches (`rg -l 'intent-falsification'`, `rg -ln '3\.2\.2'`, `rg -ln 'acceptance criteria|Body shape'` — outputs enumerated in run artifacts).
- D9 [VERIFIED] (cycle 1) **This run's own dispatch models (dogfooding)** — intent-falsification + verify dispatches at fable-5 (= actor/session model; satisfies the current floor AND the being-designed elevation); mechanical-falsification + impl dispatches at opus (the configured floor; decision-complete briefs). Basis: F7 + located read bindings.md §Dispatch model tier "Floor, not guarantee" — one fact: the floor forbids downgrade below the configured tier, permitting above-floor dispatch.
- D10 [VERIFIED] (cycle 1) **Verify dispatch stays under the blanket floor** (not elevated) — the operator disposition is fork (a) NARROW: the elevation covers the intent-falsification kind only; reopen trigger: a false-accept datapoint locating a judgment-class miss specifically in verify's soft-judgment legs (requirements-enumeration faithfulness, §4.3), recorded in `dev-notes/backlog/verify-model-diversity.md` successor notes at release. Basis: requirements record (operator verbatim: fork (a) narrow, scoped to intent-falsification) — one fact: the disposition names the judgment-class leg (intent-falsification) as the carve-out scope.

- F8 [VERIFIED — addressed] (cycle 2) The operator-facing config artifacts state floor-only semantics that the elevation makes incomplete/wrong: `anneal-dev.config/model-tier.md` header claims "every anneal-dev dispatch … runs at this tier" (false under elevation); `anneal-dev.config/README.md` explains the floor only. Contradicts D3's acceptance clause "config header text remains valid" → D3 amended below. Basis: located read `anneal-dev.config/model-tier.md` header — one fact: it enumerates "(investigate/falsification/impl/verify)" as uniformly tier-pinned.
- F9 [VERIFIED — non-issue] (cycle 2) Remaining corpus mentions are citational only, no edit needed: `anneal-dev/spec/persistence.md` (artifact paths/description), `development-process.md:580` (receipt reads `.intent-falsification.md`), `dev-notes/validation-watch/V-29` (observation log). Basis: `rg -n 'intent-falsification' development-process.md anneal-dev/spec/lens-set.md anneal-dev/spec/persistence.md` → path/citation hits only, zero rule-statement hits outside the D1 homes.
- F10 [VERIFIED — addressed] (cycle 2) Potential term collision checked: glossary already uses "capability-boundary" (extensions, glossary.md:527,532) — a distinct concept; D7's new term "Checker-capability relation" must define itself without cross-contamination (impl self-check re-runs the search post-edit). Basis: `rg -n 'checker|capability' spec/glossary.md` — one fact: "capability" hits at lines 527/532 belong to the extensions capability-boundary clauses.
- D3 [INVALIDATED] (cycle 2) — acceptance clause "config header text remains valid" contradicted by F8; reopens per amendment-is-contradiction.
- D3 [PENDING] (cycle 2) — re-forming with expanded targets.
- F11 [VERIFIED — surfaced] (cycle 3) This run's own intent-falsification dispatch ran at fable-5 = actor tier = harness ceiling → **equal-ceiling residual named**: the judgment-class check had no stronger tier available; backstop = this run's gated-kernel step-4 operator soundness verdict. Basis: cycle-3 intent-falsification attack artifact (`.cycle-3.intent-falsification.md`, finding 3).
- F12 [VERIFIED — surfaced] (cycle 3) Enum gap: equal-tier-with-higher-available was permitted-but-invisible under cycle-1 D2/D3 ("preferring" unenforced; no enum member) — the same-tier false-accept class would survive un-named. Basis: attack artifact finding 1 (refutation: modellwahl rule 3 read — the rendered rule is stronger than the design was). Orchestrator response: D2/D3 amendments (cycle 4).
- F13 [VERIFIED — surfaced] (cycle 3) The checker-capability header field had no producer-independent re-derivation (no coverage-check clause-(v) analogue) — residual visibility rested on orchestrator self-report. Basis: attack artifact finding 2 (refutation: core.md:554-587 read — enumeration (i)-(v) closes without it). Orchestrator response: D2 amendment (cycle 4).
- F14 [VERIFIED — surfaced] (cycle 3) D4's mechanical catch was contingent on the decision self-declaring the closed-set claim in its basis; the category-keyed always-include precedent (modules.md:408-411) was not adopted. Basis: attack artifact finding 4. Orchestrator response: D4 amendment (cycle 4).
- F15 [VERIFIED — surfaced] (cycle 3) D5's trigger ("formula/predicate/threshold/boundary-bearing") is a judgment classification; backstop = the next fresh-context intent-pass re-spawn re-reading the design (the §4.1.4 dodge-down backstop shape). Named in D5's failure modes (cycle-4 sub-annotation). Basis: attack artifact finding 5.
- D8 [INVALIDATED] (cycle 3) — mechanical-falsification-candidate CONFIRMED by orchestrator computation: candidate `rg -l 'intent-falsification|convergence cycle' /home/g/dev/Gunther-Schulz/coding-clippy /home/g/dev/Gunther-Schulz/beat-the-books/.daneel`, predicate `any-outside-scope:/home/g/dev/Gunther-Schulz/anneal-framework`, result: 5 files in coding-clippy → falsified (dependents plan was single-repo). Intent-delta this cycle; mechanical pass skipped.
- D8 [PENDING] (cycle 3) — re-forming with all-instance render obligations.
- D3 [VERIFIED] (cycle 2) **Instance binding amendment (re-formed)** — as cycle-1 D3 with expanded targets: (a) targets now also include the two operator-facing config artifacts in this repo (`anneal-dev.config/model-tier.md` header comment; `anneal-dev.config/README.md` explainer sentence) and the bindings.md First-run-bootstrap placeholder-header sentence (the config header's source); (b) delta addition: header/explainer texts state floor + intent-falsification elevation ("…runs at this tier as floor; the intent-falsification dispatch additionally runs at checker tier ≥ the actor tier"); plugin SKILL.md bootstrap text = render-debt (D8); all other cycle-1 D3 content unchanged; (c) acceptance: bindings section + both config artifacts consistent; (e) basis: F8 + cycle-1 D3 basis (unchanged parts).

- D2 [INVALIDATED] (cycle 4) — F12 (enum gap / unenforced preference) + F13 (no producer-independent re-derivation) contradict the recorded resolution.
- D2 [PENDING] (cycle 4) — re-forming.
- D2 [VERIFIED] (cycle 4) **Kernel principle (re-formed)** — as cycle-1 D2 with two strengthenings: (i) the required relation is the **strongest the execution environment offers** relative to the actor, closed ordering **above > diverse > equal-ceiling**; a pass dispatched at a weaker relation than the environment offers is a **malformed dispatch** (replaces the unenforced "preferring"; the permitted-but-invisible equal-below-ceiling class from F12 can no longer legitimately occur, so the closed enum stays three-membered with every occurring member dispositioned); (ii) §4.1.4's coverage check on the intent-artifact return gains a clause mirroring clause (v): the **orchestrator computes** the checker-capability relation from the dispatch's observable model parameter under the instance binding's declared ordering and rejects an artifact whose header field mismatches (producer-independent re-derivation; answers F13). Acceptance updated accordingly; all other cycle-1 D2 content unchanged. Basis: F12 + F13 (attack artifact) + located read core.md:554-587 — one fact: the coverage-check enumeration provides the (v)-style re-derivation pattern the new clause mirrors.
- D3 [INVALIDATED] (cycle 4) — F12 contradicts the "preferring a strictly higher tier" resolution language.
- D3 [PENDING] (cycle 4) — re-forming.
- D3 [VERIFIED] (cycle 4) **Instance binding amendment (re-formed 2)** — as cycle-2 D3 with the elevation language hardened: the intent-falsification dispatch runs at the **strongest checker-capability relation the harness offers relative to the actor** — a strictly higher tier where one exists (above); else a different model within the actor's tier where one exists (diverse); else same-model, recorded equal-ceiling. Same-tier-same-model with a stronger option available is a forbidden downgrade (malformed dispatch, same force as the floor rule). Config-artifact texts carry the same three-step rule in operator-facing words. All other cycle-2 D3 content unchanged. Basis: F12 + cycle-2 D3 basis.
- D8 [VERIFIED] (cycle 4) **Dependents plan (re-formed)** — all-instance render obligations: the kernel deltas (D2/D4/D5/D6/D7) queue as a render-debt row in `dev-notes/backlog/instance-reinstantiation.md` covering EVERY tracked instance that renders the changed machinery — anneal-dev plugin (this repo; F5) AND coding-clippy (confirmed dependent: `rg -l 'intent-falsification|convergence cycle' /home/g/dev/Gunther-Schulz/coding-clippy` → 5 files incl. phases/investigate-design.md, references/tracker.md, references/foundations.md, SKILL.md) AND the other instances the matrix already tracks as owing renders (daneel, campaign-craft, bauleitplan — per instance-reinstantiation.md:61-66 precedent rows). In-cycle edits unchanged from cycle-1 D8 (modellwahl rule 3; backlog transitions; pbs report-only flag). Basis: cycle-3 falsification result (the executed candidate query + any-outside-scope predicate) + located read instance-reinstantiation.md:61-66 — one fact: the matrix's discharged/owing rows are keyed per instance.
- D5 [VERIFIED] (cycle 4) **(corrected entry — failure-mode clause extended)** as cycle-1 D5 with (d) naming the backstop for trigger mis-classification: a decision mis-classed as non-boundary-bearing is re-attackable by the next fresh-context intent-falsification re-spawn (the §4.1.4 dodge-down backstop shape); residual honestly judgment-grade (F15). All other content unchanged. Basis: cycle-1 D5 basis + F15.

- F16 [VERIFIED — surfaced] (cycle 5) Ledger-integrity + design-adequacy: F14's recorded "orchestrator response: D4 amendment (cycle 4)" never landed — no cycle-4 D4 line exists; the closed-set conversion stayed contingent on basis self-declaration. Basis: cycle-5 attack artifact finding 1 (`grep -n "D4" <tracker>` → :84/:100/:112 only). Response: D4 amendment ACTUALLY landed cycle 6 below.
- F17 [VERIFIED — surfaced] (cycle 5) The cycle-4 hardening rested on un-artifacted inputs: no declared model catalogue/tier ordering exists, so "strongest available" was forbidden-but-unverifiable and the relation computation had no declared input; D3's carried enforcement clause over-claimed for the elevation arm. Basis: attack artifact finding 2 (bindings.md:262-264, :287-289 reads; `rg -n 'Checker-capability' spec/ anneal-dev/spec/` → zero). Response: D2/D3 amendments cycle 6.
- F18 [VERIFIED — surfaced] (cycle 5) Spawn-fallback × elevation unreconciled: in-context fallback has no dispatch model parameter, no enum member, and a doubled (isolation + capability) residual nothing named. Basis: attack artifact finding 3 (core.md §4.1.4 spawn-fallback paragraph read). Response: D2 amendment cycle 6.
- F19 [VERIFIED — surfaced] (cycle 5) The diverse arm was silently counted as full coverage: INV-3's anchor is model-keyed, the field datapoint tier-keyed — same-tier-cross-model judgment coverage is evidence-open, yet residual-naming attached only to equal-ceiling. Basis: attack artifact finding 4. Response: D2/D3 amendments cycle 6 (residual visibility keys on relation ≠ above).
- F20 [VERIFIED — surfaced] (cycle 5) D6's re-derivation obligation produced no adherence-distinguishing artifact — read-along and re-derived attacks emit the same citation string (evidence-bearing rule unmet). Basis: attack artifact finding 5 (modules.md §3.4.1 refutation-field doc read). Response: D6 amendment cycle 6.
- D8 [INVALIDATED] (cycle 5) — mechanical candidate CONFIRMED (orchestrator-computed): instance SPEC dispatch-binding sections are D2-contract dependents outside D8's set — `rg -n "Convergence cycle|falsification subagent|Dispatch models" /home/g/dev/Gunther-Schulz/coding-clippy/spec/bindings.md` hits :136-160, outside covered scope → falsified. Intent-delta; mechanical pass skipped cycle 5.
- D8 [PENDING] (cycle 5) — re-forming with dependent KINDS widened.

- D4 [INVALIDATED] (cycle 6) — F16: the promised strengthening was never adopted; the self-declaration contingency (F14) contradicts the "mirroring is generic" acceptance.
- D4 [PENDING] (cycle 6) — re-forming.
- D4 [VERIFIED] (cycle 6) **Closed-set completeness claim (re-formed)** — as cycle-1 D4 PLUS the category-keyed always-include, mirroring the amendment precedent: (a) targets now include `spec/modules.md` §3.4 (one sentence parallel to modules.md:408-411); (b) delta addition: "Decisions whose resolution reads, branches on, or renders a closed value set always include that set's completeness claim in the basis's claimed shapes; the candidate set's closed-set candidate re-runs the member-disposition enumeration against the set's defining source (`expected-match`)" — the mirror no longer depends on the decision self-declaring the shape; (c) acceptance: §3.2.2 paragraph + §3.4 always-include sentence both present. All other cycle-1 content unchanged. Basis: F14 + F16 + located read modules.md:405-411 — one fact: the always-include sentence for amendment/target-dependents is the verbatim pattern mirrored.
- D2 [INVALIDATED] (cycle 6) — F17/F18/F19 contradict the cycle-4 resolution's completeness (undeclared ordering input; fallback unreconciled; diverse-arm residual unnamed).
- D2 [PENDING] (cycle 6) — re-forming.
- D2 [VERIFIED] (cycle 6) **Kernel principle (re-formed 2)** — as cycle-4 D2 PLUS: (iii) the relation is computed from the dispatch's observable model parameter AND the instance's **declared tier-ordering artifact** (instance-bound home; for anneal-dev per D3); (iv) **spawn-fallback**: an in-context pass records the existing "without isolation" marker in place of a relation and the §4.1.4 spawn-fallback sentence names the doubled residual (neither fresh-context nor capability elevation — both waived, never silently); (v) **residual visibility keys on relation ≠ above**: diverse and equal-ceiling both keep the judgment residual visible for review (the diverse arm's coverage is model-diversity-warranted but tier-evidence-open, F19); only `above` counts as the datapoint-warranted full elevation. Enum stays {above/diverse/equal-ceiling}. Basis: F17 + F18 + F19 + cycle-4 basis.
- D3 [INVALIDATED] (cycle 6) — F17: the binding named no ordering input.
- D3 [PENDING] (cycle 6) — re-forming.
- D3 [VERIFIED] (cycle 6) **Instance binding amendment (re-formed 3)** — as cycle-4 D3 PLUS the declared-ordering input: `anneal-dev.config/model-tier.md` format grows an optional **tier-ordering declaration** — tiers listed top-first, each tier a set of one or more model names (ties = same tier), e.g. `order: fable-5 > opus-4.8 > sonnet-5`; the orchestrator computes above/diverse/equal-ceiling from actor + dispatch model + this declaration; relation computation is mechanical GIVEN the declaration, whose truthfulness is operator-owned (same trust class as the floor value — an operator-editable artifact); **ordering absent** → inherit-default: the session/actor model is the assumed ceiling (the same assumption the floor's inherit default already makes), the intent dispatch runs the actor model and records equal-ceiling-under-assumed-ordering; the bootstrap placeholder documents the order line. Enforcement clause re-worded honestly: dispatch parameter + declared ordering are the observable inputs; no offered-model omniscience claimed. Basis: F17 + bindings.md:312-325 read (bootstrap placeholder spec is the format's home) + cycle-4 basis.
- D6 [INVALIDATED] (cycle 6) — F20 contradicts the acceptance (the obligation produced no distinguishing artifact).
- D6 [PENDING] (cycle 6) — re-forming.
- D6 [VERIFIED] (cycle 6) **Domain-claim re-derivation (re-formed)** — as cycle-1 D6 PLUS the evidence-bearing form: the refutation field for an external-truth attack carries the **claimed-fact / independently-re-derived-fact pair** (the design's claimed value stated against the value the attacker re-derived from the primary source); a matching pair evidences the re-derivation, a mismatching pair IS the finding; an attack line on an external-truth-binding claim lacking the pair is malformed. Data-beats-commentary unchanged. Basis: F20 + located read core.md §3.1 (evidence-bearing gradient: the pair makes faking require fabrication) + cycle-1 basis.
- F21 [VERIFIED — surfaced] (cycle 7) Inter-decision incoherence: D3's boundary value "equal-ceiling-under-assumed-ordering" is not a member of D2's closed enum — the within-field-qualifier malformed shape (modules.md:207-210); the assumed-ordering marker's home is an open design decision (implementability gap). Basis: cycle-7 attack artifact finding 2. Response owed: D2/D3 reconciliation (cycle 8, paused for operator).
- F22 [VERIFIED — surfaced] (cycle 7) D5's obligation has no tasked checker — §4.1.1's supporting facts don't enumerate body-shape (c); in-run demonstration: D2/D3 are boundary-bearing yet carry no worked example, and the F23 boundary cell survived six cycles. Basis: attack artifact finding 3 (core.md:406-426 + 1172-1187 reads). Response owed: checker tasking (cycle 8).
- F23 [VERIFIED — surfaced] (cycle 7) Floor × elevation composition undefined in the absent-ordering + pinned-floor-≠-actor cell — this repo's LIVE configuration; D3's inherit-default branch presupposes the tier comparability whose absence defines the branch. Basis: attack artifact finding 4 (bindings.md:265-289 + config read). Response owed: D3 boundary-cell resolution (cycle 8).
- F24 [VERIFIED — surfaced] (cycle 7) D6's trigger classification lacks a named backstop (asymmetric with D5's F15 correction); the generic §4.1.4 fresh-respawn backstop exists but is uncited in D6. Basis: attack artifact finding 5. Response owed: D6 backstop citation (cycle 8, minor).
- D2 [INVALIDATED] (cycle 7) — mechanical candidate CONFIRMED (orchestrator-computed): the §3.3 dispatch-brief schema is a D2-contract dependent outside D2's enumerated targets (`rg -n 'dispatch-brief' spec/core.md spec/modules.md` → modules.md:351 outside scope); without brief-carriage of the relation inputs the header field is orchestrator-self-stamped, defeating the F13 fix. Intent-delta; mechanical pass skipped cycle 7.
- D2 [PENDING] (cycle 7) — re-form owed (cycle 8): target set + §3.3 brief clause; PAUSED — operator requested halt after this cycle.

- D8 [VERIFIED] (cycle 6) **Dependents plan (re-formed 3)** — dependent KINDS widened to three: (a) plugin render trees per instance (render-debt rows in `instance-reinstantiation.md`: anneal-dev plugin + coding-clippy + the matrix's other tracked instances); (b) **instance spec dispatch-binding sections** — each instance's dispatch-model binding gets a reconcile-or-amend obligation queued in the same row (search-established: `rg -ln 'Dispatch models|dispatch model' /home/g/dev/Gunther-Schulz/coding-clippy/spec/` → bindings.md §Dispatch models :136-160; the D2 kernel contract makes instance bindings the relation-computation home); (c) in-repo edits (modellwahl rule 3; config artifacts per D3; backlog transitions; pbs report-only flag in final report). Basis: cycle-5 falsification result + the executed searches (both cited above).
  - basis strengthened (cycle 8): the opus control pass's independent R6 sweep confirmed the dependent set — render instances = clippy + daneel (beat-the-books/.daneel) + self; campaign-craft/bauleitplan without local source repo (matrix "locate source first"); copywriting-quill not a render instance (`.cycle-7.opus-control-experiment.md`).

- F25 [VERIFIED — addressed] (cycle 8) **Opus control experiment** (operator-approved): same-brief opus-4.8 pass on the frozen cycle-6 state — caught 2 of fable's 5 findings + partial third, MISSED both enforcement-depth findings incl. the mechanically-confirmable §3.3 catch; contributed one unique angle (inert-by-default) + the F21 resolution shape. Second false-accept-side datapoint (framework-side); one positive diverse-arm datapoint. Feeds D2/D3 re-forms below + the backlog item at release. Basis: `.cycle-7.opus-control-experiment.md` (diff table; both artifacts cited there).
- D2 [VERIFIED] (cycle 8) **Kernel principle (re-formed 3)** — as cycle-6 D2 PLUS: (vi) target set gains `spec/modules.md` §3.3 dispatch-brief schema: the intent-falsification dispatch's brief clause (c) additionally carries the **relation inputs** — the actor model identity + the instance's declared tier-ordering (or its recorded absence) — so the SUBAGENT stamps the header field from carried inputs and the orchestrator's coverage check re-derives it producer-independently from the dispatch's observable model parameter + the same declaration (completing all three legs of the mirrored structural-bind pattern: brief-carries → artifact-cites → check-re-derives); (vii) F21 resolution (shape per the opus control pass): the header field records the **bare enum member** only; a sibling header field **ordering-basis: declared / assumed** (closed two-member set) carries the assumption state — no within-field qualifier. Basis: cycle-7 falsification result (the §3.3 candidate + predicate, orchestrator-computed) + F21 + F25 + located read modules.md:288-329 — one fact: brief clause (c) is where per-dispatch parameters live.
- D3 [INVALIDATED] (cycle 8) — F23 (composition cell undefined) + opus finding-C (inert-by-default consequence) contradict the cycle-6 inherit-default branch.
- D3 [PENDING] (cycle 8) — re-forming.
- D3 [VERIFIED] (cycle 8) **Instance binding amendment (re-formed 4)** — as cycle-6 D3 with the boundary cell resolved comparison-free: **no cross-comparison between the pinned floor and the actor is ever computed** — non-elevated dispatch kinds run at the configured tier exactly as today (pinned model, or session model when unpinned); the intent-falsification dispatch runs per the three-step relation rule, and **absent a declared ordering** it runs the **actor model itself** (identity satisfies checker ≥ actor with no ranking needed), header = equal-ceiling + ordering-basis: assumed. The carried "self-adjusts regardless of the pinned floor" clause is DROPPED (it presupposed unavailable comparability, F23). **Inert-by-default named openly** (opus finding-C): without an `order:` line the carve-out can never elevate (only equal-ceiling) — the binding and the bootstrap placeholder both state this plainly, and populating the ordering is the named arming step. Impl plan gains a proposed unit for THIS repo: populate `order: fable-5 > opus-4.8 > sonnet-5` in `anneal-dev.config/model-tier.md` (operator-owned artifact — lands only via the operator's step-5 commit approval). Basis: F23 + F25 (opus finding-C) + cycle-6 basis; located read anneal-dev.config/model-tier.md — one fact: no order line exists today.
- D5 [INVALIDATED] (cycle 8) — F22 (no tasked checker) contradicts the "presence checkable" acceptance.
- D5 [PENDING] (cycle 8) — re-forming.
- D5 [VERIFIED] (cycle 8) **Worked-boundary-example obligation (re-formed)** — as cycle-4 D5 PLUS the tasked checkers: (a) targets gain core.md §4.1.1 (the supporting-facts enumeration adds: every [VERIFIED] decision whose resolution is boundary-bearing carries its worked boundary example per §5.2 (c)) and one §4.1.4 sentence (the intent-falsification attack includes, for each boundary-bearing decision, the mechanical presence check of its worked example — fresh-context, so the check is not self-stamped; a missing example routes as a finding); trigger mis-classification backstop unchanged (F15: the re-spawn re-reads the design). Basis: F22 + cycle-4 basis + located read core.md:406-426 — one fact: the supporting-facts enumeration currently ends at embedded target-naming/count premises.
- D6 [VERIFIED] (cycle 8) **(corrected entry — backstop cited)** as cycle-6 D6 with (d) naming the mis-classification backstop symmetrically with D5: a claim the attacker fails to classify as external-truth-binding is re-attackable by the next fresh-context re-spawn (§4.1.4 dodge-down backstop, core.md:640-643); residual judgment-grade, named. All other content unchanged. Basis: F24 + opus finding-A (same catch, both artifacts) + cycle-6 basis.

- F26 [VERIFIED — surfaced] (cycle 9) Post-re-form dogfooding gap: D2's cycle-8 resolution is boundary-bearing (closed relation ordering + malformed-dispatch predicate) yet its body carries no §5.2(c) worked boundary example, and the diverse arm has no concrete input→outcome trace anywhere in the D-track (D3 works only the absent-ordering cell) — under the design's own D5 obligation the next fresh-context pass would flag this design. Basis: cycle-9 attack artifact (`.cycle-9.intent-falsification.md`, per-finding line 1). Orchestrator response: folded into the D2 amendment below (worked examples added at re-form, cycle 10).
- F27 [VERIFIED — surfaced] (cycle 9) Relation-computation totality gap: the closed enum {above/diverse/equal-ceiling} has no named computation outcome for a below-actor dispatch parameter — malformed-dispatch is named as the *verdict* for forbidden downgrades but not as the *computation's* disposition for that input class; F21-shape qualifier recurrence risk at impl (an implementer stamping a fourth token). Basis: cycle-9 attack artifact, per-finding line 2. Orchestrator response: D2 amendment below (below-actor totality clause, cycle 10).
- F28 [VERIFIED — surfaced] (cycle 9) F25's "one positive diverse-arm datapoint" is mis-tiered under the design's own proposed ordering: the opus-4.8 control against a fable-5 actor ran below-actor — none of {above/diverse/equal-ceiling}; it evidences cross-model catching generally, not the diverse (same-tier, different-model) member; carried into the release item it would overstate the diverse arm's evidential base. Basis: cycle-9 attack artifact, per-finding line 3 (external-truth pair cited there). Orchestrator response: F25 corrected below; release-item wording follows the corrected label at impl (D8 (c)).
- F25 [VERIFIED — addressed] (cycle 9, corrected) As cycle-8 F25 with the datapoint re-labeled: the opus control is one positive **below-actor cross-model** datapoint (cross-model catching generally); the **diverse** arm (same-tier, different model) remains tier-evidence-open per D2 (v)/F19. All other content unchanged. Basis: F28 (re-derivation pair in the cycle-9 attack artifact).
- D2 [INVALIDATED] (cycle 9) — amendment adopted in response to F27 (computation totality) + F26 (worked-example self-application); flips per amendment-is-contradiction. Zero mechanical candidates from the cycle-9 pass; the delta is orchestrator-adopted, mechanical pass skipped this cycle (design about to change).
- D2 [PENDING] (cycle 9) — re-forming cycle 10.

- D2 [VERIFIED] (cycle 10) **Kernel principle (re-formed 4)** — as cycle-8 D2 PLUS: (viii) **computation totality**: the relation computation's domain is the kind-keyed elevation's satisfying parameters (checker ≥ actor); a dispatch parameter below the actor yields **no relation** — the coverage check rejects the dispatch as a malformed dispatch (elevation violation) before any header stamping; the enum stays three-membered, the below-actor input class **loudly not-covered** (named disposition, mirroring D4's member-disposition discipline); kernel wording stays tier-abstract; (ix) **worked boundary examples** carried in this decision body per the design's own D5/§5.2(c) obligation, tier-abstract: under declared ordering `T1 > T2` with actor at T2, intent dispatch parameter at T1 → above/declared · actor at T1 with a second distinct model declared tied at T1 → that model → diverse/declared · actor at T1, no ordering declared → the actor model itself → equal-ceiling/assumed (D3's worked cell) · dispatch parameter at T2 under a T1 actor → no relation, malformed dispatch, rejected before stamping. All other cycle-8 content unchanged (targets, acceptance, failure modes, clauses (i)-(vii)). Basis: F26 + F27 + cycle-8 basis.

- D9 [INVALIDATED] (cycle 11) — mechanical-falsification-candidate CONFIRMED (orchestrator-computed): candidate = located read `anneal-dev.config/model-tier.md` + `grep -c 'order:' anneal-dev.config/model-tier.md`, predicate `expected-match:order:` (the floor-satisfaction claim "fable-5 satisfies the pinned opus floor" is computable only from a declared tier-ordering), result: value line `opus`, grep count 0 → **falsified** — the verify-at-fable-5 leg's floor-satisfaction is a silent assumed-ordering comparison; the design's own D3 comparison-free rule (:153) computes no floor-vs-actor comparison, and R3 forbids silent assumed claims. Intent-delta this cycle; mechanical pass skipped.
- D9 [PENDING] (cycle 11) — re-forming cycle 12.
- D8 [INVALIDATED] (cycle 11) — mechanical-falsification-candidate CONFIRMED (orchestrator-computed): candidate = `ls /home/g/dev/Gunther-Schulz/beat-the-books/.daneel` + `find /home/g/dev/Gunther-Schulz -maxdepth 2 -iname '*daneel*'`, predicate `expected-match:skills` (a render-source home carries a skills tree), result: `runs` (sole entry) → **falsified** — the cycle-8 sub-annotation's daneel locator holds run state only; daneel belongs in the locate-source-first class, and a render-debt row keyed to the cited path would point impl at a dead locator. Intent-delta this cycle.
- D8 [PENDING] (cycle 11) — re-forming cycle 12 (locator-class correction; dependent substance unchanged).
- F29 [VERIFIED — surfaced] (cycle 11) Self-application gap, remainder of the F26 class: D4/D5/D6 carry predicate-bearing resolutions with no §5.2(c) worked boundary example (presence-check table in the cycle-11 artifact: D2/D3 PRESENT, D4/D5/D6 MISSING); the F26 fix was scoped to D2 only, not to the search-established class (all boundary-bearing entries). Basis: cycle-11 attack artifact (`.cycle-11.intent-falsification.md`, finding 3 + presence-check table). Orchestrator response: class-complete fix — D4/D5/D6 amendments cycle 12, worked examples drawn from the run's own executed events.

- D9 [CONDITIONAL] (cycle 12) **This run's own dispatch models (re-formed)** — intent-falsification dispatches at fable-5 grounded **comparison-free**: actor-model identity (checker = actor, equal-ceiling + ordering-basis: assumed — D3's worked cell, no ranking needed); mechanical-falsification + impl dispatches at opus = the pinned configured tier **exactly** (comparison-free floor compliance); the verify dispatch at fable-5 rests on the **named assumption** that fable-5 ≥ the pinned opus floor under the undeclared ordering — recorded openly (R3-compliant: named and visible, not silent), operator-resolvable: D3's impl-plan unit proposes declaring `order: fable-5 > opus-4.8 > sonnet-5`, which makes the claim computable; until declared/resolved this entry reaches [AUTO-ACCEPTED] at [READY] for the operator's gate review. Basis: F7 + located read `anneal-dev.config/model-tier.md` (value `opus`, no `order:` line) + D3 (:153) comparison-free rule; assumption named: fable-5 above opus-4.8 (operator-declarable ordering).
- D8 [VERIFIED] (cycle 12) **Dependents plan (re-formed 4)** — as cycle-6 D8 with the daneel locator corrected: daneel IS a render dependent (rendered machinery at plugin cache `daneel/daneel/0.2.50` — `rg -l 'intent-falsification|convergence cycle'` → 3 files: references/closed-artifact.md, references/foundations.md, phases/investigate-design.md) with **no local source repo found** (`find /home/g/dev/Gunther-Schulz -maxdepth 2 -iname '*daneel*'` → only `beat-the-books/.daneel`, run state) → daneel joins the **locate-source-first** class alongside campaign-craft/bauleitplan; its render-debt row keys to the rendered-plugin locator + locate-source-first, never to `beat-the-books/.daneel`. All other cycle-6 content unchanged (three dependent kinds; clippy confirmed local-source; copywriting-quill not a render instance). Basis: cycle-11 finding-2 pair (executed ls/find/rg queries, orchestrator-computed falsification) + cycle-6 basis.
- D4 [INVALIDATED] (cycle 12) — F29 amendment adopted (worked-example self-application); flips per amendment-is-contradiction.
- D4 [PENDING] (cycle 12) — re-forming.
- D4 [VERIFIED] (cycle 12) **Closed-set completeness claim (re-formed 2)** — as cycle-6 D4 PLUS (§5.2(c) worked boundary example, from the run's own executed events): the design's enum {above/diverse/equal-ceiling} as the closed set — defining source read to its close (D2/glossary entry), member dispositions: above = datapoint-warranted full elevation, diverse = residual-visible, equal-ceiling = residual-visible, below-actor input class = loudly not-covered → all named → the closed-set candidate (`expected-match` on each member in the basis) holds; boundary trace: had `diverse` gone unnamed, `expected-match:diverse` on the basis text fails → falsified — the F27→D2(viii) event is the lived instance (an input class initially unnamed, flagged, then dispositioned). All other cycle-6 content unchanged. Basis: F29 + cycle-6 basis.
- D5 [INVALIDATED] (cycle 12) — F29 amendment adopted; flips per amendment-is-contradiction.
- D5 [PENDING] (cycle 12) — re-forming.
- D5 [VERIFIED] (cycle 12) **Worked-boundary-example obligation (re-formed 2)** — as cycle-8 D5 PLUS (§5.2(c) worked boundary example, the run's own executed event): input = cycle-8 D2 (boundary-bearing: closed ordering + malformed-dispatch predicate; body carried NO worked example) → the fresh-context presence check (this obligation's §4.1.4 sentence) → routed as a finding (F26, cycle 9) → fixed at re-form (D2 (ix), cycle 10) → the cycle-11 presence check then computes PRESENT; counter-cell: D3 (boundary-bearing, worked absent-ordering cell in body) → presence check computes PRESENT with no finding. All other cycle-8 content unchanged. Basis: F29 + cycle-8 basis + the F26→D2(ix) ledger sequence (this tracker).
  - basis corrected (cycle 13, basis-only): the §4.1.1 observable fact restated — the supporting-facts enumeration contains no §5.2(c)/worked-example item (re-derived to paragraph close by the cycle-13 pass); the carried cycle-8 phrasing "enumeration currently ends at embedded target-naming/count premises" was imprecise — the enumeration continues through the finding-disposition clauses (F31).
- D6 [INVALIDATED] (cycle 12) — F29 amendment adopted; flips per amendment-is-contradiction.
- D6 [PENDING] (cycle 12) — re-forming.
- D6 [VERIFIED] (cycle 12) **Domain-claim re-derivation (re-formed 2)** — as cycle-6 D6 PLUS (§5.2(c) worked boundary example, both cells from the run's own executed events): MATCH cell — claimed-fact (D3 basis) "the current rule names a single configured tier for all dispatch kinds, no relation to the actor's model" / re-derived-fact from the primary source (`anneal-dev/spec/bindings.md:265-276` read) "Every anneal-dev subagent dispatch — across all dispatch kinds … runs at the configured model tier", zero actor-relative clauses → pair matches → the attack line evidences the re-derivation; MISMATCH cell — claimed-fact (F25 pre-correction) "one positive diverse-arm datapoint" / re-derived-fact: under the proposed ordering `fable-5 > opus-4.8` the opus control ran below-actor, not diverse → pair mismatches → the pair IS the finding (F28, cycle 9). All other cycle-6 content unchanged. Basis: F29 + cycle-6 basis + the F28 ledger event (this tracker).

- F30 [VERIFIED — surfaced] (cycle 13) D10's reopen trigger is boundary-bearing under a contestable classification (cycle-11 pass: not boundary-bearing; cycle-13 pass: contestable) and carries no §5.2(c) discriminating trace (e.g. the F25 framework-side false-accept — located in the intent-falsification leg, not verify — should NOT fire it); classification judgment-grade per the F15 residual, the fresh-respawn backstop functioning as designed. Basis: cycle-13 attack artifact (`.cycle-13.intent-falsification.md`, finding 1 + presence table). Orchestrator disposition: no amendment adopted — two consecutive fresh passes yield only judgment-grade residue on this point and no impl failure mode is named (D10 emits no kernel text; the trigger lands in a backlog successor note at release); gate-visible for the operator's step-4 review.
- F31 [VERIFIED — surfaced] (cycle 13) D5's carried cycle-8 basis phrasing ("enumeration currently ends at embedded target-naming/count premises") is imprecise against live §4.1.1 (the enumeration continues through the finding-disposition clauses); assessed not load-bearing by the pass (the warrant — no §5.2(c) item in the enumeration — re-derives true) and not closed-predicate-reducible. Basis: cycle-13 attack artifact, finding 2 (external-truth pair cited there). Orchestrator response: basis-only sub-annotation under D5 (cycle-12 line) above.
