# foundation-invariants-register — cycle 1 standardized pass

- **Bloat:** clean (by design) — the register's membership rule (D2: anchor-gated;
  an invariant anchorable only to anneal itself is EXCLUDED) is the anti-bloat
  forcing function; no rule-text prose added this cycle (design-formation only).
- **Fragmentation:** FINDING (F3) — the exclusion / loopback / falsification
  principles share one kernel home (the falsification pass, `spec/core.md` §4.1.4);
  the seed set must resolve to distinct externally-anchored entries (Gunter vs
  Platt-recycle vs Quine-Duhem), not duplicate entries pointing at the same home.
  Carried to next cycle.
- **Leakage:** clean — D3 wiring touches `development-process.md` (domain-general),
  carries no domain concretion (no language/tool/path); the register is dev-notes
  working-state, not rendered into instances (F1: contract absent from instances).
- **Missed-dependents:** clean — D1 enumerates the dependents of the verify
  discipline (CLAUDE.md lines 14-20 restatement; run-gate hook reference);
  search-established (F1).
- **Unenforced-rule:** clean (by design) — D3's per-touched-invariant
  holds/violates artifact + D4's git-observable Invariant-change-ratified marker are
  the evidence-bearing/structural enforcement; neither is prose-suggestion-only.
- **Undefined-shorthand:** FINDING (F4) — "foundation invariant" /
  "foundation-invariants register" enter the kernel as load-bearing terms with no
  glossary definition; D6 opened to resolve.
- **Over-claimed-verification:** clean — this cycle's [VERIFIED] tags (D1, D5, F1,
  F2, F5) each cite a re-runnable search or located read; no completeness claim
  rests on recall (D1 scope = the F1 corpus-wide search, not a recalled model).
- **Lossy-render / Over-claimed (render):** N/A this cycle — no plugin render or
  paraphrase committed (render batched per the spec-only cadence policy).
