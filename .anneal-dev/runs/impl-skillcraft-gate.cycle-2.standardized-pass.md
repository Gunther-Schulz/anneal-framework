# impl-skillcraft-gate — cycle 2 (convergence) standardized inspection pass

New surfaces this cycle: implement.md:76-78 (§Dispatch prose), implement.md:193-194/:239 + SKILL.md:76-77 (spawn-fallback parentheticals), spec/core.md §4.2 (framework dispatch). In-scope lenses on the cycle's findings/amendments:

- **Missed-dependents** → **finding (self-corrected this cycle)**: cycle-1 D1 under-enumerated the impl-brief renders (2 named, 3 actual). Now re-attested complete via the falsification pass's newline-flattened wrap-tolerant grep — 3 impl-brief renders enumerated (F5); D1 re-formed. basis: cycle-2.falsification.md D1 target-dependents.
- **Bloat** → clean. basis: D2 re-formed adds the spawn-fallback parenthetical (load-bearing — closes F6) + cross-refs; no restatement.
- **Fragmentation** → clean. basis: D2's cross-ref shape unchanged (practice 5 + skill-craft cross-referenced, not restated).
- **Leakage** → clean (N/A by level — anneal-dev/spec is domain-specific).
- **Unenforced-rule** → clean. basis: enforcement = the pre-edit hook (the gate); now covering the spawn-fallback path too (main-context edit also gated).
- **Undefined-shorthand** → clean. basis: no new coined terms (spawn-fallback, dispatch brief — established).

Out of scope this cycle (impl write-time + verify): Lossy-render, Over-claimed-verification.
