# Intent-falsification artifact — cycle 5 (fresh dispatch a8b75d9c53b9a6e80, model fable-5 = actor → equal-ceiling, header-recorded by subagent + orchestrator-confirmed)

Faithfulness leg: enumeration faithful (verbatim → R1-R6 mapping walked; grooming-first = pre-run, discharged c9230a5).
Per-R# lines: R1 finding · R2 served · R3 finding · R4 finding · R5 served · R6 finding (all six attacked, none unserved).

Per-finding lines (full verbatim in subagent return; substance):
1. F14's promised D4 amendment NEVER LANDED (no cycle-4 D4 ledger line; grep :84/:100/:112) — R4 — [VERIFIED — surfaced] → F16; orchestrator response: D4 amendment (cycle 6, actually landed).
2. Elevation inputs un-artifacted: no declared model catalogue/tier ordering; "strongest available" forbidden-but-unverifiable; D3 enforcement over-claims for the elevation arm — R1/R3 — [VERIFIED — surfaced] → F17; response: D3/D2 amendments (cycle 6, order declaration).
3. Spawn-fallback × elevation unreconciled (no enum member, no model parameter to re-derive from, doubled residual unnamed) — R3 — [VERIFIED — surfaced] → F18; response: D2 amendment (cycle 6).
4. Diverse arm counted as covering with no residual note (INV-3 anchor model-keyed vs datapoint tier-keyed — evidence-open) — R3 — [VERIFIED — surfaced] → F19; response: D2/D3 amendments (cycle 6).
5. D6 re-derivation artifact-indistinguishable from read-along (same citation string either way; evidence-bearing rule unmet) — R4 — [VERIFIED — surfaced] → F20; response: D6 amendment (cycle 6, claimed-vs-rederived pair).
6. Instance SPEC dispatch-binding sections are D2-contract dependents outside D8's set: coding-clippy/spec/bindings.md:136-160 §Dispatch models (zero checker/relation/actor language; verify class covers the convergence falsification subagent) — R6 — mechanical-falsification-candidate on D8: {shape: target-dependents, candidate: rg -n "Convergence cycle|falsification subagent|Dispatch models" /home/g/dev/Gunther-Schulz/coding-clippy/spec/bindings.md (executed, hits :136-160), predicate: any-outside-scope:<anneal-framework/** + queued plugin render trees>}
   ORCHESTRATOR-COMPUTED: hit is an instance SPEC section outside the covered scope → FALSIFIED → D8 [INVALIDATED]→[PENDING]. INTENT-DELTA THIS CYCLE.

mechanical skipped: intent-delta this cycle.
