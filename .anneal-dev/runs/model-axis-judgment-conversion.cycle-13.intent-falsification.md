# Intent-falsification-pass artifact — cycle 13

- run: `model-axis-judgment-conversion`
- cycle: 13 (convergence attempt 6)
- dispatch model: fable-5
- checker-capability relation: equal-ceiling
- ordering-basis: assumed
  - relation inputs verified by located read: `anneal-dev.config/model-tier.md` — value line `opus`; `grep -c 'order:'` semantics confirmed by read: no `order:` line exists (comment lines + value `opus` only). Actor = fable-5 (run working context); dispatch model = fable-5 → checker = actor, identity, no ranking computed.
- criteria source: requirements record read FIRST (tracker :21–69 only), criteria C1–C6 derived from R1..R6 + verbatim request before any D-track read.
- verbatim-faithfulness leg (live core.md §4.1.4, self-hosting: live spec governs): verbatim = "ok was empfhilst du wenn wir es vernünftig machen wollen?" → AI rec "fork (a) narrow; one gated-kernel campaign over both items; grooming first" → "ja, go". Mapping: fork (a) narrow → R1/R2 (+D10 scope); "both items" = `verify-model-diversity` + `judgment-to-mechanical-lens-candidates` (both exist: `ls dev-notes/backlog/`) → R1–R3 / R4; gated-kernel constraints → R5; dependents/single-writer → R6; "grooming first" = pre-run act, correctly not enumerated. **No dropped verbatim requirement found — enumeration faithful.**

## Per-R# attack lines

`{R#, serving-decisions, attempted-refutation, outcome}`

- {R1, serving-decisions: D1 D2 D3 D7, attempted-refutation: re-derived the current rule from live `anneal-dev/spec/bindings.md` §Dispatch model tier ("The rule (blanket)": "Every anneal-dev subagent dispatch — across **all** dispatch kinds … runs at the configured model tier"; zero actor-relative clauses) and live `spec/core.md` §4.1.4 intent-pass paragraph (independence levers named: fresh-context + criteria-first/verbatim + faithfulness leg; NO checker-capability axis) — so the carve-out is genuinely absent today and D2/D3 supply exactly it; attacked whether same-tier could still silently count as judgment coverage: D2 (v) keys residual visibility on relation ≠ above (diverse AND equal-ceiling stay residual-visible; only `above` is datapoint-warranted full elevation), outcome: served}
- {R2, serving-decisions: D3 D9 D10, attempted-refutation: swept the D-track for any elevation leaking beyond the intent-falsification kind — D3 keeps the blanket floor for all dispatch kinds and keys the elevation to the kind (not per-dispatch sensitivity, preserving the blanket's no-judgment property re-derived from bindings.md "not a per-dispatch 'is this one sensitive?' judgment"); D9 runs mechanical+impl at the pinned tier exactly; D10 commits verify to the floor with a named reopen trigger; the three R4 conversions change no dispatch tier (they are §3.2.2/§5.2/§4.1.4/§3.4 scaffolding), outcome: served}
- {R3, serving-decisions: D2 D3 D9, attempted-refutation: hunted silent assumed-ordering comparisons and pseudo-diversity — D3's absent-ordering branch is comparison-free (actor-model identity; equal-ceiling + ordering-basis: assumed as sibling field, no within-field qualifier), inert-by-default named openly with the arming step (populate `order:`) as an operator-owned impl unit; D2 (iv) names the spawn-fallback's doubled residual, (v) refuses full-coverage credit to `diverse`, (viii) makes below-actor loudly not-covered; external-truth pair on the corrected datapoint — claimed (F25 corrected): "one positive below-actor cross-model datapoint" / re-derived: opus-4.8 control vs fable-5 actor under the proposed `fable-5 > opus-4.8` ordering ran below-actor, not diverse → pair matches (F28's correction holds, no diverse-arm overstatement remains); D9's verify-at-fable-5 assumption is explicitly named and operator-resolvable, outcome: served}
- {R4, serving-decisions: D4 D5 D6 (+ D2 (vi) brief-carriage), attempted-refutation: re-derived each conversion's absence from the live spec — §3.2.2 read to section close: completeness-claim family (replacement/amendment + non-reference structural dependents + co-producer + behavior-change) contains NO closed-value-set clause (D4 adds it + §3.4 always-include mirroring the live always-include sentence at modules.md ~:408); §5.2 Body-shape (c) read: "acceptance criteria — observable conditions", no boundary-example obligation (D5 adds it + tasked checkers in §4.1.1/§4.1.4); modules.md §3.4.1 refutation field read: "the located read or query that surfaced the concern", no primary-source pair requirement (D6 adds the claimed-fact/re-derived-fact pair; data-beats-commentary present in the carried delta); all three land as mechanical/attention-class forms (expected-match candidate, presence check, pair form) so same-tier scaffolding catches them; self-application attack found one residual gap (finding 1 below) and one basis-wording drift (finding 2 below), outcome: finding}
- {R5, serving-decisions: D2 D4 D5 D1, attempted-refutation: (no-judgment-in-mechanical) D4's closed-set candidate is expected-match per member, orchestrator-computed; D2 (viii) rejects below-actor by computation before stamping; relation computation is mechanical given the declared ordering (operator-owned input, same trust class as the floor value); (no-operator-detection) re-read live §4.1.4 operator-independence boundary — D2's residual visibility feeds the step-4 soundness verdict via the permitted menu-selection path, unchanged; D2 acceptance carries "no operator-detection enforcement introduced"; (lens-crowding) swept the D-track targets: no decision touches `lenses.md`/lens-set — the conversions land as kernel machinery, and the pbs supplement-lens Vorgriff (independently located via modellwahl rule 4: `pbs-office/anneal-dev.config/lenses.md`) is flagged for deletion = a named consolidation, not an addition, outcome: served}
- {R6, serving-decisions: D8 D3 D7 D2, attempted-refutation: re-ran the dependent sweeps — `rg -ln 'model-tier|Dispatch model tier|dispatch model' --glob '!.anneal-dev' .` → every hit maps to a covered class (bindings.md/config = D3; plugin render trees = D8 render-debt; briefs = report-class session snapshots, verified by read of `2026-07-17-pbs-handover-3-design-grenze.md` head: "ich schreibe nicht ins anneal-framework-Repo", citational; validation-watch/archive = observation logs; backlog items = D8 (c) transitions); `rg -ln 'intent-falsification'` outside the D1 homes → plugin renders (D8) + `persistence.md`/`development-process.md:580` citational (F9 re-confirmed by read: :580 is a receipt path) + backlog/validation-watch observational; modellwahl rule 3 re-read: carries the deferral clause verbatim ("die kanonische Carve-out-Form entscheidet der anneal-dev-Zyklus … danach diese Passage auf dessen Formulierung ziehen") = D8's in-cycle re-render obligation aimed correctly; both campaign backlog items exist on disk; `instance-reinstantiation.md` :55-70 read — matrix keys per instance, clippy/daneel/campaign-craft/bauleitplan named as still owing = D8's row precedent holds; daneel locator corrected per D8 re-form 4 (locate-source-first), outcome: served}

## Per-finding lines

`{finding, criterion-attacked, refutation, route}`

- {finding: D10's reopen trigger ("a false-accept datapoint locating a judgment-class miss specifically in verify's soft-judgment legs") is a classification predicate over future datapoints, arguably boundary-bearing under D5's own trigger ("formula, predicate, threshold, or boundary-bearing rule"), yet D10's body carries no §5.2(c) worked boundary example — e.g. no traced cell showing whether the F25 opus-control datapoint (a framework-side false-accept located in the intent-falsification leg, not verify) fires the trigger (it should not — a discriminating trace would show that); the F29 class-complete fix's presence table covered D2–D6 only, implicitly classifying D10 as non-boundary-bearing without a recorded disposition; classification is judgment-grade (the F15 residual — this re-spawn is its named backstop, functioning as designed), criterion-attacked: R4(ii) (the worked-boundary-example obligation's self-application; secondarily R3's honest-residual discipline), refutation: located read tracker :94 (D10 latest line — resolution + trigger + basis, no traced input→outcome cell) + the presence-check table below, route: [VERIFIED — surfaced]}
- {finding: D5's cycle-8 basis records the observable fact "the supporting-facts enumeration currently ends at embedded target-naming/count premises" — mismatched against the live spec; external-truth pair per D6's own discipline: claimed-fact "enumeration ends at embedded target-naming/count premises" / independently-re-derived-fact (live `spec/core.md` §4.1.1 read to paragraph close): the enumeration continues past that member — "…premises carry a re-runnable basis (per §3.2…); **no finding is left open.** The convergence cycle's intent-falsification findings (§4.1.4) must be **dispositioned**…" → pair mismatches; NOT load-bearing (operational test: the decision's warrant is the absence of a §5.2(c)/worked-example item in the enumeration, which the re-derived read confirms — no D-entry or [READY] fact changes), and no closed-predicate reduction is expressible ("contains material after X" is not formattable as any-match / any-outside-scope / expected-match against the basis), so no mechanical candidate is named; suggested handling: corrected basis wording as basis-only refinement at the orchestrator's discretion, criterion-attacked: R4(ii) (the D5 decision's recorded basis accuracy), refutation: located read live `spec/core.md` §4.1.1 (the [READY] judgment-criteria paragraph, read to its close) vs tracker :156 (D5 cycle-8 basis), route: [VERIFIED — surfaced]}

## D5-obligation presence check (worked boundary example per boundary-bearing [VERIFIED] decision, §5.2(c))

Mechanical presence check, per the design's own D5 §4.1.4 sentence (fresh-context, not self-stamped):

| D# | boundary-bearing? | worked example in body | result |
|----|-------------------|------------------------|--------|
| D1 (:83) | no (home split; no formula/predicate/threshold) | — | n/a |
| D2 (:166) | yes (closed ordering, malformed-dispatch predicate, relation computation) | clause (ix): four traced cells incl. below-actor rejection | PRESENT |
| D3 (:153) | yes (three-step relation rule; absent-ordering branch) | absent-ordering cell traced (actor-model identity → equal-ceiling + ordering-basis: assumed); inert-by-default consequence named | PRESENT |
| D4 (:178) | yes (expected-match closed-set candidate) | enum-member dispositions + "had `diverse` gone unnamed" falsification trace (F27→D2(viii) lived instance) | PRESENT |
| D5 (:181) | yes (presence-check obligation) | F26→D2(ix) full trace + D3 counter-cell | PRESENT |
| D6 (:184) | yes (pair match/mismatch predicate) | MATCH cell (D3/bindings pair — independently re-derived this pass, matches) + MISMATCH cell (F28) | PRESENT |
| D7 (:91) | no (glossary entry additions) | — | n/a |
| D8 (:175) | no (dependents enumeration; locate-source-first membership is search-established, not a predicate over future inputs) | — | n/a |
| D9 (:174) | [CONDITIONAL] — outside the obligation's [VERIFIED] scope; body nonetheless carries per-dispatch traced cells (fable-5 intent = equal-ceiling/assumed; opus mech+impl = pinned exact; verify = named assumption) | — | n/a (noted) |
| D10 (:94) | judgment-contestable (reopen-trigger predicate) | none | MISSING under boundary-bearing classification → finding 1 |

## D9 [CONDITIONAL] attack (committed recommendation incl. assumption honesty)

Attacked as the committed recommendation it is: the named assumption ("fable-5 ≥ the pinned opus floor under the undeclared ordering") is the ONLY assumption in the body — re-checked each leg: intent dispatches at fable-5 rest on actor identity (no assumption); mechanical+impl at opus rest on pinned-tier equality (no comparison); only the verify leg rests on the ordering assumption, and it is named, R3-compliant, operator-resolvable via the proposed `order:` impl unit, correctly held [CONDITIONAL] → [AUTO-ACCEPTED] at [READY]. This cycle-13 dispatch itself ran fable-5 = actor (consistent with the recommendation; header stamps above are its worked instance). No finding.

## Summary

R1 served · R2 served · R3 served · R4 finding (2 findings, both [VERIFIED — surfaced]) · R5 served · R6 served. Zero `mechanical-falsification-candidate` routes — no D-track delta from this pass. Verbatim-faithfulness leg: no dropped requirement.
