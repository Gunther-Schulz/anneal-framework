# anneal-dev run: campaign3-enforcement-fidelity

## Header
- Status: PASSED — **RUN COMPLETE. Released `014b7b0` (main, spec-only; pushed; step-4 discharge validated by the commit-msg hook). Operator soundness SOUND→ship 2026-06-05.** D3.1 (core.md §5.1 (c)) shipped; D1 closed (completeness-search archived); D2 watch-as-backlog (basis-query stays); deferred-finding archived (shipped). 3 spawned items filed. §5.1 render-debt → ⑥.
- Phase: verify [PASSED] → released
- Mode: interactive + run-until-(n) cadence (operator present; auto-cycles between [READY] and the method-kernel step-4 soundness verdict, where the operator halts; open [CONDITIONAL]s surface at [READY] for operator resolution, never silent [AUTO-ACCEPTED]). NOTE: this is the candidate-(b) affordance (`dev-notes/backlog/auto-battle-cadence-mode.md`) = **interactive**, NOT auto-battle (prior runs mislabeled it "auto-battle (cadence variant)"; the operator IS present resolving the two forks at [READY]).
- Task: Campaign ③ first run — the **enforcement-fidelity & disposition** bundle: are the basis-rule's recorded-search/query fidelity disciplines (wrap-tolerance, verbatim-query) and the deferred-finding disposition under-enforced, and if so what structural form earns its place? (method-kernel edit — `spec/core.md` §5.1 + §4.1.4/`modules.md` §3.3/§3.4 dispatch-brief; kernel-independent verify = skill-craft self-review + operator soundness + foundation-invariant register check.)

### Requirements record
**Operator verbatim request:** "pickup from backlog readme" → campaign ③ (the settled-order next campaign after ② shipped); "ok then lets start" with the standing run-until-(n) cadence (auto-cycle, halt at [READY] + step-4). Three of campaign ③'s six items form this bundle: `completeness-search-enforcement` (Fork A), `basis-recorded-query-fidelity` (Fork B), `deferred-finding-owed-artifact-forcing-function`. (The other three — `core-md-bloat-measure-then-cut`, `verified-integrity-consolidation`, `surface-non-task-observations` — are separate later runs: core-md-bloat needs a fresh re-measure post-`f74b145`; the other two carry undesigned mechanisms.)

**Goals (R#, separated from proposed solution):**
- R1 — a load-bearing completeness search that silently under-counts (a multi-word pattern split across a prose-wrap newline; an over-clever boundary regex) does not produce a false completeness claim that ships uncaught. [completeness-search-enforcement]
- R2 — a recorded basis whose cited query is a paraphrase/reconstruction (not the verbatim re-runnable query) does not degrade the audit trail such that a future re-check cannot rely on the cited query as written. [basis-recorded-query-fidelity]
- R3 — a finding deferred to a *to-be-authored* artifact (whose authoring IS the discharge — canonically a validation-watch V-entry) does not let that owed artifact silently never come to exist. [deferred-finding-owed-artifact-forcing-function]

**Proposed solutions (operator decide-ahead — design premises, grounded in investigation, not assumed because proposed):**
- Fork A (→R1): **close as "safety-net adequate"** (the search-time discipline already exists — practice 4 for audits; the convergence falsification pass as the completeness-claim safety-net — and both historical trips were caught) **+ one Pareto harden**: mandate wrap-tolerance on the *falsification* dispatch's own candidate searches (§4.1.4 / `modules.md` §3.3/§3.4), the one search-context practice 4 does NOT already cover, closing the "safety-net depends on its own un-enforced wrap-tolerance" hole. [CONDITIONAL — operator: close+harden (rec) / pure-close / mandate-search-time]
- Fork B (→R2): **n=1 → convert to a validation-watch V-entry** (correctness-watch; catcher = the convergence coverage-check; closes if a recorded-but-false-as-written query ever passes the mechanical check, or n≥2). Soundness is preserved by the separate-checker re-deriving the conclusion; only the audit-trail degrades → practice-7 no-over-build at n=1. [CONDITIONAL — operator: V-entry (rec) / build forcing-function-(b) now]
- R3: **structural fix, earns n=1** — `core.md` §5.1 finding-state #3 `deferred` gains a third sub-shape (c) *deferred-pending-authoring*: where the deferral's discharge is the authoring of an artifact (the trigger lives in that artifact), the artifact must **exist** — basis cites its path, checked present — before the finding records `[VERIFIED — deferred to <path>]`. The artifact's existence is the un-fakeable evidence (faking it = authoring it = the work). Domain-general (the artifact kind is instance-defined; for framework-dev it's a V-entry). NOT a fork — classifiable structural enforcement per practice 8.

## F-track
<!-- findings appended below, append-only, fixed-shape ledger lines -->

### Cycle 1 — investigation pass (current-state findings)
- F1 [PENDING] (→R1) The domain-general basis rule does NOT mandate wrap-tolerance: `core.md` §3.2.1 true-unit says "an exhaustive search recorded as the re-runnable search itself," and §4.1 Scope says "an exhaustive search … not the AI's model of it" — neither carries "wrap-tolerant." Wrap-tolerance lives at `development-process.md` practice 4 (the dev-process/audit home, correctly domain-specific). basis: read `core.md` §3.2.1 (216–228) + §4.1 Scope (340–349) — observable fact: neither range contains "wrap-tolerant"; read `development-process.md` practice 4 (161–167) — "made **wrap-tolerant** … a bare multi-word line-grep is an incomplete audit, and dispatched audit subagents carry the same."
- F2 [PENDING] (→R1) practice 4 ALREADY mandates wrap-tolerance for contract-change audits AND dispatched audit subagents — so trip-2 (the S3-run bare multi-word line-greps) is an **adherence gap** against an existing unambiguous evidence-bearing rule, not a spec gap (practice 1 underspecification test: the rule names the violating form "an incomplete audit"). basis: read `development-process.md` practice 4 (163–167, "a bare multi-word line-grep is an incomplete audit") + `completeness-search-enforcement.md` finding-2 (trip-2 = the rule not followed, no actual miss).
- F3 [PENDING] (→R1) The safety net binds: the convergence mechanical-falsification pass (§4.1.4) re-derives each completeness claim's conclusion via a **fresh-context separate-checker subagent running its OWN candidate searches**, so an imprecise recorded query doesn't corrupt the verdict; both historical trips were caught (trip-1 §4.2 under-count by the convergence pass F7; trip-2 by the operator). basis: read `core.md` §4.1.4 (481–546, the mechanical pass + coverage-check re-derives independently) + `completeness-search-enforcement.md` ("the safety net already works").
- F4 [PENDING] (→R1, the one real gap) The *falsification* dispatch's candidate searches are NOT mandated wrap-tolerant: `modules.md` §3.3 brief (c)/(d) and §3.4 candidate form say "an executable query … Form follows §3.2," and §3.2 carries no wrap-tolerance — so the safety-net's OWN searches can silently under-count, the "safety net depends on its own un-enforced instruction" hole. This is the search-context practice 4 does NOT cover (practice 4 = contract-change audits; this = convergence falsification). basis: read `modules.md` §3.3 (288–329) + §3.4 candidate (361–366) — observable fact: "Form follows §3.2," no "wrap-tolerant"; read `core.md` §3.2 (171–202) — no "wrap-tolerant."
- F5 [PENDING] (→R2) The convergence coverage-check (§4.1.4 clauses i–v) verifies a basis's **conclusion** (the subagent re-derives via its own candidate + predicate + result) but does NOT verify **recorded-query fidelity** — whether the AI's cited query, re-run as written, reproduces the claimed result. A reconstructed-but-conclusion-true basis passes the mechanical check. basis: read `core.md` §4.1.4 Coverage check (513–546) — observable fact: clauses (i)–(v) check candidate-set/predicate/result/aggregate/intent-clean, none checks the entry's recorded query re-runs as written.
- F6 [PENDING] (→R2) Soundness is preserved (the separate-checker re-derives the conclusion independently → an imprecise recorded query never corrupts the verdict); only the audit trail degrades (a future reader can't rely on the cited query as written). n=1 (the `verify-vs-original-requirements` D1 multi-repo glob `ls */phases/investigate-design.md → 3` that resolves to 1 from repo root). basis: read `basis-recorded-query-fidelity.md` (the soundness-vs-audit-trail analysis + n=1 origin run D1).
- F7 [PENDING] (→R3) `core.md` §5.1 finding-state #3 `deferred` enumerates exactly two sub-cases — (a) re-observes a gap an existing [AUTO-ACCEPTED] decision deferred (cites that decision's tracker ID), (b) operator-pull defer citing a trigger condition (a named observable event class) — neither of which requires that an owed, *to-be-authored* artifact EXIST. The deferred-to-a-V-entry-I-will-author shape is a third sub-shape uncovered. basis: read `core.md` §5.1 (1018–1029) — observable fact: cases (a)/(b) enumerated, no owed-artifact-existence requirement.
- F8 [PENDING] (→R3) The owed artifact's existence is the natural evidence-bearing artifact (faking `[VERIFIED — deferred to <path>]` requires authoring `<path>` = the work); the originating incident is the `foundation-invariants-register` run's F13 (deferred to a V-entry "to be authored at implement" that was never authored → slipped to persist and was lost; verify's design-completeness audit caught it as F-DC1 — a backstop, not the source). Classifiable as structural enforcement (practice 8), earns n=1. basis: read `deferred-finding-owed-artifact-forcing-function.md` (F-DC1 origin + the candidate-fix classification).

### Cycle 1 — investigation pass continued (scope-establishing search; the decisive Fork-A finding)
- F9 [VERIFIED — non-issue] (→R1, CLOSES the F4 harden) The F4 gap is already closed: `anneal-dev/spec/bindings.md` binds the framework's `search` (`core.md` §3.2) to "**a wrap-tolerant** text search across the whole corpus … distinctive single tokens, or newline-flattened input, because prose wraps across lines and a multi-word pattern can split mid-match," and `modules.md` §3.4 states the falsification **target-dependents** candidate "re-runs §3.2.2's reference enumeration as its search" — so the safety-net's OWN completeness (target-dependents) candidate searches inherit wrap-tolerance via the binding. The residual (a target-existence `expected-match` candidate whose expected pattern wraps) is **conservative** — a wrapped-miss yields a false-*falsify* (reopens an entry that actually holds → one extra cycle), never a shipped false completeness claim. So the F4 harden earns nothing load-bearing. basis: read `anneal-dev/spec/bindings.md` (line 41, the wrap-tolerant search-binding row) + `modules.md` §3.4 (408–411, "target-dependents candidate re-runs §3.2.2's reference enumeration as its search"). [convergence note: this is a cycle-1 deepening, not a D-delta — it refines D1's committed resolution before any terminal.]

### Cycle 2 — convergence-attempt intent-falsification finding (artifact: `.cycle-2.intent-falsification.md`; subagent `ab66c55aea61feb0f`, opus)
- F10 [VERIFIED — surfaced → ACTIONED by D3.1] D3's "checked present" left the existence-checker unnamed → would degrade the disposition to a §3.1 **strong surfacer** (or forbidden operator-detection, §1), failing D3's OWN practice-8 structural-enforcement classification (an unnamed-checker rule is prose suggestion) and reproducing the F13 self-assertion hole D3 exists to close. The intent pass routed it [VERIFIED — surfaced] (pure judgment — D3's own basis/target/dependents hold → no mechanical-candidate); the working context **ACTIONS** it as a load-bearing improvement (naming the checker is what makes D3 *bind* at all). → D3 amended to D3.1. basis: subagent `ab66c55aea61feb0f` artifact — read `core.md` §3.1 (139–154 bind-vs-surface) + §1 (69–78 operator-detection malformed) + §5.1 (1043–1047 malformed-on-absence → §4.3) + §3.2 (187–190 verifiers re-open each citation). R1 + R2 attacked → both **SERVED** (D1/D2 hold; see artifact per-R# lines).

### Cycle 3 — convergence re-attempt intent-falsification finding (artifact: `.cycle-3.intent-falsification.md`; subagent `a0d22986f4dfa89c2`, opus) — CONVERGED-CLEAN
- F11 [VERIFIED — surfaced] D3.1's bind rests on the §3.2 (`:187-190`) standing mandate "Verifiers re-open each citation" reaching F-track **disposition** bases — but §4.3's four enumerated verify legs (`core.md:920-948`) do NOT restate it as an explicit leg. INHERITED enforcement surface — the existing `surfaced` + `deferred (a)/(b)` dispositions rest on the identical arrangement (`core.md:1030-1041`); D3.1 introduces NO new gap, NO operator-detection (checker = verify isolated, `:901-905`); no positive search falsifies the bind (spec text supports it) → pure judgment. **Terminal; feeds operator step-4. SPAWNED follow-up filed** → `dev-notes/backlog/verify-disposition-citation-reopen-explicit-leg.md` (file-not-fold per subagent disposition; firms ALL dispositions, separate from D3.1 which ships on the inherited surface). basis: subagent `a0d22986f4dfa89c2` artifact — read `core.md` §4.3 (920-970), §3.2 (187-190), §5.1 (1030-1047), §3.1 (139-154), §1 (69-78).

### Cycle 3 — F-entry resolutions (cycle-1 investigation findings → terminal at convergence)
- F1 [VERIFIED — addressed by D1] wrap-tolerance correctly NOT in the domain-general basis rule; enforced at practice 4 + bindings.md (the close). basis: D1.
- F2 [VERIFIED — addressed by D1] trip-2 = adherence gap; practice-1 three-form residual accepted. basis: D1.
- F3 [VERIFIED — addressed by D1] safety-net binds + caught both trips = the accepted enforcement form. basis: D1.
- F4 [VERIFIED — non-issue] the harden gap does not exist — bindings.md:41 already binds the safety-net's own completeness searches wrap-tolerant (closed by F9). basis: F9.
- F5 [VERIFIED — addressed by D2] coverage-check verifies the conclusion not recorded-query fidelity → watched (V-entry), not built, at n=1. basis: D2.
- F6 [VERIFIED — addressed by D2] soundness preserved by the separate-checker; audit-trail degradation tracked via the V-entry catcher. basis: D2.
- F7 [VERIFIED — addressed by D3.1] §5.1 `deferred` (a)/(b) → sub-case (c) added. basis: D3.1.
- F8 [VERIFIED — addressed by D3.1] owed-artifact existence is the evidence-bearing artifact; checker named (verify §4.3 re-check). basis: D3.1.

## D-track
<!-- design decisions appended below, append-only, fixed-shape ledger lines -->

### Cycle 1 — design

- **D0 [PENDING] Scope.** The live-spec homes of the three contracts: `core.md` §5.1 finding-state #3 `deferred` (R3 — add sub-shape (c)); `core.md` §4.1.4 + `modules.md` §3.3 (brief (d)) + §3.4 (candidate form) (R1 harden — falsification candidate-search wrap-tolerance); plus `dev-notes/validation-watch/` (R2 — new V-entry; R3's canonical owed-artifact home). NON-edits this run: practice 4 (R1 — already mandates wrap-tolerance; no change), `core.md` §3.2/§4.1 (R1 — wrap-tolerance correctly NOT domain-general). Render dependents (plugin `references/foundations.md` §finding-states, `phases/investigate-design.md` §convergence, `references/tracker.md` §3.4) NOT edited — queued to campaign ⑥ render-debt. basis: TBD — wrap-tolerant corpus search this cycle (queries to record); held [PENDING] until search-established.

- **D1 [CONDITIONAL] (R1, Fork A) Close completeness-search as safety-net-adequate + harden the falsification dispatch's own candidate searches.**
  (a) target: `core.md` §4.1.4 + `modules.md` §3.3 brief (d) + §3.4 candidate form (the harden); `completeness-search-enforcement.md` (the close).
  (b) shape: (i) CLOSE — the search-time wrap-tolerance discipline is adequately enforced (practice 4 for audits; the convergence safety-net for completeness claims, F2/F3); trip-2 was an adherence gap (F2); per practice 1 the three structural-enforcement forms against the search-completeness failure shape: **mechanical** fails (can't cheaply auto-verify an arbitrary grep is wrap-safe — cited in item), **structural** (basis-shape check "recorded search single-token-or-flattened") fails (the flatten-and-loop form creates the harness loop-hang failure mode — cited in item), **safety-net** is the accepted form (in place, caught both trips) → residual recorded as observation per practice 1. (ii) HARDEN — the one non-redundant fix: the falsification dispatch's candidate-search form (`modules.md` §3.4 candidate / §3.3 brief (d)) gains a wrap-tolerance requirement (single-token-or-flattened), the search-context practice 4 does NOT cover (F4).
  (c) acceptance: a falsification candidate's search is wrap-tolerant per the §3.4 candidate form; no new search-time mandate added to the domain-general basis rule (§3.2/§4.1 untouched).
  (d) side effects/failure modes: the harden adds one clause to the candidate form; if the operator prefers pure-close (no harden) the F4 hole stays accepted-residual; if mandate-search-time, friction on every search + loop-hang hazard (rejected, cited).
  (e) basis: F1/F2/F3/F4; assumption (operator-resolvable): that the F4 harden is worth one clause vs pure-close. [CONDITIONAL on operator's close+harden / pure-close / mandate choice.]
  - considered: pure-close (rejected: leaves F4's safety-net-self-dependency hole open — but operator may accept); mandate search-time wrap-tolerance (rejected: friction on every search + the flatten-and-loop loop-hang hazard, both cited in `completeness-search-enforcement.md`).

- **D2 [CONDITIONAL] (R2, Fork B) Convert basis-query-fidelity to a validation-watch V-entry; no forcing function at n=1.**
  (a) target: `dev-notes/validation-watch/` (new V-entry) + `basis-recorded-query-fidelity.md` (close-to-watch).
  (b) shape: a new V-entry (correctness-watch; watch-kind = recorded-query-fidelity; catcher = the convergence coverage-check / a future operator catch; closing rule = a recorded-but-false-as-written query passes the mechanical check, OR n≥2 recurrence). No kernel mechanism added (practice 7 no-over-build at n=1; soundness is preserved by the separate-checker, F6 — only audit-trail degrades).
  (c) acceptance: the V-entry exists with watch-kind + catcher + closing rule; the backlog item archives as converted-to-watch.
  (d) side effects: none (no kernel change). If the operator prefers building forcing-function-(b) now (the coverage-check compares the AI's recorded query against the subagent's re-derived candidate, flags a mismatch), that is the alternative — a §4.1.4 coverage-check clause.
  (e) basis: F5/F6; the item's own n=1 recommendation ("do NOT reflexively add machinery at n=1"). [CONDITIONAL on operator's V-entry / build-now choice.]
  - considered: build forcing-function-(b) at n=1 (rejected: practice-7 no-over-build — soundness already preserved, n=1, the item itself recommends watch; operator may override).

- **D3 [PENDING] (R3) Add `core.md` §5.1 deferred sub-shape (c): deferred-pending-authoring.**
  (a) target: `core.md` §5.1 finding-state #3 `deferred` (amendment — add sub-case (c)); `spec/glossary.md` "Finding disposition" entry (check if it enumerates deferred sub-cases → dependent).
  (b) shape (amendment delta): add a third sub-case to `deferred`: **(c) deferred-pending-authoring** — where the deferral's discharge is the *authoring of an artifact* (the trigger/watch lives in that to-be-authored artifact, distinct from (a)'s existing-decision cite and (b)'s existing-trigger cite), the basis must cite the artifact's path and the artifact must **exist** (checked present) before the finding records `[VERIFIED — deferred to <path>]`. Stated domain-generally (the artifact kind is instance-defined; framework-dev's is a validation-watch V-entry). The existing-artifact existence is the un-fakeable evidence.
  (c) acceptance: a finding deferred-pending-authoring cannot reach `[VERIFIED — deferred]` unless its cited `<path>` exists; faking the disposition requires authoring the artifact.
  (d) side effects/failure modes: composes with (a)/(b) — a third sub-shape, not replacing them; the [READY] gate's "no load-bearing finding short of [VERIFIED]" now binds the owed artifact into existence; Missed-dependents → glossary "Finding disposition" + plugin render (deferred to ⑥). Leakage check: must NOT hardcode "V-entry" in domain-general `core.md` (V-entry is the framework-dev binding) — state the artifact-kind instance-defined.
  (e) basis: F7/F8; classifiable structural enforcement (practice 8) earning n=1 — the candidate fix in `deferred-finding-owed-artifact-forcing-function.md`. Amendment completeness (§3.2.2): references to §5.1 `deferred` enumeration across the corpus = search this cycle (D0).

### Cycle 1 — design (scope search-established + Fork-A refined)

- **D0 [VERIFIED] Scope.** The ONLY kernel edit is `core.md` §5.1 finding-state #3 `deferred` (D3 — add sub-shape (c)). D1 (Fork A) and D2 (Fork B) resolve to NO kernel edit (close / V-entry). **§5.1 `deferred` amendment dependents (search-established):** `glossary.md` "Finding disposition" (`:343-347`) enumerates the four disposition NAMES and indexes `core.md` §5.1 for bodies → adding a sub-case *under* `deferred` does NOT change the four-name enum → dependent, **no edit** (indexes, doesn't restate sub-cases); the other `[VERIFIED — deferred]` references (`core.md:653,797`; `glossary.md:254,456-458`; `modules.md:119`) cite the disposition **generically** (impl routing, auto-battle convergence-exception) → unaffected by a new sub-case → **no edit**; plugin render (`references/foundations.md` finding-states) → **deferred to campaign ⑥** render-debt. basis: wrap-tolerant searches (recorded): `rg "deferred" spec/ development-process.md foundation.md anneal-dev/spec/` (12 matches, partitioned above) + `rg "surfaced" spec/` (disposition-enum home = `core.md` §5.1 + glossary index) — observable fact: the only sub-case-enumerating site is `core.md` §5.1 (1018-1029); every other match cites `[VERIFIED — deferred]` as a whole.

- **D1 [CONDITIONAL → refined: pure-close] (R1, Fork A) Close `completeness-search-enforcement` as safety-net-adequate; NO kernel edit (the harden is redundant).** Refines the cycle-1 D1 (close+harden) on F9: the wrap-tolerance discipline is enforced at **every relevant level** — `development-process.md` practice 4 (audits + dispatched audit subagents, explicit), and `anneal-dev/spec/bindings.md` binds the framework `search` wrap-tolerant, which reaches the convergence safety-net's own target-dependents completeness candidates (F9). Trip-1 (boundary-regex) was caught by the convergence pass (F7-of-`framework-spec-cleanup`); trip-2 (bare multi-word line-greps) is an **adherence gap** against practice 4's explicit "incomplete audit" rule (F2), caught by the operator. **Practice-1 three-form enumeration of the adherence-gap residual:** *mechanical* — fails (no cheap auto-verify that an arbitrary grep is wrap-safe; cited in item); *structural* (basis-shape "recorded search single-token-or-flattened") — fails (the flatten-and-loop form creates the harness multi-line-loop-hang failure mode; cited in item); *safety-net* — the **accepted** form (in place, caught both trips, and itself wrap-tolerant via bindings.md, F9). → residual **accepted, recorded as observation** per practice 1. The item archives as resolved.
  (e) basis: F1/F2/F3/F9 + `completeness-search-enforcement.md` (the two-trip record + the loop-hang hazard). [CONDITIONAL: operator-resolvable — the residual-acceptance + "no harden" is the operator's call per practice 1 + the item's "decide whether to act before designing"; recommendation = pure-close.]
  - considered: close+harden the falsification candidate-search (rejected: F9 — redundant; bindings.md already binds it wrap-tolerant; the residual is conservative false-falsify, non-load-bearing); mandate search-time wrap-tolerance in domain-general §3.2 (rejected: friction every search + the flatten-and-loop loop-hang hazard + Leakage — wrap-tolerance is a corpus-prose concretion, correctly NOT domain-general).

### Cycle 1 — standardized inspection pass (artifact: `.cycle-1.standardized-pass.md`)
In-scope lenses (the cycle's work touches `core.md` §5.1 sub-case (c) = D3; D1/D2 no edit) — all clean with this-cycle basis:
- **Bloat** — clean: D3 adds one load-bearing sub-case (forces the owed-artifact existence); no restatement/hedge. basis: D3 (b)/(c).
- **Fragmentation** — clean: D3 is the §3.1 evidence-bearing principle *applied* to the `deferred` disposition, not a second copy of it (consolidation, not duplication). basis: read `core.md` §3.1 (111-169, the principle) + D3 (the application).
- **Unenforced-rule** — clean: D3's violation is visible (cited `<path>` checked present; absent → can't reach `[VERIFIED — deferred]`); evidence-bearing per §3.1. basis: D3 (c) acceptance.
- **Undefined-shorthand** — clean: "deferred-pending-authoring" is a single-home sub-case label in `core.md` §5.1 (not a new top-level disposition; the glossary's four-name enum unchanged) → single canonical defining sentence suffices, no glossary entry (per practice 10, like campaign2's single-home terms). basis: read `glossary.md` "Finding disposition" (343-347, four-name enum) + practice 10.
- **Missed-dependents** — clean: D0 search enumerated the §5.1 `deferred` dependents (glossary indexes, generic-cite refs, plugin render→⑥); none restate the sub-cases. basis: D0 search.
- **Leakage** — clean (with design guard): D3 in domain-general `core.md` §5.1 states the owed-artifact **kind instance-defined** (corpus-evolution's is a V-entry) — does NOT hardcode "V-entry" in the domain-general spec. basis: D3 (b) shape + (d) Leakage guard.
- **Lossy-render / Over-claimed-verification** — out of scope this cycle (cited): no render committed (the §5.1 render is deferred to ⑥); no paraphrase of a source rule introduced.

- **D3 [VERIFIED] (R3) Add `core.md` §5.1 deferred sub-shape (c): deferred-pending-authoring.** Body per the cycle-1 D3 entry above (target/shape/acceptance/side-effects/basis), now locked: the amendment delta + amendment-completeness (D0 search-established: `core.md` §5.1 sole sub-case-enumerating home; glossary indexes; render→⑥) are detailed enough that impl introduces no new design decision (realization output = the exact sentences, produced at impl). basis: F7/F8 + D0 amendment-completeness search; classifiable structural enforcement (practice 8) at n=1.

### Cycle 1 — fresh-session implementability test: PASSED
Per-implementer-step external evidence (each step's edit site / target cited):
- **Step D3** (edit `core.md` §5.1 `deferred`): located read `core.md` §5.1 (1018-1041) — observable fact: `deferred` enumerates cases (a)/(b) at 1018-1029; the `[VERIFIED — <disposition>]` suffix rule + malformed-on-absence at 1043-1047. A fresh session adds sub-case (c) per D3's delta + the suffix form. PASSED.
- **Step D1** (no kernel edit; archive `completeness-search-enforcement.md` + record the practice-1 residual observation): file exists at `dev-notes/backlog/completeness-search-enforcement.md` (read this run). PASSED.
- **Step D2** (no kernel edit; author the V-entry + archive `basis-recorded-query-fidelity.md`): the V-entry home `dev-notes/validation-watch/` exists (ls confirmed this run); the item file exists at `dev-notes/backlog/basis-recorded-query-fidelity.md` (read this run); D2's shape specifies watch-kind/catcher/closing-rule. PASSED.

**Cycle 1 status:** design complete (D0/D3 [VERIFIED]; D1/D2 [CONDITIONAL] with committed recommendations); standardized pass clean; fresh-session test PASSED. → proceed to the **convergence cycle** (cycle 2): new-surface investigation + intent-falsification pass (fresh-context, criteria-first) then — if intent-clean — mechanical falsification over the [VERIFIED] D-entries (D0, D3) + the technical-basis shapes of the [CONDITIONAL] entries (D1, D2).

### Cycle 2 — convergence-attempt intent-falsification D-delta (append-only)
- **D3 [INVALIDATED]** (superseded by D3.1, per F10 — checker unnamed; would ship as surfacer not bind).
- **D3.1 [VERIFIED] (re-forms D3 — closes F10) (R3) §5.1 `deferred` sub-shape (c) deferred-pending-authoring, producer-independent existence-checker NAMED.**
  (a) target: `core.md` §5.1 finding-state #3 `deferred` (amendment — add sub-case (c)).
  (b) shape (amendment delta): add sub-case **(c) deferred-pending-authoring** — where the deferral's discharge is the *authoring of an artifact* (the watch/trigger lives in that to-be-authored artifact; distinct from (a)'s existing-decision cite and (b)'s already-existing observable trigger), the basis **cites the artifact's path**, and that path is a basis citation **re-checked by verify's isolated context (§4.3) and by the convergence cycle re-opening each citation (§3.2)** — its absence makes the basis malformed (§3.1), so the finding cannot reach `[VERIFIED — deferred to <path>]` and verify forces [ISSUES FOUND] (§5.1 malformed-on-absence + §4.3 finding-status ledger). The existence-check binds via the EXISTING separate-checker — NOT the producing context's self-assertion, NOT operator-detection (§1). Domain-general — the artifact KIND is instance-defined (corpus-evolution's is a validation-watch V-entry).
  (c) acceptance: a finding deferred-pending-authoring cannot reach `[VERIFIED — deferred to <path>]` unless `<path>` exists at verify's citation re-check; faking the disposition requires authoring the artifact (= the work). The checker is named (verify §4.3 / convergence citation re-check), so the rule is practice-8 **structural enforcement that binds**, not prose suggestion.
  (d) side effects: composes with (a)/(b) — distinct referents (decision-ID / existing-trigger-class / to-be-authored-artifact-path); no operator-detection introduced (§1); Missed-dependents → glossary "Finding disposition" indexes (no edit), plugin render → ⑥; the [READY] gate's "no finding short of [VERIFIED]" + §4.3 re-check bind the owed artifact into existence.
  (e) basis: F7/F8/F10; read `core.md` §5.1 (1018–1047) + §4.3 (920–970 verify re-check + finding-status ledger) + §3.2 (187–190 verifiers re-open each citation) + §3.1 (139–154 bind criterion). Classifiable structural enforcement (practice 8) at n=1 — now binding (checker named).
  - considered: tighten existing (b) instead of adding (c) (rejected: (b)'s trigger is an *already-existing* observable event class — file change / verification-output / dependency change; the deferred-pending-authoring discharge is *authoring an artifact that does not yet exist* — a genuinely distinct shape per the item + subagent; folding it into (b) would overload (b)'s "observable event class" with a not-yet-existing carrier — (c) is the minimal honest sub-case, not enum bloat); add (c) but leave checker unnamed (rejected per F10 — ships a surfacer mislabeled as structural enforcement).

**Cycle 2 status:** mechanical falsification pass **SKIPPED: intent-delta this cycle** (per `core.md` §4.1.4 / `modules.md` §3.4 skip carve-out). D-delta: D3 → D3.1 (non-behavior-preserving — changes what the rule prescribes → re-opens the intent pass). → NOT converged; cycle 3 re-attempts convergence over the amended design.

### Cycle 3 — CONVERGENCE CLEAN → [READY]
Convergence cycle 3 is **zero-D-delta**: investigation (new surfaces via the falsification passes) + standardized pass (no lens newly in scope — no new rule-text this cycle; cycle-1 accounted whole; `.cycle-3.standardized-pass.md`) + intent-falsification (CLEAN — F11 [VERIFIED — surfaced], no mechanical-candidate; `.cycle-3.intent-falsification.md`, subagent `a0d22986f4dfa89c2`) + mechanical falsification (ALL entries HOLD; `.cycle-3.falsification.md`, subagent `a75d68f4fdf34a922`; orchestrator coverage-check PASS incl. clause (v) intent-clean re-derivation). New-surface requirement met.

**Fresh-session-implementability result line: PASSED** (design unchanged since D3.1 — which only NAMED the checker within the same §5.1 sub-case (c)):
- U1 (D3.1): located reads `core.md` §5.1 (1018-1047, `deferred` (a)/(b) + suffix-rule + malformed-on-absence) + §4.3 (920-970 verify re-check + finding-status ledger) + §3.2 (187-190 re-open each citation). PASSED.
- U2 (D1/D2 housekeeping): `dev-notes/backlog/{completeness-search-enforcement, basis-recorded-query-fidelity}.md` exist; `dev-notes/validation-watch/` exists. PASSED.

**Open judgment residuals feeding operator step-4 ([VERIFIED — surfaced]):** F11 (D3.1's bind rests on the §3.2 "re-open each citation" mandate reaching F-track disposition bases — an INHERITED surface §4.3 doesn't restate; spawned follow-up filed `verify-disposition-citation-reopen-explicit-leg.md`, ships separately).

### Foundation-invariant focusing artifact (method-kernel edit — FOCUSING INPUT for the operator's step-4 soundness; register-clean ≠ sound)
D3.1 is the only kernel edit (`core.md` §5.1 deferred disposition). Per-touched-invariant ledger (`dev-notes/foundation-invariants/`):
- **INV-1 basis-rule** (anchor: Zave-Jackson designation) — TOUCHED — **HOLDS, strengthened**: the deferred disposition's basis now cites a re-checkable path (designation-grounded), deepening basis-completeness.
- **INV-2 complete-state** (anchor: Zave-Jackson S,K⊢R) — TOUCHED — **HOLDS, strengthened**: a deferred finding's owed artifact is bound into existence (held-visible), can no longer silently vanish.
- **INV-3 verify-isolation** — TOUCHED (leans on) — **HOLDS**: D3.1's bind RUNS THROUGH verify's isolated citation re-check; relies on verify-isolation, does not modify it. **FOCUS POINT for operator soundness**: the existence-check's producer-independence IS verify-isolation — confirm D3.1 leans on (doesn't weaken) it.
- **INV-4 loopback-not-settled** (anchor: Platt recycle + Quine-Duhem) — TOUCHED — **HOLDS, strengthened**: an absent owed-artifact → verify [ISSUES FOUND] → loopback; adds a reopen trigger, settles nothing prematurely.
- **INV-5 exclusion-via-named-falsifier** (anchor: Platt exclusion + WRSPM) — TOUCHED — **HOLDS, strengthened**: the existence-check is a named falsifier (verify re-opens the citation; absence falsifies) — extends named-falsifier coverage to the deferred-pending-authoring disposition. Reinforces the keystone.
All 5 HOLD (D3.1 strengthens INV-1/2/4/5, leans on INV-3). Operator soundness focus: INV-3 (bind rests on verify-isolation) + F11 (inherited surface). Register-clean does NOT certify soundness — the operator's design-vs-intent verdict is irreducible.

### Impl plan (persisted `.impl-plan.md`)
- **U1 [kernel] — D3.1**: edit `spec/core.md` §5.1 finding-state #3 `deferred` — add sub-case (c) deferred-pending-authoring (checker named: verify §4.3 citation re-check; domain-general, artifact-kind instance-defined). Rule-corpus edit → skill-craft pre-edit gate fires; dispatched in-place under the Integrity check. Scope: `spec/core.md` §5.1 only. First (no dependency).
- **U2 [non-kernel housekeeping] — D1 + D2**: (D1) `git mv dev-notes/backlog/completeness-search-enforcement.md` → `archive/` with the resolution (safety-net adequate; practice-1 residual accepted; harden redundant per bindings.md F9). (D2) author the V-entry in `dev-notes/validation-watch/` (watch-kind recorded-query-fidelity; catcher = convergence coverage-check; closing rule = a recorded-but-false-as-written query passes the mechanical check OR n≥2) + `git mv dev-notes/backlog/basis-recorded-query-fidelity.md` → `archive/`. NOT a rule-corpus edit (dev-notes) → no skill-craft gate. Scope: `dev-notes/` only.
- Parallel-eligibility: U1 (`spec/core.md`) ∥ U2 (`dev-notes/`) — disjoint scopes (no shared file). **GATED on the operator's [READY] resolution of D1/D2** (the [CONDITIONAL] dispositions); operator override (harden / build forcing-function) re-opens U1/U2.
- Render-follow: U1's §5.1 render dependent (plugin `references/foundations.md` finding-states) → campaign ⑥ render-debt (spec-only cadence).

**STATUS: [READY].** Halting per the run-until-(n) cadence for the operator's step-4 (method-kernel soundness verdict) + `next phase` selection + the two [CONDITIONAL] resolutions (D1/D2).

## Implement (operator selected `next phase` 2026-06-05; D1/D2 recommendations accepted, D2 revised per operator's V-vs-backlog clarification)
### U1 [kernel] — D3.1 — DONE (dispatched subagent `af5a8060cffa5f4a5`, in-place, opus)
- Subagent **invoked skill-craft** (Skill tool) before the edit → pre-edit gate passed (no block — the `anneal-dev-impl-skillcraft-gate` dispatch path worked when the brief explicitly required *invoke*, not just load). Self-check: Lossy-render / Missed-dependents / Undefined-shorthand / Over-claimed-verification all clean; change-set-vs-listed-scope = ∅ outside §5.1. No loopback.
- **Orchestrator Integrity check: PASS** — `git diff --stat -- spec/` = `spec/core.md` only (13 insertions); the diff is exactly the (c) sub-case appended after (b), before `surfaced`. No other spec file, no out-of-scope change. (The dev-notes/backlog working-tree changes are the orchestrator's own session bookkeeping, not the subagent's — cleanly attributable.)
- **Watched nit (for blind verify):** (c) appended after (b)'s close without the "or" list-connector ("one of: (a)…; or (b)…). (c)…"). Behavior-preserving prose-coherence call; NOT pre-flagged to verify (independence preserved).
### U2 [non-kernel housekeeping] — D1 + D2(revised) — DONE (orchestrator in-context; dev-notes, not work-product, no skill-craft gate)
- **D1**: `completeness-search-enforcement.md` → resolution recorded (safety-net adequate; practice-1 three-form residual accepted; harden redundant per bindings.md F9) → `git mv` to `archive/`.
- **D2 (REVISED — operator's V-vs-backlog clarification: V-entries are post-ship effect-watches, not pre-impl considerations)**: `basis-recorded-query-fidelity.md` **kept as an active backlog item with watch criteria inline** (watch-kind: recorded-query-fidelity; catcher: convergence coverage-check; closing rule: a recorded-but-false-as-written query passes the mechanical check OR n≥2 → then build forcing-function-(b)). **NOT converted to a V-entry, NOT archived** (the original D2 plan; revised in-flight). The `v-entry-is-post-ship-only` side-quest will codify why.

## Verify (isolated) — [ISSUES FOUND] → loopback (cycle 4)
Two kernel-independent legs dispatched (both opus, both blind to the watched nit — independence preserved):
- **Leg 1 — anneal isolated verify** (subagent `a6a862580361dd1e3`): **[ISSUES FOUND]**, one finding V-F1. All else PASSED: planned-vs-actual = D3.1 on every element; R1↔D1 / R2↔D2 / R3↔D3.1 covered; lenses clean; **(c) binding confirmed REAL** (§3.2:187 re-open each citation + §3.1:139-154 malformed weak-basis + §4.3:965-970 [ISSUES FOUND] — genuinely binds, not prose-suggestion); render-fidelity N/A (no plugin render changed — git diff = spec/core.md only); cross-refs resolve.
- **Leg 2 — skill-craft form self-review** (subagent `a6fc5b3e5f3fc3155`): one **notable/fix-now** = same V-F1; 5 observations keep-as-is (no naked-judgment; binding crisp/un-softened; earns-its-place vs Additive-reflex — (c) provably disjoint from (b); instance-binding correctly scoped, Leakage-clean). One borderline observation (the un-fakeable-evidence rationale sentence — keep now, flag for a future compression pass).
- **V-F1 [PENDING] — §5.1 deferred (a)/(b)/(c) connector / parallel-structure break.** The U1 render appended (c) after (b)'s terminal period without the `; or` disjunction connector, mis-signalling the closed sub-case set as two-members-plus-orphan; contradicts the doc's own multi-member-enumeration convention (§4.1.4:473-476, §5.2:1098-1106). basis: subagents `a6a862580361dd1e3` + `a6fc5b3e5f3fc3155` (both caught it independently, blind).
- **Loopback root-cause triage (§4.2.7, orchestrator on receipt):** **adherence-root** — the impl subagent mis-rendered the list structure against D3.1's explicit "same prose style and list structure" instruction (design correct; the impl self-check's Lossy-render lens mis-judged "authored prose, not a render" and didn't fire). No spec/framework gap (the instruction was unambiguous). Disposition: adherence-noted; the **verify safety-net caught it (both legs, blind)** — a verify-quality datapoint. Fix = behavior-preserving copy-edit (restore `; or`); D3.1 stands (no design change) → delta-verify on re-run.

### Loopback fix + delta-verify — [PASSED]
- **Fix (orchestrator spawn-fallback, behavior-preserving — per campaign-② precedent):** skill-craft invoked (gate); `spec/core.md` §5.1 (b)-close `is malformed).` → `is malformed); or` — restores the three-armed disjunction. Edit-as-Pareto: reduces the coherence defect, no semantic change. Integrity check: `git diff` = spec/core.md only (14 insertions, 1 deletion) — exactly the (c) addition + the one connector edit.
- **Delta-verify (isolated, subagent `a55d17570bc0b4220`): [PASSED]** — V-F1 closed (the bullet reads `one of: (a) …; or (b) …; or (c) …`, matching §4.1.4:473-476 + §5.2:1098-1106 convention); minimal regression confirmed ((c) semantics intact; only the connector changed). F12/V-F1 → [VERIFIED — addressed by the connector fix]. **Verify [PASSED].**

## Step-4 discharge (method-kernel edit — release loop step 4; HALTED for operator soundness + commit approval)
Commit (pending operator approval): `spec/core.md` (D3.1 — §5.1 deferred sub-shape (c)) + the session's dev-notes bookkeeping (backlog README cadence-levels/discipline, D1 archival, D2 watch-as-backlog, 3 spawned items, auto-battle datapoint-3 capture).
- **Render fidelity (practice 2)** → N/A: no plugin render changed (`git diff` work-product = `spec/core.md` only; `render-and-open-diff` disabled; render-debt → ⑥).
- **Spec-origin trace** → N/A: no `plugin/skills/*` in diff (git status: `spec/core.md` + `dev-notes/` only).
- **Practice-4 dependent audit** → grep cited (D0): `rg "deferred"` whole corpus → only `core.md` §5.1 enumerates the sub-cases; `glossary.md` "Finding disposition" indexes the 4 NAMES (no edit); other `[VERIFIED — deferred]` refs cite generically. Render dependent → ⑥.
- **Skill-craft full review on changed skill files** → N/A: no skill files changed.
- **Skill-craft self-review on framework-spec section** → subagent `a6fc5b3e5f3fc3155`: PASS-with-1-fix-now (V-F1, now fixed) + 5 keep-as-is observations (form clean: no naked-judgment, binding crisp, earns-its-place, instance-binding scoped).
- **Foundation-invariant check (method-kernel)** → the focusing-artifact ledger above (5 invariants HOLD; D3.1 strengthens INV-1/2/4/5, leans on INV-3 verify-isolation). FOCUSING INPUT for operator soundness; NOT a verdict.
- **Skill-craft self-review on skill-craft canonical** → N/A: no skill-craft canonical file in diff.
- **Practice-6 whole-document coherence** → subagents `a6a862580361dd1e3` (anneal verify, §5.1 coherence) + `a55d17570bc0b4220` (delta-verify, V-F1 closed + bullet coheres).
- **Cross-spec multi-file coherence** → N/A: single spec file (`core.md` §5.1); cross-refs (§3.1/§3.2/§4.3) intra-core; glossary indexes (no edit, no second rule-home).

**Operator soundness focus (the irreducible step-4 verdict):** (1) **INV-3** — D3.1's bind IS verify's isolated citation re-check; confirm it leans on (doesn't weaken) verify-isolation. (2) **F11** [VERIFIED — surfaced] — the bind rests on §3.2's "re-open each citation" reaching F-track disposition bases, an INHERITED surface §4.3 doesn't restate; spawned follow-up filed (`verify-disposition-citation-reopen-explicit-leg.md`), ships separately. Register-clean ≠ sound.
