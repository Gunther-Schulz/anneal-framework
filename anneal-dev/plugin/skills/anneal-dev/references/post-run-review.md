# Post-run review

This instance has not yet rendered its own post-run-review procedure
(deferred for the bootstrap skeleton — an optional analysis step outside
the core loop). Until it does, conduct a requested review directly from
the framework source: `anneal-framework/post-run-review.md` (the standing
questions + the state-aware Q7 walk), whose minimum shape is the
**Post-run review** term (`anneal-framework/spec/glossary.md`). Output to
the operator in-session; do **not** persist the review or its findings to
a project file (the **Post-run review** term, `anneal-framework/spec/glossary.md`
— persisting prior-run analysis pollutes future runs).
