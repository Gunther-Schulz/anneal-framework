# Post-run review

This instance has not yet rendered its own post-run-review procedure
(deferred for the bootstrap skeleton — an optional analysis step outside
the core loop). Until it does, conduct a requested review directly from
the framework source: `anneal-framework/post-run-review.md` (the standing
questions + the state-aware Q7 walk), whose minimum shape is
`anneal-framework/spec/modules.md` §4. Output to the operator in-session;
do **not** persist the review or its findings to a project file
(`modules.md` §4.2 — persisting prior-run analysis pollutes future runs).
