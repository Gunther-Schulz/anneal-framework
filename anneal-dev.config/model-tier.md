# Dispatch model-tier override (anneal-dev model-tier slot;
# anneal-dev/spec/bindings.md "Dispatch model tier").
# Set the value line below to your harness's top-tier model identifier
# — every anneal-dev dispatch (investigate/falsification/impl/verify)
# runs at this tier, never downgraded. Absent or empty inherits the
# session model (the harness default), also never downgraded below.

opus
